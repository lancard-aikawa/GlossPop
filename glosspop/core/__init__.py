"""「どのマシンか」「辞書がどこにあるか」を知らない層。

ここに入っているのは **入力から出力を作るだけ**のモジュール。辞書の置き場所
(`store`)・開いているフォルダ・設定ファイル (`config`) を一切知らない。

**この層だけが GlossPopApp と共有される**（→ `docs/design-notes.md`）。共有する
ものが 2 か所に分かれると、片方で直してもう片方で直し忘れて「同じ辞書なのに違う語が
リンクになる」という、**画面を見ても分からない壊れ方**をする。だから境界に名前を
付けてある。

**守ること: ここから `config` と `store` を import しない。**
`tests/test_core_boundary.py` が見張っているので、越えるとテストが落ちる。
人が気を付ける形にすると必ず越える —— 「ついでに開いているフォルダを見たい」は
毎回出てくるので、そのたびに**呼ぶ側から引数で渡す**こと（`timeline.py` が
`Linker` と `Document` を引数で受けているのがその形）。

置いてあるもの:

* `models`     — エントリ・関係・カテゴリ名の規則
* `htmlclean`  — 外部 HTML の許可制サニタイザ
* `render`     — Markdown / テキスト / HTML → HTML
* `linker`     — 本文への自動リンク（表記の木と境界の規則）
* `relations`  — 関係の解決（正規化・転送・曖昧さ）
* `documents`  — ファイルを「表示できるテキスト」に（文字コード・書式・位置）
* `timeline`   — 関係が読めるようになる順を計算する
* `doctor`     — 点検
"""
