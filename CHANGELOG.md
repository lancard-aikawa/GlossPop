# 変更履歴

版ごとに**変わったことだけ**を書く。使い方は [MANUAL.md](MANUAL.md)、入手と起動は
[README.md](README.md)。

**この file は Release の本文になる。** タグを打つと、その版の節がそのまま
GitHub Release に載る（[`.github/workflows/release.yml`](.github/workflows/release.yml)）。
節が無いままタグを打つと**ワークフローが止まる**ので、上げる前に書き足すこと
（`.\check.cmd ci` も見ている）。

見出しは `## <版番号>` の形にすること —— 抜き出しがこれを目印にしている。

## 0.13.0

**辞書として引く道と、渡す形を足した。** これまでは「本文を読みながら引く」側が
厚く、**辞書の側から通して見る**手段が薄かった。

**索引が増えた**（辞書の **📇 索引**）。登録した語が**本文のどこに何回出てくるか**を
並べる。用語ページの「この語が出てくる文書」は 1 語ずつだったが、こちらは辞書の側から
全部を見る。

- **いちばん見たいのは「出てこない語」。** 登録したのに本文に 1 度も出てこない語は、
  その辞書では**リンクにならない語**（表記が本文と違う、別名が足りない、など）。
  チェック 1 つでそれだけに絞れる
- 並べ替えは 出現の多い順 / 読み順 / カテゴリ順。文書を押すとビューアで開いて初出へ飛ぶ
- **索引は保存しない**（開くたびに読み直すので、外のエディタで書き換えたぶんも
  次に開けば追いつく）。読んだ文書の数と、打ち切り・読めなかった文書は必ず出す

**辞書一覧を五十音でも束ねられるようになった**（束ね方の選択。覚える）。カテゴリ順は
「どういう語か」で引く道、五十音は**辞書を通して引く**道で、どちらも残してある。
上の帯（中身のある行だけ）から行へ飛べる。

- **カタカナはひらがなに畳む**（「ジョバンニ」は さ行）。濁点も小さい字も同じ行
- **読みが無くても、見出しがかなならそこに並ぶ。** 並べられないのは**漢字の見出しで
  読みが書かれていない語**だけで、「読みなし」に集めて数を出す（黙って「あ」行に
  混ぜない）
- 五十音のときは**カードにカテゴリを出す**（見出しがカテゴリでなくなるため）

**読みをその場で書けるようになった。** 「読みなし」の束に欄が並び、手で書けば
その語はその行へ移る。**✨ 読みを下書き**を押すと AI が同じ欄を埋める ——
**入力欄は 1 つで、埋め方が 2 つ**。

- **AI は保存しない**（欄に入れるだけ）。**手で書いた欄は上書きしない**
- **かなでない読みと、AI が「分からない」と返したぶんは入らない**（理由が出る）。
  間違った読みは書かれていないより悪い（辞書の並びが狂う）
- 同じ用語名が複数あるものは AI に頼まない（どちらの読みか決まらないため）

**冊子として書き出せるようになった**（辞書の **📖 冊子**）。辞書ぜんぶを
**五十音順の 1 枚**にする。**辞書の書き出し (zip) とは別物**で、あちらはデータの
持ち運び、こちらは**読んで渡すため**。

- **Markdown**（GitHub・エディタ）と **HTML**（そのまま印刷。PDF もここから）
- 中身は 目次 → 五十音順の全項目。**巻末索引も入れられる**（本文を読むので任意）
- **文書内リンクは張っていない** —— 書き出した先で見出しの飛び先の作られ方が違い、
  どこかで必ず切れる（切れたリンクは「その語が無い」に見える）

## 0.12.0

**関係に「作中の時刻」を書けるようになった**（`when`）。**判明する位置とは別物**で、
あちらは*読者がいつ知るか*、こちらは*作中でいつ起きたか*。書いても何も伏せない。

- **先頭を西暦で書く**（`1560-06-12` / `1560-06-12 14:00` / 年だけの `1562` も可）。
  **うしろには元号でも作中の暦でも書ける** ——
  `1560-06-12 永禄三年五月十九日` のように書くと、並ぶのは先頭、**画面に出るのは全文**。
  元号だけでは並べられない（「天保三年」と「享保五年」の前後は変換表がないと決まらない）
- **書かなくてよい。** 時刻の付かない関係のほうが普通で、点検も黙る。
  書いたのに西暦として読めないときだけ「時刻が西暦で読めない」と挙がる
- **時系列の見せ方に軸が増えた**（**読む順** / **作中の時刻**）。作中の時刻なら
  **文書を開いていなくても出せる**ので、辞書全体でも時系列にできる。
  時刻の無い関係はいちばん下の帯にまとめ、**「書いていない」と「読めない」を
  数え分けて**出す
- **地図が時点で巻き戻せる。** スライダを左へ動かすと、その時点までに起きた関係
  だけが残る —— **進軍路が順に伸びる**。**時刻の無い関係は常に出す**（伏せると、
  時刻を 1 つ書いただけで他が全部消えたように見える）

**地図で重なる形を見分けられるようになった。** 同じ絵に重なる**線と領域は 1 つずつ
色が違う**（一覧の色札と同じ色）。点はどれも同じ色 —— 場所が 1 つなので取り違えない。

**地図の線と領域を、絵の上を順に押して作れるようになった。** 先に形を選んでから
語を押し、**押した数だけ点が増える**（Enter か **✓ 確定**で決定、Esc か **やめる**で
取りやめ）。それまでは点を置いてから種別を変えて頂点を足すしかなく、
**経路 1 本にいちばん手数がかかっていた**。

**地図の点を用語ごとの画像にできるようになった**（「用語の画像」。絵ごとに覚える）。
既定は丸のままで、**画像のある語だけが絵**になる。人物地図が一気に読める。

**直したもの**:

- **地図の編集で、キーボードの 2 回目が効かなかったのを直した。** 保存のたびに図を
  描き直していたため、矢印キーは 1 回動かすと焦点が外れ、＋ と Delete も 1 操作ごとに
  Tab で戻る必要があった（マウスでは 1 ドラッグ 1 保存なので気付けなかった）
- **絵の外へは置けなくなった**（縁で止まる）。外に出た点は座標だけ書かれて
  画面に出ないので、点検を開くまで気付けなかった
- **点検が「絵の下へのはみ出し」も見るようになった。** 絵の縦横比を実際に読んで
  上限を決める（縦長の絵で `y` が 1 を超えるのは今までどおり正常）
- **まとめた（統合した）ときに、関係の項目が静かに消えうる作りを直した**
  （関係を項目名の並べ書きで組み立て直していた）

**サンプル**: `samples/戦国時代/` に作中の時刻を入れた。時系列は
1560 → 1562 → 1570 → 1582 の順に並び、**桶狭間の地図は時点で巻き戻すと
2 つの進軍路が順に伸びる**。

## 0.11.0

**相関図に「地図」が増えた。** 絵を 1 枚敷いて、その上に用語を置ける（見せ方は
全部で 6 つになった）。実在の地図でなくてよく、**架空の世界の地図でも同じように
使える** —— 必要なのは 1 枚の絵だけ。

- **点・線・領域**の 3 つを置ける。地名は点、道や進軍路は線、領国は領域。
  **経路そのものを 1 つの用語にできる**ので、「〇〇説」に線を書けば
  **説ごとに違う経路が同じ地図に重なって出る**
- **絵は画面から入れられる**（地図の見せ方の **🖼 絵**）。`.svg` `.png` `.webp`
  `.jpg` `.gif`、8 MB まで。辞書に何枚でも置けて、上の選択で切り替わる
- **地図の上で作れる。**「置く」を入れると丸が出て、掴んで動かせる（矢印キーでも）。
  **線分の中点の小さな丸で頂点を足し、丸に乗せて出る ✕ で消せる**（＋ と Delete でも）。
  種別（点 / 線 / 領域 / 地図から下ろす）も一覧で変えられる
- **↩ で 1 つ戻せる。** 掴んで離した時点で保存されるので、消した頂点を取り返す道
- **出すものはチェックで選べ**、**名前を出すかも切り替えられる**（AI に描かせた地図には
  地名が入っていることが多いので、そのときは外す）
- **出していないものは必ず図の下に出る**（座標の無い語・別の絵にいる語・片端が地図に
  無い関係・チェックを外した語・重なって畳んだ名前）

**用語ごとの画像を置けるようになった。** 用語ページの **🖼 画像を入れる** から。
一覧のカードと吹き出しにも出る。**語り手の顔とは別物**（顔は辞書に 1 枚、こちらは
語ごと）で、カテゴリを移せば画像も一緒に動く。

**相関図を画像として保存できるようになった**（**⬇ 画像**）。PNG は資料に貼る用、
SVG は拡大できる。**6 つの見せ方すべてで使える**。地図の絵は画像の中に畳み込むので、
渡した相手の環境でも出る。

**「ここまで読んだぶんだけ」の相関図。** ビューアの上に重ねて開いたとき、
**まだ本文に出てきていない語とその関係を伏せられる** —— 読み進めるほど図が育つ。

**辞書の点検が地図を見るようになった。** 座標だけ書かれている（どの絵にも出ない）／
その名前の絵が無い（絵を消したあとの用語）／座標が絵の外 —— どれも**画面には何も
出ない**ので、点検で言わないと気付けなかった。

**用語ページから地図へ行けるようになった**（**🗺 地図で見る**）。まだ置いていない語では
**🗺 地図に置く** になる。

**辞書の書き出し (zip) に地図の絵と用語ごとの画像が入るようになった。** 入れないと
渡した先で**形だけ揃って地図が真っ白**になっていた。**取り込みでは消えない**
（zip に入っていない画像はそのまま残る）。

**直したもの**:

- **青空文庫が文字化けしていたのを直した。** charset を名乗らないサイトの文字コードを
  中身から判断するようにした（坊っちゃん 89,216 字が正しく読める）
- **中身の無いページを「読めた」と言っていたのを直した。** 空の枠だけを返すサイトで、
  真っ白な画面をエラーも出さずに表示していた
- 地図の形を**線から点へ変えても線が残っていた**のを直した（点検が「形が 2 つ」と
  言い続ける状態になっていた）

## 0.10.4

- **exe にアイコンを付けた。** ダブルクリック用の `glosspopw.exe` は画面左上と同じ
  青緑のしるし、コマンドライン用の `glosspop.exe` は端末らしい `>_`。
  **2 つ並んでもどちらを起動するのか一目で分かる**

## 0.10.3

- **AI で下書きするたびに黒い窓（コマンドプロンプト）が開いていたのを直した。**
  その窓を閉じると下書きが失敗し、`[WinError 6] ハンドルが無効です` になっていた
- **ダブルクリック用の実行ファイルを分けた。** `GlossPop\glosspopw.exe`（`w` 付き）が
  窓の出ない起動用で、`glosspop.exe` はコマンドライン用。**起動するのは `w` 付きのほう**
- 0.10.1 で入れた「コンソールから離脱する」細工はやめた。あれが黒い窓の原因だった
  （親にコンソールが無いと、子のコマンドが自分の窓を作るため）

## 0.10.2

- **Release の本文が、その版の変更点になった。** これまでは毎回まったく同じ説明が
  出ていて、「何が変わったのか」がどこにも書かれていなかった。この `CHANGELOG.md` の
  節が Release の先頭に載り、その下に初めての方向けの短い導入が続く

## 0.10.1

- **窓を閉じたらサーバも終わるようにした。** exe をダブルクリックすると付いてくる
  コンソール窓は、アプリの窓が開いた時点で引っ込む。これまでは出しっぱなしで、
  そちらを閉じると**サーバだけ死んでアプリの窓が残り**、以後どこを押しても接続
  エラーになっていた
- 窓を閉じてから実際に止まるまで 20 秒ほどかかる（開いているページからの生存確認が
  途絶えるのを待つため）。ブラウザで開いて使っているとき（`serve`）は止まらない

## 0.10.0

- **文字の大きさを選べるようにした**（⚙ の 表示 → 小 / 中 / 大 / 特大）。本文を基準に
  注記も見出しも同じ比で動くので、比を崩さずに全体を大きくできる。選んだ瞬間に効く。
  **相関図の図の中だけは変わらない**（配置の計算に関わるため。図は拡大縮小で見る）
- **読み上げが使えなくなっていたのを直した。**「この環境には読み上げに使える音声が
  ありません」しか出ない状態だった（音声の一覧が届くのを 1.5 秒しか待っていなかった）
- 読み上げで**オンラインの音声**（🌐 印）も選べるようにした。既定は今までどおり OS の
  ローカル音声で、🌐 を選んだときだけ本文がその提供元へ送られる（画面にも出る）
- 音声が 1 つも無い環境では、読み上げボタンを出さないようにした

## 0.9.0

- **語り手の顔（ペルソナ画像）** を吹き出しと用語ページに出せるようにした。文体と同じ
  場所に置け、画面から差し替えられる
- **ビューアの左パネルを「何を読むか」だけの場所にした。** フォルダと URL はタブで
  排他になり、開いたものに合わせて切り替わる。1 つだけのファイルと貼り付けは
  「＋ その他の開き方」の中へ
- **フォルダ全体から用語を抽出する道を畳んだ。** 何ファイルまとめても AI に届く量は
  1 文書のときと変わらず、待ち時間だけが積み上がっていたため（関係の下書きは残る）
- `samples/` に『坊っちゃん』『吾輩は猫である』の辞書を実物として同梱
- サンプルの zip が、作品を 2 つ以上置いた回に壊れていたのを直した

## 0.8.1

- **AI の下書きに文体（口調）を指定できるようにした。**「講談調で」「TRPG のルール
  ブック風に」と書いておけばその調子になる。フォルダごとにも置ける
- **吹き出しの中の用語をそのまま辿れる**ようにした（吹き出しが閉じない）
- **登録済みの語も AI で書き直せる**ようにした（文体を変えたときのため）
- 説明を README / MANUAL / DEVELOP に分け、画面のスクリーンショットを貼った

## 0.8.0

- **相関図の見せ方を 4 つ足した** —— 交差しない図（BioFabric）、行列（NodeTrix）、
  時系列、1 語を中心にした図（ego network）。**行列は「まだ関係を書いていない組」が
  空きマスで見える**
- 乗せたもの・焦点が当たったものの説明を、図の下の枠に全文で出すようにした
- **辞書の一部（1 カテゴリだけ）を書き出せる**ようにした。渡した先で行き先の無くなる
  関係の本数も先に出る
- **控えの中を見て、1 件だけ書き戻せる**ようにした
- 辞書・用語ページ・相関図・点検を**ビューアに重ねて出す**ようにした（閉じれば
  読んでいた場所へ即座に戻る）
- 辞書を**併合**する取り込みを足した（手元にしか無い用語が消えない）
- 一覧からも選んだ語を登録できるようにした
- 辞書が育ったときの重さを潰した

## 0.6.0

- **長い文書でも用語と関係が拾える**ようにした。それまでは冒頭しか AI に届いておらず、
  長編では候補も関係も出てこなかった
- **初出モードでも関係が引ける**ようにした（渡す本文の配分が偏っていた）
- 一時的な失敗（高負荷・レート制限）で、数分かけた下書きを捨てないようにした
- 設定ダイアログをタブで分けた
- カテゴリの改名・削除でスコープを取り違え、**別の辞書の同名カテゴリを消していた**
  のを直した
- リリース前の予行演習 `check.cmd ci` を足した

## 0.5.0

- **データの保存先を設定から変えられる**ようにした（更新のたびのコピーが要らなくなる）
- **新しい版が出ていたら知らせる**ようにした（1 日 1 回まで。切れる）
- 表示テーマ（OS に合わせる / ライト / ダーク）を選べるようにした
- バージョンアップ時に「辞書が消えた」に見える経路を埋めた

## 0.4.0

- **用語どうしの関係と相関図**を足した
- 関係の AI 下書きと、辞書の点検（🩺）を足した
- 抽出を種別ごとの枠に分けた（登場人物が専門用語に押し出されないように）
- 旧 `related` を `relations` に一本化した

## それ以前

`0.3.0` までは記録を残していない。コミット履歴を参照のこと。
