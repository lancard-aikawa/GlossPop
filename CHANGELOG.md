# 変更履歴

版ごとに**変わったことだけ**を書く。使い方は [MANUAL.md](MANUAL.md)、入手と起動は
[README.md](README.md)。

**この file は Release の本文になる。** タグを打つと、その版の節がそのまま
GitHub Release に載る（[`.github/workflows/release.yml`](.github/workflows/release.yml)）。
節が無いままタグを打つと**ワークフローが止まる**ので、上げる前に書き足すこと
（`.\check.cmd ci` も見ている）。

見出しは `## <版番号>` の形にすること —— 抜き出しがこれを目印にしている。

## 0.10.4

- **exe にアイコンを付けた。** ダブルクリック用の `glosspopw.exe` は画面左上と同じ
  青緑のしるし、コマンドライン用の `glosspop.exe` は端末らしい `>_`。
  **2 つ並んでもどちらを起動するのか一目で分かる**

## 0.10.3

- **AI で下書きするたびに黒い窓（コマンドプロンプト）が開いていたのを直した。**
  その窓を閉じると下書きが失敗し、`[WinError 6] ハンドルが無効です` になっていた
- **ダブルクリック用の実行ファイルを分けた。** `GlossPop\glosspopw.exe`（`w` 付き）が
  窓の出ない起動用で、`glosspop.exe` はコマンドライン用。**起動するのは `w` 付きのほう**
- 0.10.1 で入れた「コンソールから離脱する」細工はやめた。あれが黒い窓の原因だった
  （親にコンソールが無いと、子のコマンドが自分の窓を作るため）

## 0.10.2

- **Release の本文が、その版の変更点になった。** これまでは毎回まったく同じ説明が
  出ていて、「何が変わったのか」がどこにも書かれていなかった。この `CHANGELOG.md` の
  節が Release の先頭に載り、その下に初めての方向けの短い導入が続く

## 0.10.1

- **窓を閉じたらサーバも終わるようにした。** exe をダブルクリックすると付いてくる
  コンソール窓は、アプリの窓が開いた時点で引っ込む。これまでは出しっぱなしで、
  そちらを閉じると**サーバだけ死んでアプリの窓が残り**、以後どこを押しても接続
  エラーになっていた
- 窓を閉じてから実際に止まるまで 20 秒ほどかかる（開いているページからの生存確認が
  途絶えるのを待つため）。ブラウザで開いて使っているとき（`serve`）は止まらない

## 0.10.0

- **文字の大きさを選べるようにした**（⚙ の 表示 → 小 / 中 / 大 / 特大）。本文を基準に
  注記も見出しも同じ比で動くので、比を崩さずに全体を大きくできる。選んだ瞬間に効く。
  **相関図の図の中だけは変わらない**（配置の計算に関わるため。図は拡大縮小で見る）
- **読み上げが使えなくなっていたのを直した。**「この環境には読み上げに使える音声が
  ありません」しか出ない状態だった（音声の一覧が届くのを 1.5 秒しか待っていなかった）
- 読み上げで**オンラインの音声**（🌐 印）も選べるようにした。既定は今までどおり OS の
  ローカル音声で、🌐 を選んだときだけ本文がその提供元へ送られる（画面にも出る）
- 音声が 1 つも無い環境では、読み上げボタンを出さないようにした

## 0.9.0

- **語り手の顔（ペルソナ画像）** を吹き出しと用語ページに出せるようにした。文体と同じ
  場所に置け、画面から差し替えられる
- **ビューアの左パネルを「何を読むか」だけの場所にした。** フォルダと URL はタブで
  排他になり、開いたものに合わせて切り替わる。1 つだけのファイルと貼り付けは
  「＋ その他の開き方」の中へ
- **フォルダ全体から用語を抽出する道を畳んだ。** 何ファイルまとめても AI に届く量は
  1 文書のときと変わらず、待ち時間だけが積み上がっていたため（関係の下書きは残る）
- `samples/` に『坊っちゃん』『吾輩は猫である』の辞書を実物として同梱
- サンプルの zip が、作品を 2 つ以上置いた回に壊れていたのを直した

## 0.8.1

- **AI の下書きに文体（口調）を指定できるようにした。**「講談調で」「TRPG のルール
  ブック風に」と書いておけばその調子になる。フォルダごとにも置ける
- **吹き出しの中の用語をそのまま辿れる**ようにした（吹き出しが閉じない）
- **登録済みの語も AI で書き直せる**ようにした（文体を変えたときのため）
- 説明を README / MANUAL / DEVELOP に分け、画面のスクリーンショットを貼った

## 0.8.0

- **相関図の見せ方を 4 つ足した** —— 交差しない図（BioFabric）、行列（NodeTrix）、
  時系列、1 語を中心にした図（ego network）。**行列は「まだ関係を書いていない組」が
  空きマスで見える**
- 乗せたもの・焦点が当たったものの説明を、図の下の枠に全文で出すようにした
- **辞書の一部（1 カテゴリだけ）を書き出せる**ようにした。渡した先で行き先の無くなる
  関係の本数も先に出る
- **控えの中を見て、1 件だけ書き戻せる**ようにした
- 辞書・用語ページ・相関図・点検を**ビューアに重ねて出す**ようにした（閉じれば
  読んでいた場所へ即座に戻る）
- 辞書を**併合**する取り込みを足した（手元にしか無い用語が消えない）
- 一覧からも選んだ語を登録できるようにした
- 辞書が育ったときの重さを潰した

## 0.6.0

- **長い文書でも用語と関係が拾える**ようにした。それまでは冒頭しか AI に届いておらず、
  長編では候補も関係も出てこなかった
- **初出モードでも関係が引ける**ようにした（渡す本文の配分が偏っていた）
- 一時的な失敗（高負荷・レート制限）で、数分かけた下書きを捨てないようにした
- 設定ダイアログをタブで分けた
- カテゴリの改名・削除でスコープを取り違え、**別の辞書の同名カテゴリを消していた**
  のを直した
- リリース前の予行演習 `check.cmd ci` を足した

## 0.5.0

- **データの保存先を設定から変えられる**ようにした（更新のたびのコピーが要らなくなる）
- **新しい版が出ていたら知らせる**ようにした（1 日 1 回まで。切れる）
- 表示テーマ（OS に合わせる / ライト / ダーク）を選べるようにした
- バージョンアップ時に「辞書が消えた」に見える経路を埋めた

## 0.4.0

- **用語どうしの関係と相関図**を足した
- 関係の AI 下書きと、辞書の点検（🩺）を足した
- 抽出を種別ごとの枠に分けた（登場人物が専門用語に押し出されないように）
- 旧 `related` を `relations` に一本化した

## それ以前

`0.3.0` までは記録を残していない。コミット履歴を参照のこと。
