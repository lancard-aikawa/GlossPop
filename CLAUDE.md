# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

使い方・データ形式・自動リンクの規則は [MANUAL.md](MANUAL.md)、動かし方とビルドは
[DEVELOP.md](DEVELOP.md)、解説動画の作りかたは [docs/video/README.md](docs/video/README.md)
にある（[README.md](README.md) は入口だけ）。ここには
**それらを読んでも分からないこと**（設計の前提、壊しやすい不変条件、開発時の落とし穴）
だけを書く。

## コマンド

```powershell
uv sync --all-groups            # 依存を入れる (dev グループ含む)
uv run glosspop serve           # http://127.0.0.1:8765/
uv run pytest                   # 全テスト
uv run pytest tests/test_linker.py::test_longest_match_wins   # 単体
uv run pytest -k "カテゴリ or category"
```

`glosspop` CLI は辞書操作もできる（`add` / `list` / `show` / `move` / `rm` / `categories`）。
`.claude/skills/gloss-add/SKILL.md` がその使い方をスキルとして持っている。**辞書の
スキーマや CLI の引数を変えたら SKILL.md も直すこと。**

### サーバの止め方（Windows で必ず踏む）

`uv run glosspop serve` の親プロセス（`uv`）を kill しても、**子の python が生き残って
ポートを掴んだまま**になる。新しいサーバは bind に失敗して黙って死に、古いコードが
動き続けるので「直したのに反映されない」という誤診をする。ポートの所有者を落とすこと。

```powershell
Get-NetTCPConnection -LocalPort 8765 -State Listen |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

`uv sync` が `glosspop.exe` を消せずに失敗するときも、たいていサーバが動いている。

## アーキテクチャ

データの流れは一本道:

```
ソース (ファイル / URL / 貼り付け)
  → render.render_source()      Markdown・テキスト・HTML → HTML
  → Linker.annotate()           テキストにだけ <a class="gloss-link"> を差し込む
  → ブラウザ                     popup.js が data-gloss から辞書を引いて吹き出しを出す
```

`store` が辞書、`categories` がカテゴリマスター、`merge` が 2 つのエントリを
1 つにまとめる操作、`ai` が Claude CLI 呼び出し。
`app.py` はこれらを繋ぐだけで、ロジックを持たない。

### `glosspop/core/` は「どのマシンか」を知らない

`models` `htmlclean` `render` `linker` `relations` `documents` `timeline` `doctor`
`entryfile`（エントリのファイル形式）`archivefmt`（zip の形と安全規則）
`imagefmt`（画像の拡張子と見分け方と大きさ）`whenfmt`（作中の時刻の読み方）
`booklet`（読ませる 1 枚の組み立て）`headline`（辞書から見出しを機械で作る）`card`（渡す 1 枚に載せる中身）の 15 個は
`glosspop/core/` にある。**入力から出力を作るだけ**で、辞書の置き場所 (`store`)・
開いているフォルダ・設定ファイル (`config`) を一切知らない。

**この層だけが GlossPopApp（携帯・共有版）と共有される**（→ docs/design-notes.md）。
共有するものが 2 か所に分かれると、片方で直してもう片方で直し忘れて**同じ辞書なのに
違う語がリンクになる** —— 画面を見ても分からない壊れ方をする。

**`core` から `config` と `store` を import しないこと。**
`tests/test_core_boundary.py` が見張っている（相対 import・絶対 import・
import 時の副作用の 3 方向）。**人が気を付ける形にすると必ず越える** ——
「ついでに開いているフォルダを見たい」は毎回出てくるので、そのたびに**呼ぶ側から
引数で渡す**こと（`timeline.py` が `Linker` と `Document` を引数で受けているのが
その形）。逆向き（`core` の外から `core` を使う）は自由。

**物理的に別パッケージへ分けていない**のは、リリースの結合を増やす理由がまだ
無いから（GlossPopApp は `uv` の path 依存でこのフォルダを直接見ている）。

**「形」を見つけたら core へ寄せる。** `entryfile` と `archivefmt` は最初
`store.py` / `archive.py` の中にあった。**照合が 2 か所にあると違う語がリンクになり、
書式が 2 か所にあると zip が「読めるのに一部だけ落ちる」** —— 後者のほうが例外に
ならないぶん気付きにくい。フロントも同じで、相関図の**形の規則**
（`static/graph-model.js`）は GlossPopApp が `/shared/` からそのまま読んでいる
（**写しを作らないこと**）。

### GlossPopApp（携帯・共有版）

`../GlossPopApp` にある別リポジトリ。**こちらを直すと即座に向こうへ効く**ので、
`core` に触るときは向こうのテストも通すこと:

```powershell
cd ..\GlossPopApp; uv run pytest
```

向こうの決めごとは [そちらの CLAUDE.md](../GlossPopApp/CLAUDE.md)、
なぜ別プロジェクトにしたかは [docs/mobile-rethink.md](docs/mobile-rethink.md)。

### 壊しやすい不変条件

**ディレクトリ名とファイル名が正。** エントリは `<辞書ルート>/<カテゴリ>/<slug>.md`。
`category` `slug` `scope` は frontmatter に書かない（書いても `_entry_from_file()` が
パスの値で上書きする）。一意キーは `Entry.ref` で、API も URL もこの文字列を使う。
**同じ用語名でもカテゴリが違えば別エントリ**なので、`find_by_surface()` はリストを返す。
単数を期待するコードを書かないこと。

**「関連語」は `relations` に一本化した。** 旧 `related`（向きも一言も無い名前の
並び）は `EntryBase` の before バリデータが `relations` に畳む。ファイル・API・CLI の
どこから来ても通るよう、**入り口 1 か所**でやっている。`_FM_KEYS` から外してあるので、
古いファイルは次に保存した時点で移行される（`migrate_layout()` と同じ考え方）。
2 か所に書けるまま残すと、どちらに書くか毎回迷ううえ、`related` 側は相関図に出ない。

**関係 (`relations`) に ID は無い。参照は名前で書く。** 揺れを吸収するのは
`relations.resolve()` **1 か所だけ**なので、ここを迂回して自前で照合を書かないこと。
吸収しているのは 3 つ:

- **正規化** — `normalize_link()` が NFC・空白・`/` の周りを潰す。「空白を足しただけで
  切れる」を防ぐのがここ
- **転送** — `move()` と `save()` がカテゴリ移動のとき旧 ref を `former_refs` に積む。
  **参照側は書き換えない**（wiki のリダイレクト）。全エントリを書き換えて回る実装に
  変えないこと —— 手で `mv` された経路が必ず漏れる
- **曖昧さ** — 同じ用語名がカテゴリ違いで併存できるので、書き手のカテゴリ →
  同じ辞書 → 全体、の順に絞る。**絞りきれないときは黙ってどれかに寄せない**
  （寄せた瞬間に相関図の辺と本文のリンク先が食い違う）

`former_refs` は**ファイルの値が正**。`save()` が下書きの値ではなく既存ファイルから
読み直しているのは、項目を省いた更新で転送情報が消えないため。

**向きの基準は「自分から `to` を見て」に固定してある。** `label` `back` `rank` の
全部が同じ向き。「相互 / 一方的」のフラグを別に持たないのは、`back` の有無が
それを決めるから。フラグを足すと二重に持つことになり、必ずずれる。

**関係の時間は 2 つあり、別物。** `reveal` は**読者がいつ知るか**（ネタバレ抑止に
使う。書くと図で伏せる）、`when` は**作中でいつ起きたか**（並べるだけで何も伏せない）。
時系列の見せ方はどちらの軸でも並べられるが、**どちらで並べているかを必ず画面に出す**
（一致しないから 2 つある）。守ること 3 つ:

- **並べ替えに使うのは先頭の西暦だけ**（`core.whenfmt`。読むのはここ 1 か所で、
  図と点検が別々に読むと「点検は通るのに並ばない」が起きる）。**元号を解釈しない**
  ——「天保三年」と「享保五年」の前後は変換表が要り、作品ごとの暦は端から載らない。
  **読めなければ `None`**（0 に寄せると、いちばん古い関係として並ぶ）
- **線は「変換表が要るか」。** 世紀・年代・算用数字の年月日は**算術**なので読む
  （`16世紀` → 1501、`1560年代` → 1560、`1560年5月19日`）。表が要るもの（元号・
  作中の暦）だけを拒む。**粗いものはその範囲の頭に置く**（`1560` が `1560-01-01`
  なのと同じ規則）—— 真ん中に置くと、書いていない値で並べたことになる
- **幅は持たない。** `1560〜1570` は読まない —— 範囲どうしの前後が決まらないので、
  持った瞬間に並べ替えが定義できなくなる。幅はうしろに文字で書ける
- **AI が書いた時刻は信じてよいところまで削る**（`ai.filter_when()`）。**実測から
  来ている決まり** —— AI は**和暦の月日をそのまま西暦の枠に写す**（「六月二日」→
  `06-02`。正しくは 1582-06-21 で**3 週間ずれる**）。`samples/戦国時代` で 3 件中
  3 件が同じ壊れ方をし、**プロンプトで禁じても 3 件中 1 件しか直らなかった**。
  しかも形は正しいので「西暦として読めるか」の検査を通ってしまう ——
  **ネタバレ抑止と同じで、プロンプトの禁止文だけに頼らない**。漢数字の月が続いて
  いて、かつ改暦（`GREGORIAN_FROM` = 1873）より前なら**年だけに丸める**
  （`whenfmt.year_only()`）。**落とすのは月日だけで年は残す**（年は本文に併記されて
  いることが多い）。**正しく換算されていたぶんも丸まる**が、間違った日付は書かれて
  いないより悪い（読みと同じ判断）。**人が書いたものはここを通らない** ——
  機械の申告にだけ枷をかける
- **「だいたい」の印は位置を変えない**（`is_about()`。`約1560` も `1560ごろ` も
  `1560` と同じところ）。ずらす幅を決める根拠が無いし、ずらせば書いていない値で
  並べたことになる。**印は画面に出す**（時系列の白抜きの目盛り）—— 位置が範囲の頭
  なので、実線の点にすると `16世紀` が 1501 年ちょうどに見える
- **表示は書かれたまま。** 先頭のうしろに元号でも作中の暦でも書ける
  （`timeline.py` が `reveal` を上書きしないのと同じ約束）
- **書かれていないのが普通。** 点検は黙り、時系列は最後の帯にまとめて**数を出す**
  —— そのとき「書いていない」と「書いたのに読めない」を**数え分ける**
  （まとめると、書き間違いが未入力に紛れて見えなくなる）

**時刻は語にも書ける。関係の時刻とは役割が違う。** `EntryBase.when` は**その語自体が
いつか**（事件・出来事）、`Relation.when` は**その関係がいつ成立するか**（時期で
変わる間柄）。事件は**それ自体に日付がある**ので、語に書けないと同じ日付を関係の
本数だけ書き写すことになり、**1 本直し忘れると時系列の帯が黙って割れる**（実際に
`samples/戦国時代` がそうなっていた。6 本に同じ日付が並んでいた）。守ること 4 つ:

- **読む口は `relations.relation_when()` 1 か所。** 相関図・辞書ページ・指されている
  側の 3 つが同じ値を出す —— 別々に決めると「図では並ぶのに一覧では時刻が無い」に
  なる。**関係に書いてあればそれが正**（語の時刻とずれうるから 2 つある）
- **無ければ両端の語の遅いほうを継ぐ。** 読む順の「両端が出そろうところ」
  （`timeline.annotate()` の `max(a, b)`）と同じ考え方。**片方にしか書かれて
  いなくても継ぐ** —— ここだけ読む順と違う。あちらは両端とも必ず位置を持つが、
  こちらの空欄は「書いていない」で、両端そろって初めて継ぐ形にすると**事件 → 人物**が
  全部時刻を失う（人物に生没を書く辞書のほうが珍しい）。**継ぐのは西暦として
  読めるものだけ** —— 読めない文字列を配ると、語 1 つの書き間違いが関係の本数だけ
  「書いたのに読めない」に化けて数を狂わせる
- **継いだことを画面に出す**（`when_inherited` → 破線の枠と「（語の時刻）」）。
  書いた時刻と同じ見た目で並べると、消そうとして**関係のほうを探し回る**
- **継いだ値を入力欄に入れない**（`graph.js` の辺のダイアログ。placeholder で見せる）。
  入れると、一言だけ直したつもりで保存した瞬間に**語の時刻が関係へ焼き付く** ——
  書き写していた元の形に戻るうえ、次に語の時刻を直しても付いてこない
- **「前後」の項目を作らない。** 順序は時刻から出る（`timeline.py` の「位置は計算する。
  保存しない」と同じ）。足すと `when` と二重になり、食い違ったときにどちらが正か
  決まらない。**因果は `label` に書く** ——「この報を受けて起きる」は前後だけの印より
  多くを伝える
- **期間 (生没) の項目も持たない。** 人物に時刻を持たせると「その日付は生まれか、
  没か、その事件のときの年齢か」が決まらない。**人物の位置は繋がっている事件から
  決まる**（継ぐ規則がそれをやる）ので、持たせる必要がそもそも無い。範囲を書きたい
  ものは先頭を 1 点にして、うしろに文字で書く（`1467 応仁の乱（〜1477）`）

**時系列には並べるものが 2 つある**（`timeline.js` の `rows`。関係 = 1 行 1 本 /
語 = 1 行 1 語 ＝ 年表）。**軸とは別の選択**で、掛け合わせて 4 通り。置き換えでは
ない —— 関係の並びは「誰と誰がいつ繋がるか」、語の並びは「何がいつ起きたか」で、
答える問いが違う（同じ事件は関係の本数だけ行に現れる）。
**既定は語（年表）** —— 同じ事件が関係の本数だけ行に現れないぶん見通しがよく、
「時系列」でいちばん先に問われるのが「何がいつ起きたか」だから。守ること 5 つ:

- **行になるのは位置のある語だけ。** 軸に載らない相手は行のうしろの**従のチップ**で、
  **消してはいない**（数を凡例に出す）。カテゴリで「これは人物」と決めつけない ——
  **人物は期間なので時刻を書かない**ぶん、自然に従へ落ちる
- **`outside` を行にしない。** カテゴリで絞ったときに辺の相手として足された語なので、
  行にすると絞った意味が消える（`build_graph(only=…)` が相手を足さないのと同じ話）
- **軸が使えるかの判定は並べるもので変わる**（`graph.js` の `hasWhen()`）。語のときに
  辺だけを見ると、**関係の書かれていない事件**（日付はある）で軸を落としてしまう
- **同じ時刻の中の順も画面に書く**（年表は五十音順。関係の並びの「主を先に」と
  同じ約束）。書かないと、**同着**が「並べ替えが効いていない」と読まれる ——
  `samples/戦国時代` の「本能寺の変」と「伊賀越え」は**どちらも天正十年六月二日**で、
  サンプルの本文がそれを実演として書いている（**日付を動かして直さないこと**）。
  前後を出したいのは**時刻を細かく書く**側の仕事で、**「〇〇の後」の項目は作らない**
  （`when` と二重になる。因果は `label` に書く ——
  実際に「この報を受けて始まる」と書いてある）
- **弧の外形を埋めない。**「コ」の字なので外形の真ん中は線の上ではないが、埋めると
  **またいだ行の従を覆って**そちらが押せなくなる（段の図の `hitBand()` とは逆の判断。
  押す先は線か一言で、どちらから触っても同じ）

**時系列の関係の並びでは、同じ時刻の中で「主」を先に出す**（`graph-model.js` の
`isMainRelation()`。両端が同じカテゴリなら主）。事件から伸びる関係には**話の筋**
（事件 → 事件）と**顔ぶれ**（事件 → 人物・場所）が混ざっていて、同じ重さで並べると
顔ぶれの多い事件ほど筋が下に埋もれる（本能寺の変は 6 本が顔ぶれ、2 本が筋）。
守ること 3 つ:

- **落とさない。** 従も同じ帯に出て、後ろに回るだけ（`hidden` / `outside` と同じ約束）
- **カテゴリ名の意味を読まない。** 見るのは*同じかどうか*だけ ——「人」「事件」と
  いった名前を当てにすると、辞書ごとに違う名前で静かに効かなくなる。鍵は
  `<scope><>カテゴリ`（**名前だけで束ねない**。同名が両方の辞書にありうる）
- **決めた並びは画面に書く**（凡例）。書かないと、同じ時刻の中の順が入力順だと
  思われる

**関係の項目を足したら、組み立て直している 2 か所も直す。** `merge.py` の
`_default_relations()`（いまはモデルの項目から引くようにした）と `merge.js` の
`apply()`（並べ書き）。**忘れると、まとめた瞬間だけその項目が静かに消える**
（`when` を足したときに実際にそうなっていた）。

**重複の判定は ref で、プロンプトへの表示は用語名で。** `ai.existing_pairs()` が
用語名、`ai.existing_ref_pairs()` が ref を返すのはこのため。片方で兼ねようとして
用語名で照合したら、**「すでにある関係」を素通しして同じ組に 2 本目の辺が生えた**。
同名がカテゴリ違いで併存できる以上、名前は鍵にならない。

**関係の書き込みはエントリ単位でまとめる**（`/api/relations`）。1 本ずつ PUT すると、
同じエントリに複数の関係が付いたとき後の書き込みが前のものを消す。クライアントから
順に投げる形に戻さないこと。相関図から 1 本だけ直すときも同じで、そのエントリの
`relations` を作り直して丸ごと書く。

**辺から書き手の関係へたどる鍵は `index` と `rel_to` の 2 つ**（`build_graph`）。
`to` は解決後の ref なので、ファイルに書かれた文字列とは違いうる（用語名だけで
書ける仕様の裏返し）。`index` は **`enumerate(entry.relations)` から採る** ——
出した辺だけを数えると、判明位置つきを伏せたぶんずれて**別の関係を書き換える**。
書く直前に読み直して `rel_to` が一致するか確かめること（合わなければ書かない）。

**点検 (`doctor.py`) は正常なものを問題として出さない。** カテゴリ違いの同名は
この辞書の狙いどおりの機能だし、関係が 1 本も無いエントリも普通にある。ここが緩むと
警告が常時数十件出て、誰も見なくなる（＝本物の壊れが埋もれる）。挙げるのは
「直さないと読み手に見えないもの」「黙って壊れているもの」だけ。

**地図はその「黙って壊れている」側の代表。** 座標を書いても `map:` が無ければ
どこにも出ず、絵を消しても用語は書き換えないので静かに消える。**画面には何も
出ない**ので、点検で言わないと気付く手段が無い。守ること 3 つ:

- **置いてある絵の一覧は呼ぶ側が渡す**（`check(entries, maps={"<scope>/<名前>": 縦横比})`）。
  `core` は辞書の置き場所を知らない（`timeline` が `Linker` と `Document` を受けて
  いるのと同じ形）。**渡されなければ絵の点検はしない** —— 「1 枚も無い」と「一覧を
  もらっていない」を混同すると、地図を使っている辞書で全部の語に警告が出る
- **絵の名前だけ書いて座標が無いのは正常**（「この絵に置きたい」の置き待ち。
  図が `pending` として数えて出す）。挙げるのは逆向き（座標だけ）のほう
- **y の上限は絵の縦横比。定数は置かず、呼ぶ側が絵から読んで渡す。** 座標は
  **絵の幅**を 1 とした比なので、**縦長の絵では y が 1 を超えるのが正常** ——
  上限は縦横比でしか決まらず、それは `core` から読めない。**比が渡されなければ
  上限は見ない**（`maps` を渡されなければ絵の点検をしない、と同じ約束）。
  読むのは `core.imagefmt.size()` で、**受け入れる形式は全部読めること** ——
  片方だけ読めないと、その形式の絵を使っている辞書でだけ点検が静かに緩む。
  **読めなかった絵を問題として挙げない**（直しようがないものを挙げると本物が埋もれる）。
  一覧からも落とさない —— 落とすと「その名前の絵がありません」が誤って出る

**統合 (`merge.py`) は似ているものを機械で挙げない。** 上と同じ理由で、
「同じ人物かもしれません」を出すと正常なカテゴリ違いの同名を大量に警告する。
まとめるのは人が 2 つ選んだときだけで、先に出す候補は**同じ表記のもの**だけ。
中で守っているのは 3 つ:

- **消える側の用語名を残す側の別名に回す。** 落とすと、本文でその表記が
  リンクにならず、名前で書かれた他エントリの関係も行き先を失う
  （まとめたのに片方の呼び方だけ引けない、では意味がない）
- **参照側のファイルは書き換えない。** 消える側の ref は `former_refs` に積んで
  転送する（カテゴリ移動と同じ）。全エントリを書き換えて回る形にしないこと
- **畳めないものを黙って寄せない。** 本文・要約・同じ相手への関係は下見
  (`GET /api/merge`) で並べ、決まった値だけを実行 (`POST /api/merge`) に渡す。
  同じ相手かどうかの判定は**解決後の ref** で行う —— 用語名で照合すると、
  片方が名前・片方が ref で書かれているときに 2 本目の辺が生える

**相関図では `reveal` つきの関係を既定で出さない**（`build_graph(spoilers=False)`）。
相関図は本文より先を一望させるので、ネタバレ抑止は本文生成 (`ai.py`) だけでは
足りない。いつ判明するかを機械で比べる手段が無いので、**判明位置が書かれているか**で
切る —— これが嘘をつかない唯一の線引き。伏せた本数は `hidden` で必ず返すこと
（黙って欠けた図を出さない）。

**相関図の範囲は 2 つあり、どちらを出しているかを必ず画面に書く。** 既定は辞書全体、
`/api/graph?doc=<content の中のパス>` でその文書に出てくる語だけ。**書かないと、
全体の図を「開いている文書の図」だと思われる** —— この取り違えが元で、フォルダ全体を
読む「関係を下書き」を相関図から外した（読むものが決まっているのはビューアだけなので、
下書きの入り口は本文の側にある）。3 つ守ること:

- **出てくるかどうかは `Linker.entries_in()` に決めさせる。** 素の部分一致に戻すと
  `API` が `rapid` に当たり、**本文でリンクにならない語を「出てくる」と言う**ことになる
  （`?ref=` の出現探しと同じ理由）。読むのは 1 文書だけで、フォルダ全体には広げない
- **`build_graph(only=…)` は相手を足さない。** カテゴリ絞りは辺が宙に浮かないよう
  相手を `outside` ノードとして足すが、こちらで足すと**その文書に出てこない語が
  混ざり**、絞った意味が無くなる。落とした辺は `outside` で数を返す（`hidden` と同じ
  約束。未登録の相手も同じで、**赤リンクを生やさない** —— 辞書に無い語は文書に
  出てくる語ではありえない）
- **貼り付け・URL では出さない。** `?doc=` はサーバが content の中を読み直す仕組みなので、
  読み直せない経路では絞れない。リンクを出しておいて絞れないより、出さないほうがまし

**辞書ルートは 2 つある。** グローバル (`data/glossary/`) と、開いているフォルダの
ローカル (`<フォルダ>/.glosspop/glossary/`)。`store.glossary_dir(scope)` を必ず通すこと。
ref はグローバルが `<カテゴリ>/<slug>`、ローカルが `.local/<カテゴリ>/<slug>`
（カテゴリ名は「.」で始められないので衝突しない）。**グローバル側の形を変えていない**ので、
既存の URL・CLI・スキルはそのまま通る。

ローカル辞書まわりで踏みやすいのは 3 つ:

- `_signature()` に**ローカルのルートパス自体**を入れてある。入れないと、フォルダを
  切り替えてもキャッシュが効いて前のフォルダの辞書が出てくる
- `find_in_category()` はスコープも見る。見ないと「全体の辞書に同名があるので
  このフォルダに登録できない」という誤った衝突になる
- **カテゴリマスターは辞書ごとにある**（`categories.py` の全関数が `scope` を取る）。
  グローバルが `data/categories.yaml`、ローカルが `<フォルダ>/.glosspop/categories.yaml`。
  小説の「登場人物」が**全体の**マスターに残ると邪魔、という理由は変わっていない ——
  載る先がそのフォルダの中になっただけ。`categories._signature()` に**ファイルのパス
  自体**を入れてあるのは `store._signature()` と同じ理由。**辞書の無いフォルダには
  マスターを作らない**（開いただけのフォルダを汚さない）

**カテゴリの改名・削除は必ずスコープを取る**（`rename_category` / `delete_category`）。
グローバル決め打ちだったため、**用語の入ったローカルのカテゴリが「空」と判定され、
同名のグローバル側が代わりに消えた**（空かどうかを `not e.is_local` で数えていた）。
数えるのは `e.scope == scope` の中だけ。**API・CLI・UI の 3 つとも `scope` を
渡すこと** —— 渡さない経路が 1 つでも残ると同じ事故が起きる。

**改名はマスターを先に直してからディレクトリを動かす。** 逆にすると、
`categories.load()` が動かした先のディレクトリを新カテゴリとして自動で取り込み、
続く改名が「既にあります」で落ちる。以前は逆順で、**マスターのキャッシュが
温まっているときだけ通っていた**（冷えた状態で踏むと改名できない）。

**画面でも 2 つの辞書を見分けられるようにする。** カテゴリ管理と絞り込みは
`📁` を付け、絞り込みの値は `<scope>/<カテゴリ>` にしてある（相関図と同じ形。
`/` はカテゴリ名で禁止なので 1 回だけ割れる）。名前だけを値にすると、同名が
両方にあるとき選択肢が見分けられない。**一覧のグルーピングも同じで、鍵は
`<scope><>カテゴリ`** —— 名前だけで束ねると 2 つの辞書の用語が 1 つの見出しに
混ざり、マスターの順に並べるときに同じ見出しが 2 回出る。

**一覧の束ね方は 3 つ**（カテゴリ順 / 五十音順 / 作中の時刻順。`glossary.js` の
`groupBy`）。**置き換えではない** —— カテゴリは「どういう語か」で引く道、五十音は
辞書を通して引く道、時刻は起きた順に見る道で、どれも要る（相関図の見せ方と同じ判断）。
守ること 3 つ:

- **かなで置けない語を黙って「あ」行に混ぜない**（`ROW_NONE`）。漢字の見出しで
  読みが無ければどの行にも置けない —— 別の束にして数を出し、**どうすればそこへ
  並ぶかだけ書く**（責めない）。**読みが無くても見出しがかななら置ける**
  （`rowOf()` が読み → 見出しの順に見る）
- **カタカナはひらがなに畳む。** 畳まないと「ジョバンニ」が読みなしに落ちる
- **五十音のときはカードにカテゴリを出す**（`showPath`）。見出しがカテゴリで
  なくなるので、書かないと「ソース（料理）」と「ソース（開発）」が見分けられない
  （**年表も同じ**）

**年表（作中の時刻順）が並べるのは「語」で、時系列の図が並べるのは「関係」。**
別のものなので、片方があるからもう片方は要らない、にしないこと。守ること 4 つ:

- **並べ替えの数はサーバから受け取る**（`_term_card()` の `when_at`）。一覧が
  先頭の西暦を自前で読むと**読む口が 2 つ**になり、「図では並ぶのに一覧では
  並ばない」が起きる（読むのは `core.whenfmt` 1 か所という約束）
- **束ねるのは「並べ替えの値も見出しも同じ」ものだけ**（時系列の `bandsOf` と
  同じ規則）。人は同じ日を 2 通りに書けるので、寄せると**書いていない文字列が
  見出しに出る**
- **「書いていない」(`TIME_NONE`) と「書いたのに読めない」(`TIME_BAD`) を分ける。**
  まとめると、書き間違いが未入力に紛れて見えなくなる
- **飛ぶ帯 (`jumpBar`) は出さない。** 五十音は行が 13 個と決まっているが、時刻は
  いくつでも増えるので、同じ帯を作ると本体より背が高くなる

**読みを埋める道は 2 つあるが、入力欄は 1 つ**（`glossary.js` の `readingForm`）。
手で書いても AI が埋めても**同じ欄**に入り、**同じ 1 回の保存**（`POST /api/readings`）
で書き込む。別々の口にすると、AI が埋めたぶんだけ先に保存されて「手で直したのに
戻った」が起きる。守ること 4 つ:

- **読みだけを差し替える**（専用の口。`/api/map-shape` と同じ理由 —— 一覧は
  エントリの一部しか持っていないので、組み立てて PUT すると本文も関係も落ちる）
- **AI が埋めた欄で、手で書いたぶんを上書きしない**（先に書いた人の指定が勝つ）
- **かなでない読みは落として理由を返す**（`ai.filter_readings()`）。読みは
  **確かめようがある**ので申告を丸呑みしない。**分からないものは空で返させる**
  —— 間違った読みは書かれていないより悪い（辞書の並びが狂う）
- **同じ用語名が 2 つあるものは AI に頼まない。** AI は用語名で答えるので、
  戻すときにどちらの語か決まらない（`relations.resolve()` が絞りきれないときに
  寄せないのと同じ）。手で書く道は残っているので、そう言って返す

**カテゴリの並べ替えは全体を送る**（`PUT /api/category-order`）。「これを 1 つ上へ」
という差分にすると、続けて押したとき後の書き込みが前のものを消す（関係の書き込みと
同じ話）。`categories.reorder()` は**送られなかったカテゴリを後ろに残す** ——
落とすと、一覧を作ってから送るまでの間に増えたものが黙って消える。
パスが `/api/categories/order` でないのは、`order` という名前のカテゴリを作れて
しまうと `/api/categories/{name}` と食い合うため。

**CLI の `--folder` は `config.set_content_dir()` を呼ぶだけ。** 一度きりの CLI には
「開いているフォルダ」の状態が無いので、そこだけ先に決めてから既存の経路
（`store.glossary_dir(LOCAL_SCOPE)`）に流す。**CLI 用のローカル辞書探索を別に
書かないこと** —— 祖先方向の探索・段数の上限・URL 辞書との排他が 2 か所に分かれる。
辞書を読む前に反映する必要があるので `main()` の `ensure_ready()` より前で呼んでいる
（あとから差し替えると `store` のキャッシュが前のフォルダの辞書を掴んだままになる）。
書いた場所は `_announce_local()` が stderr に出す —— 祖先の辞書が使われることがあり、
`--folder` を省けば既定の content フォルダになるので、**黙って書かない**。

**Linker はレンダリング済み HTML を文字列として走査する。** DOM は作らない。
`_tokenize()` でタグとテキストに切り、`_runs()` で「読者に見えるテキスト」の
まとまりを作ってから照合する。ここが 2 つの規則を担っている:

- `SKIP_TAGS`（`pre` `a` `script` `style` `textarea`）の内側はリンクしない
- `INLINE_TAGS` の中だけテキストを連結する。ブロック要素と `<br>` は連結しない
  → `**冪**等` は繋がるが `<p>冪</p><p>等</p>` は繋がらない

**照合の正規表現は表記を前方一致で木にまとめてある**（`_trie`）。`a|ab|abc` と
並べると候補ごとに順に試すので、**本文の長さ × 候補数**で伸びる（実測: 3000 語で
照合だけ 112 ms。木にすると 0.6 ms）。ここを素直な並びに戻さないこと。
守っている規則が 3 つあり、どれも外すと**速いのに結果が違う**という質の悪い
壊れ方をする:

- **続きのある枝を先に、ここで終わる枝を後に置く**（`_branch`）。この順序が
  そのまま「同じ位置では長い表記が勝つ」になる。逆にすると `銀河` で切れて
  `鉄道の夜` が地の文に残る
- **枝のキーは小文字に畳む**（`_fold`）。全体が IGNORECASE なので、`A` と `a` を
  別の枝にすると同じ位置で両方成立し、どちらが選ばれるかが並び順まかせになる
- **境界チェックは位置が違う。** 先頭側 (`_LOOKBEHIND`) は最初の 1 文字で決まる
  ので枝の根に、末尾側 (`_LOOKAHEAD`) は表記ごとに違うので**終端に**置く

`(?-i:…)` で囲む大文字小文字を区別する表記だけは木に混ぜられないので並べたまま
（3 文字以下の全大文字 ASCII だけなので数は少ない）。**木より後ろに置くこと** ——
木のほうが長い表記を持ちうるので、先に試させる。

保証しているのは `test_linker.py` の**素直に並べた版との突き合わせ**（`_flat`）
だけなので、消さないこと。

**組み立てた `Linker` は使い回す**（`app._linker()`）。鍵は `load_all()` が返す
リストそのもので、`store` は署名が変わったときだけ新しいリストを作る。
**時間や件数で判定する形に変えないこと** —— 外のエディタで書き換えたぶんを
黙って無視するようになる（「エディタで直接編集してよい」が売りなので、
そこが崩れると気付けない）。

**`store._signature()` は `os.scandir` で作る。** ディレクトリを読んだ時点で OS は
各項目の情報を返しているので、`scandir` の `stat()` は追加のシステムコールを
出さない。`Path.glob` + `path.stat()` に戻すと件数に比例して効く（3000 語で
33 ms → 6 ms）。**ここは全リクエストで通る。**

**大文字小文字を無視するのは既定であって全部ではない。** `_case_sensitive()` が真に
なる表記（3 文字以下の全大文字 ASCII）は `(?-i:…)` で囲んで区別する。これが無いと
別名 `MD` が `README.md` の拡張子に当たった —— 境界チェックは英数字以外を通すので
`.` が境界になる。**ここを「全部無視」に戻さないこと**（`ML` `DB` を登録するたびに
同じ壊れ方をする）。上限 `_ACRONYM_MAX_LEN` を外すと `NASA` のような全大文字の
固有名詞まで区別する側に倒れるので、広げるなら実例が出てからにする。

**複合語に当たるのは「当てない表記」(`excludes`) で止める。規則にしないこと。**
日本語に語境界が無いので、動詞から来た用語（`読み` `控え` `間引き`）は
**複合語と活用の中にも当たる** ——「読み込む」の中の「読み」にリンクが付く。
用語名を選び直しても解けない（`読み` はその辞書の正しい用語名で、たまたま
生産的な語幹でもある、というだけ）。守ること 4 つ:

- **一律の規則に逃げない。**「送り仮名で終わる語の直後が漢字なら当てない」を
  実測したら、`samples/坊っちゃん` の **うらなり先生 (7 件)** と **野だ公 (3 件)** という
  **正しいリンクを 11 本壊した**。続くかどうかは語の性質で、書き方からは導けない
- **実体は木に長い枝を混ぜるだけ**（`Linker.__init__` の 2 周目）。「同じ位置では
  長い表記が勝つ」に相乗りしているので、**照合規則は増えていない**。群にエントリを
  付けなければ `annotate()` も `_hits()` も既存の「エントリの無い群は飛ばす」で素通しする
- **表記の登録を全部終えてから混ぜる。** 先に混ぜると、同じ文字列を用語名や別名に持つ
  エントリが**黙ってリンクにならない**（用語が除外に負ける）
- **`finditer()` でも落とす。** ここは `app._entry_finder`（`?ref=` の出現探し）が
  直に使う口なので、通すと**リンクにならない位置を「出てくる」と言う**ことになる。
  群を引くのは `_group_for()` 1 か所

**語幹まで書けば足りる**（`読み直` で 直す / 直し / 直さ が止まる）。境界チェックは
英数字以外を通すので、CJK の枝に末尾チェックは付かない。

**`htmlclean.VOID_TAGS` に void 要素を漏れなく入れること。** `DROP_TREES`（中身ごと
捨てるタグ）に void 要素が入っていると、閉じタグを待ち続けて**それ以降の本文が全部
消える**。`<input>` がそうで、**検索ボックスのあるページ（＝たいていの Web ページ）が
空になっていた**。URL 読み込みを触るときは、実際のサイトを 1 つ開いて本文が出ることを
確かめること（`example.com` のような単純なページでは気付けない）。

**`MAIN_CANDIDATES`（`main` / `article`）で本文を採るのは最初のひとつだけ。** 見つける
たびに `parts` を差し替える作りにすると、**2 つめの `<article>` が 1 つめを丸ごと
消す**。記事を `<article>` で並べるページ（ブログの一覧、章が並ぶページ）が該当する。
`_main_done` がその番人。これも「黙って本文が消える」系統なので、上の `<input>` と
同じく実際のページで確かめること。

`render.md_to_html()` は `html: False` で生 HTML を通さない。これは安全のためだけで
なく、**Linker のタグ走査が想定外の構造を踏まないため**でもある。外部 HTML を
入れる経路（URL 読み込み）は `htmlclean` の許可制サニタイザを必ず通す。

**吹き出しの中の用語は、その吹き出しの中で辿る**（`popup.js` の `drill()`）。
吹き出しは 1 枚しかないので、中の語で `show()` を呼ぶと `paint()` が innerHTML を
差し替え、**いまポインタが指している要素ごと消える** —— 指す先を失った離脱で
元の吹き出しまで閉じる（「両方消える」の正体）。守ること 3 つ:

- **ホバーでは開かない**（`inPop()` で弾く。`pointerover` / `pointerout` / `focusin`
  の 3 か所とも）。押したときだけ辿る
- **位置の基準は元のアンカーのまま。** 辿った先の語は吹き出しの中にあるので、
  そこを基準にすると吹き出しが自分を追いかけて飛んでいく
- **戻る道は `paint()` に 1 か所だけ置く。** 描き方は 1 件 / 複数 / 読み込み中 /
  エラーの 4 通りあるので、それぞれに書くと必ずどれかで抜ける（辿った先でだけ
  戻れない、は画面を見ても分からない）

**登録済みの語は編集から書き直せる**（`ai.build_prompt(current=…)`）。
**「本文があるならボタンを隠す」に戻さないこと** —— 文体（口調）を変えても
書き直す手段が無くなる（実際に困った）。書き直しても**保存するまで確定しない**ので、
隠して守るほどのものではない。守ること 3 つ:

- **いま書かれている説明を渡す。** 編集中は選択テキストも文書も手元に無いので、
  渡さないと AI は用語名だけを頼りに一般論を書き、**人が手で直した内容が丸ごと消える**。
  プロンプトでも「事実は変えずに文体だけ整え直す」と明示する
- **差し替えるのは要約と本文だけ。** カテゴリは変えるとファイルごと移動するし、
  **使用例は本文からそのまま採った引用**なので、書き直すと引用でなくなる
- **まとめて書き直す口は作らない**（→ design-notes）。手で直した本文まで消える

**辞書本文は保存時に 1 文 1 行へ整形される**（`render.soften_paragraphs()`）。
表示側は `breaks: True` の markdown インスタンスを使う。ソース文書用の `_md` とは
別インスタンスなので、片方だけ設定を変えないこと。

**開いているフォルダは `config.content_dir()` で引く。** ビューアから
`/api/content-root` で切り替えられるようになったので、`config.CONTENT_DIR` は
「既定値」でしかない。直接参照すると、切り替えたあとに一覧・読み出し・パス検査が
別のフォルダを見て、`_safe_content_path()` のディレクトリ脱出チェックが意味を失う。
上書きはプロセス内に残るので、**テストは conftest の fixture でリセットしている**。

任意のフォルダを開ける = 巨大なツリーを掴みうるということでもある。`_iter_content_files()`
が隠しディレクトリと `SKIP_DIRS` に降りず、`MAX_CONTENT_FILES` で打ち切るのはそのため。
打ち切ったことは `truncated` で UI に出す（黙って切らない）。

**凍結した exe の標準出力は UTF-8 ではない。** 日本語 Windows のコンソールでは
CP932 になる（開発時の `uv run` では UTF-8 なので、**この差は exe でしか表面化しない**）。
子プロセスとやりとりする値は必ずバイト列で受け渡すこと — `picker.py` は
`sys.stdout.buffer` へ UTF-8 で書き、親は `text=True` を使わずに自分で復号している。
これを怠って「日本語を含むフォルダを開けない」不具合を出した。CLI もパイプ越しのときだけ
UTF-8 に固定している（コンソールへ直接出すときに UTF-8 で書くと今度は表示が化ける）。

**固定するのは stdin も。** `_use_utf8_when_piped()` が stdout / stderr だけを直していた
ため、`echo '{"term":"冪等"}' | glosspop add --json -` の入力が CP932 で復号され、
**見出し語がサロゲートに化けたまま保存されていた**（`/gloss-add` スキルが使う経路そのもの）。
`uv run` でも凍結後でも起きる。ファイル入力 (`@path`) は最初から UTF-8 決め打ちなので、
`-` だけが穴だった。

**「いま読んでいるもの」はフォルダか URL のどちらか一方。** `config.set_reading_url()`
が立っていれば `sites/` の下を最長一致で探し、そうでなければフォルダを祖先方向に探す。
併用しないのは、小説フォルダを開いたまま Web を読んで登場人物名が無関係なページで
リンクになるのを防ぐため。**URL 側は辞書を勝手に作らない**ので
`local_glossary_dir()` は `None` を返しうる —— `store.glossary_dir()` と
`local_available()` はそれを前提にしている。

**URL のパスをそのままディレクトリ名にしない**（`sites.py`）。`..`・`:`・クエリ・
Windows の禁止文字を潰したうえで、組み立てた結果が `SITES_DIR` の外に出ていないことを
最後に必ず検査する。ホストらしくない入力（空白入り、ドットもポートも無い）は弾く。

**ローカル辞書は祖先方向に探す**（`config.local_root()`）。開いているフォルダから
いちばん近い `.glosspop` を使い、無ければ開いているフォルダに作る。1 巻 2 巻を別
フォルダにしていても作品フォルダに 1 つ置けば共有できる。遠い祖先の辞書が黙って
繋がると驚くので、**親を使っているときは UI にその場所を出す**
（`/api/content` の `local_dir` / `local_is_ancestor`）。

**アプリモードで開いた `msedge.exe` の寿命は当てにできない**（`appwindow.py`）。
`--app=<URL>` で起動したプロセスはブラウザ本体を別プロセスで生んですぐ終了するので、
`Popen.wait()` は「窓が閉じた」ことにならない。これを使って「窓を閉じたらサーバも
止める」を書いたら、**窓は開いたままサーバだけ落ちた**。専用プロファイルを
渡しているのは、普段のブラウザのセッションと混ぜないため（localStorage も別勘定になる）。

**窓の寿命はページからの生存確認で追う**（`watchdog.py` と `static/heartbeat.js`）。
守ること 5 つ:

- **専用ウィンドウで開いたときだけ数える**（`arm()` を呼ぶのは `app` の経路のみ）。
  `serve` で数えると、**タブを閉じただけでサーバが落ちる**
- **「閉じました」の合図は作らない。** `pagehide` で知らせる形にすると、
  ページを移動しただけ（覆いではない本物の遷移）で落ちる。**来なくなったこと**で
  判断すれば移動の途中では落ちない。そのぶん見切り (`IDLE_SECONDS`) は
  ページ側の間隔 (`PING_MS`) の 2 倍以上にすること（テストが見張っている）
- **開くまでの猶予を別に持つ**（`GRACE_SECONDS`）。ブラウザの起動に数秒かかるので、
  立ち上げ直後を「誰も見ていない」と判定しない。**ただし猶予にも上限を置く** ——
  窓が開けなかったときに、誰も見ていないサーバが残り続ける
- **`heartbeat.js` は全ページに要る**（`settings.js` と同じく HTML に 1 行）。
  ビューアだけに置くと、辞書ページを直接開いて読んでいる間に落ちる
- **止める先を持つために `uvicorn.run()` ではなく `Server` を自分で作る**
  （`cmd_serve`）。`uvicorn.run()` は掴む手段をくれない

**実行ファイルは 2 本ある。**`glosspop.exe`（`console=True`。CLI 用）と
`glosspopw.exe`（`console=False`。ダブルクリック用）。Windows の実行ファイルは
console / GUI のどちらかのサブシステムで作るしかないので、1 本では両立できない
（`python.exe` / `pythonw.exe` と同じ形）。守ること 4 つ:

- **大文字小文字だけで分けないこと。** Windows では `GlossPop.exe` と `glosspop.exe` が
  **同じ名前**なので、zip の展開や更新の入れ替えで片方が黙って消える。だから `w` を足す
- **`FreeConsole` で離脱する形に戻さないこと。** 一時期そうしていたが、**親に
  コンソールが無くなると子（claude）が自分の窓を作る** —— AI の下書きのたびに黒い窓が
  開き、閉じると下書きごと落ちた（`[WinError 6] ハンドルが無効です` も同じ原因）。
  `ShowWindow` で隠す案も駄目で、Windows 11 の既定のホスト（Windows Terminal /
  ConPTY）では `GetConsoleWindow()` が**見えないダミーの窓**を返す（実際に踏んだ）
- **子プロセスには `CREATE_NO_WINDOW` を渡す**（`llm.py` `picker.py` `appwindow.py`）。
  窓なしの親から起こしたコンソールアプリは、**指定しないと自分の窓を作る**。
  親のコンソールを間借りしていたぶんは、それまで見えていなかっただけ
- **窓なしでは `sys.stdout` / `sys.stderr` が `None` になる**（`entry.py` の
  `_ensure_streams()`）。`print()` は黙って読み飛ばすが **uvicorn のログはそうではない**
  ので、塞がないとサーバが立ち上がらない。**リダイレクトすると再現しない**ので、
  確かめるときは素で起動すること（`check-exe.ps1` がそうしている）

**アイコンは 2 本で別にする**（`packaging/icons/`）。exe が並ぶので、同じ絵だと
**どちらを押すのか分からない**。`glosspopw.exe` は `static/favicon.svg` と同じ図案
（タスクバーとブラウザのタブが揃っていないと同じアプリに見えない）、`glosspop.exe` は
端末らしい図案。**`.ico` はビルド時に作らない** —— `make-icons.py` が playwright で
描くので、そこを通すと CI とビルドにブラウザが要る。svg を直したら手で走らせて、
出来た `.ico` も一緒にコミットすること。

**窓を出す合図は「自分のサーバが listen したか」**（`cli._wait_started`）。
ポートが開いたかで見ると、**閉じた直後に開き直したとき**（前のサーバが生存確認で
終わるまで 20 秒ほどある）にそのポートは「開いている」ので、**もうすぐ死ぬ古い
サーバへ向いた窓**が出る。`server.started` を見れば、こちらが bind に失敗したことも
同時に分かる。`--reload` のときだけは Server を掴めないので従来どおりポートで待つ。

**フォルダ選択ダイアログは必ず別プロセスで開く**（`picker.py`）。tkinter はメイン
スレッドでしか安全に動かず、FastAPI のワーカースレッドから呼ぶと固まる。凍結した
exe には python インタプリタが無いので、自分自身を隠しコマンド `__pick-folder` で
再実行している（`cli.main()` の先頭で引数を横取りしている）。**`glosspop.spec` の
`excludes` に tkinter を戻さないこと** — 戻すと exe 版だけダイアログが開かなくなる。

**ファイルを読むのは `documents.py` の仕事。** `path.read_text()` を直接呼ばないこと。
吸収しているのは 3 つ:

- **文字コード** — UTF-8 → cp932 → euc_jp。青空文庫は Shift_JIS で、UTF-8 決め打ち
  （`errors="replace"`）だと化けたまま「読めてしまう」ので気付きにくい
- **改行** — バイト列から decode すると CRLF が素通りする。`\n\s*\n` で段落を
  数えている箇所（ネタバレ抑止）が静かに壊れるので `decode()` で LF に揃えている
- **位置** — epub は章名、pdf はページ番号、それ以外は行番号。`Document.locate()`

**全ファイルを読む経路は `read_cached()` を使う**（横断検索・フォルダ抽出・
初出探し・ビューアの読み出し）。epub の解釈が重く（1 冊 9.6 ms。`.md` は 0.1 ms）、
**検索 1 回の 95% がそこだった**。初出探しは**語ごとに呼ばれる**ので、同じ epub を
語の数だけ読み直していた（20 語で 178 ms → 17 ms）。

**これは索引ではない。** 毎回すべてのファイルを見るのは変わらず、変わっていない
ものを読み直さないだけ。ここを本物の索引（どの語がどこにあるか）に変えると、
取りこぼしが**「その語は無かった」と区別が付かなくなる** —— 打ち切りを必ず返して
いるのと同じ理由で、検索は黙って欠けてはいけない。鍵はパス + mtime + サイズなので
**外のエディタで書き換えれば読み直す**（テストで見張っている）。上限は**数と総文字数の
両方**を持つ —— 数だけだと長編ばかりのフォルダで数百 MB を抱えうるし、文字数だけだと
小さいファイルが際限なく増える。返す `Document` は使い回されるので書き換えないこと。

**検索の重さは語によって違う。** よく出る語は `MAX_SEARCH_HITS` ですぐ打ち切られる
ので速く、全部読むのは**見つからない語を探したとき**だけ。性能を測るときは
見つからない語で測らないと、何も測っていないことになる。

**epub の manifest の href は percent-encoded。** OPF の href は IRI なので、日本語や
スペースを含むファイル名は `%E7%AC%AC...` で入っている。`zipfile` は生の名前しか
受けないので、復号せずに読むと `KeyError` になり**その章だけ黙って消える**
（`_read_member()` が両方を試す）。章名が英数字の epub では気付けない。

**青空文庫の正規表現は行単位で書く。** 冒頭の説明ブロックを `(?:.*\n){0,6}?...` と
DOTALL で書いたら 4 万字の作品でハングした（`.` が行をまたいで際限なく戻る）。
`[^\n]*` を使い、走査範囲も先頭 3000 字に限ること。

**ルビは `rt` / `rp` を中身ごと捨てる**（`htmlclean.DROP_TREES`）。タグだけ落とすと
読みが地の文に残り、`銀河ぎんが` になって照合が壊れる。親字側の `ruby` / `rb` は
`KEEP_TAGS` と `linker.INLINE_TAGS` の両方に入れて、ルビをまたぐ語を 1 語として繋ぐ。

**ビューアで開ける拡張子は 2 か所にある。** `app.CONTENT_SUFFIXES`（一覧と読み出し）と
`viewer.js` の `OPENABLE`（リンクを辿るかの判定）。片方だけ足すと、一覧には出るのに
リンクからは開けない（またはその逆）という食い違いになる。

**ローカル `.html` には base_url が無い。** `htmlclean._safe_url()` は base_url が
空のときだけ相対 href を残す（`allow_relative`）。ここを緩めて `src` も残すと、
配信する経路が無いので壊れた画像が並ぶ。URL 経由では base_url があるので従来どおり
絶対化される。

### AI 下書き（`ai.py` と `llm.py`）

**「何を頼むか」と「誰にどう頼むか」を分けてある。** `ai.py` がプロンプトの組み立てと
後処理、`llm.py` が提供元（Claude Code CLI / Gemini API）・モデル・思考の深さ。
`ai.py` は提供元を知らず、外に触るのは `ai._generate()` の 1 か所だけ
（テストもそこを差し替える）。**提供元ごとに差し替え先が変わる作りにしないこと。**

**モデルを既定の引数に焼き込まない。** `GLOSSPOP_CLAUDE_ARGS` の既定に
`--model sonnet` が入っていたため、⚙ でモデルを選んでも**追加引数が後勝ちして
効かなかった**（設定を足したその日に踏んだ）。既定は `llm.DEFAULT_CLAUDE_MODEL`
に置き、追加引数に `--model` を書いた人の指定だけを尊重する。

**文体（口調）は `ai.py` が持つ。** 「何を頼むか」であって「誰にどう頼むか」では
ないので、`llm.resolve()` には入れない（同じ ⚙ の AI タブに出るというだけで、
繋いでいるのは `app.py`）。**効く範囲を毎回プロンプトに書き添えること**
（`build_style_block()` の `fields` / `keep`）—— 範囲を書かずに文体だけ渡すと、AI は
**用語名も関係の相手も**その口調に崩す。ところが `term` は文書中の表記と一致しないと
`filter_candidates()` が、`from` / `to` は一覧の表記と一致しないと
`filter_relations()` が落とすので、**頼んだ側には「なぜか候補が 1 つも出てこない」
としか見えない**（カテゴリはディレクトリ名にもなる）。**抽出には効かせない** ——
返ってくるのは本文の表記そのままなので、混ぜても得るものが無い。

**文体はフォルダごとに置ける**（`config.local_style_file()` → `.glosspop/style.md`）。
置き場所を辞書と同じにしてあるので、**祖先方向の探索も効く範囲も辞書とずれない**。
設定ファイル側に「フォルダ → 文体」の対応表を持たせないこと —— フォルダを移動・
コピーした瞬間に切れる。守ること 4 つ:

- **優先順は 環境変数 > 📁 このフォルダ > 全体 > 既定。** どれが効いているかは
  `describe_style()` が返し、画面に必ず出す（相関図の範囲と同じ約束）。**押しのけた
  側の値も返す** —— 出さないと「全体に書いたのに効かない」の原因が見えない
- **基準は「いま読んでいるもの」で、エントリの保存先 (`scope`) ではない。** 保存先で
  切り替えると、`scope: "auto"` は語ごとに行き先が変わるので **1 回の「まとめて登録」
  の中で口調が混ざる**
- **覚え込まない。** `_folder_style()` は呼ばれるたびにファイルを開く（外のエディタで
  書き換えたぶんを黙って無視しないため。`store._signature()` と同じ理由）。AI を呼ぶ
  経路でしか通らない数百字のファイルなので、毎回読んで困らない
- **書き込みは `/api/ai/style` の 1 か所だけ**（`/api/ai/settings` には載せない）。
  📁 はフォルダに `.glosspop` を作りうるので、モデルを選び直したついでに書かれると
  「開いただけのフォルダを汚さない」が崩れる。空文字はファイルごと消す

**用語ごとの画像 (`images/<カテゴリ>/<slug>.<拡張子>`) は顔とは別の軸。**
顔は「誰が書いているか」で辞書に 1 枚、こちらは**その語そのもの**で語ごと。
守ること 6 つ:

- **鍵は ref（2 段）。名前だけにしない** —— `images/<slug>` にすると
  **カテゴリ違いの同名が同じ画像を指す**（`find_by_surface()` がリストを返す理由と
  同じ話）。「カテゴリを移すと取り残される」は**移す側で塞ぐ**（`store.move()` が
  画像も動かし、`delete()` は消し、`merge` は残す側に無いときだけ引き継ぐ）
- **見分けは `core.imagefmt.sniff()` 1 か所**（顔・地図・用語で共用）。写しを作ると、
  対応形式を足したときに**片方だけ通る** —— 実際 `persona_types` が返す一覧は
  WebP を落としていて、受け入れる側とずれていた。**SVG を通すかは呼ぶ側が決める**
  （地図だけが出し方で担保している）
- **一覧には出す。** 顔を出さない理由は「辞書に 1 枚なので同じ絵が並ぶだけ」で、
  **語ごとに違うこちらには当てはまらない**。ただし数千枚になりうるので
  `loading="lazy"` を外さないこと。**URL は `_image_index()` が 1 回の走査で作る**
  （語ごとに `image_file()` を呼ぶと 3000 語で 15,000 回 stat が飛ぶ）
- **吹き出しでは用語の画像が顔に勝つ**（同じ場所を取り合うので、その語の絵を優先）。
  判断は `popup.js` の `faceOf()` / `hasFace()` の 2 つだけで、呼ぶ側に書かない
- **地図では点の代わりに出せる**（`map.js` の `FACE_R`。切り替えは絵ごとに覚え、
  **既定は丸のまま** —— 見え方が大きく変わるので頼まれていないのに変えない）。
  **点だけ**にすること —— 線と領域は形そのものが場所を表しているので、真ん中に
  絵を置くと**自分の形を覆う**。**画像のある語だけが絵になる**ことは凡例に書く
  （書かないと「読み込めていない」に見える）。URL を足すのは `_graph_images()` で、
  **地図に置かれた語だけ** —— 全ノードに足すと、地図を出していないときまで
  `_image_index()` の走査が全リクエストで通る
- **zip には入れるが、取り込みでは消さない**（地図とまったく同じ約束）。
  `images/` は 2 段、`maps/` は 1 段 —— **同じ prefix にまとめない**

**語り手の顔 (`persona.*`) はエントリの居場所につく。文体とは基準が違う。**
文体は「いま読んでいるフォルダ」（これから書くときの指定だから）、顔は
「そのエントリがどの辞書にあるか」（すでに書かれたものの出どころだから）。
**揃えないこと** —— 揃えると、小説のフォルダを開いている間だけ全体の辞書の用語にまで
その作品の顔が付く。守ること 6 つ:

- **`store.persona_file(scope)` / `store.persona_dir(scope)` を通す**
  （`glossary_dir()` と同じ）。ローカルは読んでいるものに追従するので、参照のたびに
  解決し直す。**「見つける」と「置き場所」は別の関数** —— 場所は決まっているのに
  顔がまだ無い、という状態が普通にある
- **パスを外から受け取らない。** 名前 (`persona`) も拡張子も決め打ちで、`scope` から
  場所を引くだけ（控えの取り出しと同じ規則）。**SVG は扱わない** —— スクリプトを
  持てるうえ、`GET /api/persona` は中身を検査せずにそのまま返す口
- **画面から差し替えられるが、拡張子は中身から決める**（`ai._sniff_persona()`）。
  送られてきたファイル名も Content-Type も**名乗りでしかない** —— `.png` という名前の
  HTML を置かれると、配る側が検査しない以上そのまま配ってしまう。だから
  `POST /api/persona` は生のバイト列で受け（multipart にしてファイル名を受け取らない）、
  マジックバイトで見分けた拡張子だけを使う。**上限も持つ**（`PERSONA_MAX_BYTES`）
- **書いたら別の拡張子の顔を片付ける**（`ai._clear_personas()`）。残すと
  `find_persona()` の探索順で決まる顔が出て、「差し替えたのに変わらない」になる
- **URL に更新時刻を入れる**（`_persona_url`。`/api/ai/*` の応答へ足すのは
  `app._ai_state()` の 1 か所で、`ai.describe_style()` に URL の写しを作らない）。
  入れないと差し替えても古い顔が出る
- **一覧 (`glossary.js` の `card()`) には出さない。** 顔は辞書に 1 枚なので、カードが
  並ぶと同じ絵が何十個も続くだけで何も区別できない。出す価値があるのは、複数の辞書が
  並ぶ吹き出し（行ごとに違う顔）と、1 件を見る用語ページ

**Gemini の思考の深さは世代で項目が違う。** 3 系は `thinkingLevel`、2.5 系は
`thinkingBudget` しか受けず、取り違えると 400 で落ちる（実測）。モデル名で見分けると
新しい世代が出るたびに直すことになるので、**API が「その項目は無い」と言ってきたときだけ
1 度言い直す**。**モデルの一覧も焼き込まない** —— API から引く（記憶で書いたら、
手元のキーで引いた一覧には知らない世代が並んでいた）。

**API キーは返す口を作らない。** 設定ファイルに置けるようにしてあるが、
`/api/ai/settings` が返すのは「設定済みかどうか」だけ。環境変数
（`GEMINI_API_KEY`）が優先で、その場合は UI から触らせない。

`claude -p` は API キー不要だが、**プロジェクト内で実行すると
CLAUDE.md と `gloss-add` スキルを拾い、「重複を確認するため CLI を実行させて」と
要求して承認待ちで固まる。** そのため:

- cwd は `tempfile.gettempdir()/glosspop-ai`（プロジェクト外）
- `--disallowed-tools` で全ツールを落とし、`--disable-slash-commands` を付ける
- `--bare` は使えない（ANTHROPIC_API_KEY 必須になり、OAuth を読まなくなる）

プロンプトを触るときは、カテゴリの提案が既存へ寄りすぎないか実際に確かめること
（音楽用語を「プログラミング」に入れる、他カテゴリのサブカテゴリを流用する、
といった失敗を実際に踏んだ）。

**ネタバレ対策（`spoiler`）はプロンプトだけでなく、渡すテキスト自体で担保する。**
`position` は AI を呼ばない（`/api/ai/draft` が claude 抜きで返す唯一の経路）。
`first` は `context_up_to_first()` が初出の前後だけを切り出す。**後ろは文字数ではなく
段落の切れ目で打ち切る** — 初出が章末にあると N 文字取るだけで次の章まで入り、
「読んでいないはずの展開」が要約に出る（実際にテストで踏んだ）。プロンプトの禁止文だけに
頼らないこと。

**抽出は「何を抜き出すか」を先に決めさせる（`EXTRACT_KINDS`）。** ただ
「辞書化する価値のある語」と頼むと、AI は**語義を説明できる語**ばかり挙げ、
**登場人物がまるごと落ちる**。人名は「意味の分からない語」ではないので、その基準に
引っかからない。種別ごとに独立した枠を与え、プロンプトで**枠の振り替えを明示的に
禁じる**ことでしか埋まらない（「集まらなくても他へ回さない」の一文を消さないこと）。

**上限も種別ごとにかける**（`allocate_quota()`）。合計に対して先頭から切ると、AI が
人物を先に並べただけで専門用語が全部消える。`filter_candidates()` の `limit` は
**合計の厳密な上限**で、種別数より小さいと 0 件の枠ができる。実際の経路
(`extract_terms()`) だけが下限を持ち上げている ——「選んだ種別が黙って出てこない」が
いちばん困るので。

**関係の下書き (`draft_relations`) は用語を作らない。** 一覧に無い語を AI が足して
きたら `filter_relations()` が落とす。ここを通すと、本文中の別の呼び方や AI の
言い換えが行き先になり、**保存はできるのにリンクも図の辺も生えない**関係ができる
（候補語で `filter_candidates()` がやっているのと同じ話）。

**1 語ぶんの下書き (`focus`) は、頼む側と落とす側の両方で絞る。** 落とすだけに
すると、上限の大半を他人どうしの関係が食って肝心の語の関係が数本しか残らない。
**範囲そのものを 1 語にはできない** —— 関係は 2 語が揃って初めて書けるので、相手は
今までどおり範囲（既定は辞書全体）から選ばせる。渡すテキストも絞ること
（`cooccurrence_context(focus=…)` / `first_scene_context(focus=…)`）—— **その語が
写っていない場面を渡しても、頼んだ関係は絶対に出てこない**ので、1 語ぶんのときは
頭出しへの落としもしない。

**焦点の表記は焦点**（`ai._resolve_side()`）。揺れの吸収は `relations.resolve()` 任せの
ままで、足しているのはこれ 1 つだけ —— 用語ページから頼んだときは**どのエントリの話かが
画面で決まっている**（吸収する側には分からない情報）。無いと、**別のエントリが同じ表記を
別名に持っているだけで下書きが丸ごと落ちる**（「寒月」から頼んだのに「水島」が別名に
「寒月」を持っていて決めきれず、1 本も残らなかった。同じ人物が 2 エントリに割れている
辞書では普通に起きる）。**焦点が無いときは今までどおり寄せないこと。**

**まとめて登録は「抽出 1 回 + 下書き N 回」。** `extract_terms()` は候補語だけを 1 回で
挙げさせ、本文の生成は選ばれた語について `/api/ai/draft` を語数ぶん呼ぶ（クライアント側で
逐次）。ここを 1 回のプロンプトで N 語ぶんの本文まで作らせる形にすると、出力が長くなって
品質が落ちる。サーバに進捗の状態は持たせていない。

**保存先（`scope`）も AI に選ばせている。** `/api/ai/draft` に `scope: "auto"`
（既定）を渡すと、プロンプトに「保存先」の節とフォルダ名が入り、`scope` を答えさせる。
明示された値が来たときは聞かない（答えより指定が優先）。**カテゴリマスターを触るかは
解決後の `scope` で判断する** — リクエストの値で判断すると、AI がローカルを選んだのに
マスターへ登録してしまう。

**AI が挙げた候補はそのまま使わない。** `filter_candidates()` が、登録済みの語と
**文書中にその表記で現れない語**を落とす。後者を通すと「登録したのに本文でリンクにならない」
エントリができる（AI は平気で原形や言い換えを返す）。落とした語は理由つきで UI に出す。

**長い本文は頭から切らずに全体から間引く**（`sample_text()`）。`text[:12000]` のように
先頭だけ渡すと、345,000 字の小説では**冒頭 3.5% しか AI に届かない** —— 『吾輩は猫である』で
迷亭も寒月も一度も渡っておらず、「候補に挙がらない」「関係が引けない」の正体がこれだった。
窓の頭は `[0, 末尾-take]` に等間隔で置く（`len` で割ると**結びが一度も届かない**）。
切れ目には `〔中略〕` を入れ、**間引いたことをプロンプトにも書く** —— 黙って繋ぐと、
飛んだ前後を連続した話として読まれる。`combine_documents()` の 1 ファイルあたりの枠は
**下限**で、文書が少ないときは全体の枠を等分する（長編 1 冊のフォルダで冒頭 3000 字しか
読めなかった）。

**候補語の抽出はフォルダを横断しない**（`extract_terms()` の 1 つだけ。入り口も
ビューアの ✨ 用語を抽出 だけ）。かつて `/api/ai/extract-folder` があったが、
`combine_documents()` で何ファイルまとめても `build_extract_prompt()` が
`EXTRACT_TEXT_CHARS` まで間引き直すので、**AI に届く量は 1 文書のときと変わらない**
（5 ファイルで 24,129 字 → 11,762 字。`### ファイル名` の行も割れて出典が壊れる）。
薄まったぶん取り分が減り、待ち時間（実測 100〜260 秒。見積もりの 260 秒とほぼ同じで、
タイムアウトしたりしなかったりしていた）だけが積み上がる。**戻すなら先に「渡せる枠を
増やせるか」から** —— 枠が同じまま入り口だけ戻すと、消した理由がそのまま戻る。
`tests/test_ai.py` の `TestFolderWideExtractIsGone` がこの実測を持っている。

**ただし関係の下書きのフォルダ経路は残してある**（`app._read_content_docs()`）。
用語ページの「✨ この語の関係を下書き」には**表示中の文書が無い**ので、ここでしか
本文を得られない。抽出と違って**待ち時間には効かない** —— 渡すのは読んだ本文ではなく
`cooccurrence_context()` / `first_scene_context()` が選んだ窓で、読む代金は
749,051 字を `read_cached` して 17.6 ms（温まれば 3.2 ms）。**同じ「フォルダを読む」
でも話が違うので、まとめて消さないこと**（実際に消しかけて、用語ページのボタンが
「読める文書がありません」しか返さなくなった）。

**持ち時間は「出す件数」と「頼む相手」から見積もる**（`llm.estimate_timeout()`）。
既定の 180 秒では関係の下書きが**必ずタイムアウトする**（実際に踏んだ）。**所要時間は
出力トークン数（思考を含む）にほぼ比例する** —— 毎秒 90 トークン前後、関係 20 本ぶんで
12,000 トークン＝約 140 秒。起動は 5 秒しかかからないので、**そこを削っても意味は無い**。
**提供元で桁が違う**（同じ 19 語・20 本要求で Claude 258.6 秒 / Gemini 72.4 秒）ので、
`SECONDS_PER_ITEM` を提供元ごとに持つ —— 同じ見積もりを当てると、速いほうで
「駄目だった」と分かるのが遅すぎる。UI 側では本数を選ばせて経過秒を出す
（応答は最後にまとめて返るので、出さないと固まったように見える）。

**一時的な失敗だけ引き直す**（`llm.TransientError`）。高負荷 (503) やレート制限 (429) は
待てば直るのに、数分かけた下書きがその一言で丸ごと消えるのは割に合わない（Gemini で
実際に踏んだ）。**恒久的な失敗（鍵違い・モデル名の誤り）は引き直さない** —— 待たせた
うえで同じ結果になる。`timeout` は**全体の持ち時間**として扱い、残りを次の試行に配る
（1 回ごとに与えると、引き直すたびに待ち時間が倍々になる）。CLI 側は文言で見分けるので
`_TRANSIENT_MARKS` を増やしすぎないこと。

**セッションを継いでも速くならない。試す前にここを読むこと。** `claude -p` は毎回
使い捨てなので「親セッションを立てて語ごとに `--fork-session` で枝分かれさせれば、
本文を渡し直さずに済んで速いはず」と考えたくなる。**実測は逆で、遅くなった**
（2 語ぶんの下書きで 25.8 秒 → 41.3 秒）。理由は 2 つ:

- **入力の再処理はほぼ無料。** 本文 12,000 字を渡して答えさせるだけなら 4.8 秒。
  起動も 5 秒。**節約できる余地がそもそも無い**
- **文脈に本文を抱えたまま質問すると、モデルが余計に考えて出力が増える**
  （同じ下書きで 476 → 714 トークン）。所要時間は**出力トークン数（思考を含む）に
  ほぼ比例する**（毎秒 90 トークン前後）ので、そのぶん遅くなる

「2 回目は速い」という測定結果が出たら、**同じ仕事量を比べているか**を先に疑うこと
（20 本と 3 本を比べて「3.6 倍速い」と誤読した）。速くしたいなら**書かせる量を減らす**
（本数を絞る）のが唯一効く手で、渡す本文を削っても効かないうえ、関係が引けなくなった
原因（本文が足りない）に逆戻りする。

なお仮に速くなったとしても、`--resume` は**前の回の本文を文脈に残す**ので
**ネタバレ設定をまたいで継いではいけない** —— この辞書のネタバレ抑止は「渡すテキスト
そのもので担保する」（プロンプトの禁止文に頼らない）という約束の上に立っているので、
継いだ瞬間に「初出の場面だけ」の保証が嘘になる。

**初出モードの本文は、全員ぶんが入るように配分する**（`first_scene_context()`）。
初出窓（1 語あたり最大 2,400 字）をそのまま繋いで頭から切っていたため、19 語では
**52% が黙って落ち、後ろに並んだ主人・吾輩・迷亭・黒 が丸ごと消えていた**。関係は
2 語が揃って初めて書けるので、片方が消えた時点でその関係は**絶対に出てこない**
（「初出モードでは関係が出ない」の正体がこれ）。切り詰めるときは
`_densest_window()` が**相手の名前も写っているところ**を残す —— 頭から取ると、その語が
出てくる前の場面ばかりが残って関係が読めない。**初出窓の外は見ない**ので、ネタバレの
約束は変わらない（テストが実際に窓の内側であることを確かめている）。

**関係の下書きに渡すのは「登録済みの語が一緒に出てくる場面」**（`cooccurrence_context()`）。
関係が書いてあるのは 2 つの名前が近くに並ぶところであって、文書の冒頭ではない。
ここを `combine_documents()` の頭出しに戻すと、長編では関係の場面が一度も届かず
**「見つかりませんでした」しか返さない**（それが実際の壊れ方だった）。窓は**別名も同じ
エントリとして**数え、重なる窓は捨てる（同じ場面を何度渡しても新しい関係は出ない）。

**同じものの別の呼び方は新しいエントリにしない**（`split_aliases()` → `/api/aliases`）。
「主人」と「苦沙弥先生」が別エントリに割れると、本文のリンク先も相関図のノードも二重になる。
抽出のプロンプトに `alias_of` を答えさせ、**行き先が 1 つに決まるときだけ**別名の枠に回す
（決まらないときは黙って寄せず、普通の候補として扱う）。**本文に無い表記は別名にもしない** ——
候補語と同じで、登録してもリンクにならない。

## 確認用スクリプト

`check.cmd`（メニュー / `test` `app` `build` `rebuild` `exe` `kill` `all`）と
`packaging/check-exe.ps1`。**大半の変更はビルドせずに確認できる** — 凍結でしか
変わらないのは `config.DATA_ROOT`・`cmd_serve` の文字列 import・`picker` の子プロセス・
`entry.py` の既定引数の 4 か所だけで、どれも `sys.frozen` を差し替えるテストで見られる。
ビルドが要るのは **`hiddenimports` / `datas` の漏れ**だけ（静的解析で追えないものが対象なので、
原理的に凍結しないと出ない）。手元で繰り返すときは `build.ps1 -Fast`（`--clean` を付けない。
48 秒 → 21 秒。**spec や依存を変えたときは付けないこと**）。

**`.cmd` / `.bat` は ASCII のみ、改行は CRLF。** `cmd.exe` はコンソールのコードページ
（日本語環境なら CP932）で読むので UTF-8 の日本語は化け、**2 バイト目が `0x5C` になる文字
（`表` `十` `ソ` など）は構文解析を壊す**。LF だとラベルや複数行が壊れる（`.gitattributes`
で `eol=crlf` に固定してある）。日本語を書きたい処理は `.ps1` 側に置くこと。

**`.ps1` は UTF-8 BOM 付きで保存する。** Windows PowerShell 5.1 は BOM が無いと ANSI
（CP932）として読むので、日本語の文字列リテラルが壊れて構文エラーになる。`check.cmd` は
`pwsh` があればそちらを使うが、5.1 に落ちても動くように BOM を付けてある。**BOM を外さないこと。**

**exe の確認は「起動の前後の差」で見る。** 専用ウィンドウが開いたかを「いま開いている窓の数」で
判定すると、手元で試した窓が残っているだけで落ちる（実際に踏んだ）。子プロセス（`--type=` 付きの
renderer / gpu）も数に入るので、ブラウザ本体だけを見ること。

## Windows ビルド（`packaging/`）

`.\packaging\build.ps1` で PyInstaller の onedir ビルド（`dist\GlossPop\`）を作る。
手順は [DEVELOP.md](DEVELOP.md) にある。ここには**壊しやすい点**だけ:

**凍結すると `__file__` は一時展開先（`_internal`）を指す。** そのため
`config.APP_DIR` は `sys.frozen` のとき `sys.executable` の親を基準にする。
ここを `PACKAGE_DIR` 基準に戻すと、**保存した辞書が起動ごとの一時ディレクトリに
書かれて消える**（サーバは正常に動いて見えるので気付きにくい）。逆に `STATIC_DIR` は
`_internal` 側で正しいので `PACKAGE_DIR` 基準のまま。

**`APP_DIR` と `DATA_ROOT` は別物。** `APP_DIR` は更新のたびに入れ替わる側、
`DATA_ROOT` は残る側で、既定では同じ場所を指す。ここを混ぜると、保存先を外に
移す設定（⚙）が意味を失う。

**設定ファイルは `APP_DIR` の下に置かない**（`config.SETTINGS_FILE`）。中身は
「データをどこに置くか」なので、アプリのフォルダを丸ごと入れ替えたときに一緒に
消えたら意味が無い。OS のユーザー領域に置いてある。テストは
`GLOSSPOP_SETTINGS_FILE` で逃がすこと —— **本物の `%APPDATA%` に書くテストを
足さない**。

**優先順は 環境変数 > 設定ファイル > 既定。** 環境変数を最優先にしているのは、
テストと一時的な切り替えが設定ファイルに引きずられないようにするため。
壊れた設定ファイルでは既定に落ちる（起動できなくならないこと）。

**保存先の変更は次の起動から効く。** `config` は import 時に解決済みで、途中で
差し替えると `store` のキャッシュや開いているフォルダの状態と食い違う。
`/api/settings` は設定ファイルを書くだけにして、UI に再起動を促している。
**ここを「その場で反映」に変えないこと。**

**データの複製 (`copy_data_root`) は元を消さない。** 移した先で問題が出たときに
戻れるようにするため。ブラウザプロファイルのキャッシュは運ばない（名前に `Cache` /
`Crashpad` を含むディレクトリ）が、**`Local Storage` は運ぶ** —— お気に入り・
最近開いたフォルダ・ネタバレ設定がそこにあり、落とすと「辞書は無事なのに設定だけ
消えた」になる。

**文字列 import は凍結後に解決できない。** `cmd_serve()` は `--reload` のときだけ
`"glosspop.app:app"` を渡し、通常は app オブジェクトを直接渡す。ここを文字列に
戻すと exe が `Could not import module "glosspop.app"` で即死する。

**動的に読むものを足したら spec に足す。** 静的解析で追えないものは
`packaging/glosspop.spec` の `hiddenimports` / `datas` に書く必要がある
（`glosspop.*` は `collect_submodules` でまとめて入れてある）。新しい依存を
`pyproject.toml` に足したときは、**ビルドして exe を起動するところまで確認する**
——`uv run` では動いても凍結すると落ちる、という差が出るのはここだけ。

## フロントエンド

依存ゼロの ES モジュール。ビルド工程は無い。`static/` を編集したら
サーバ再起動は不要（ファイルはディスクから読まれる）。

`/static` と HTML には `Cache-Control: no-cache` を付けている。これが無いと
ブラウザが古い JS を出し、原因不明の壊れ方をする（実際に半日溶かした）。
`RevalidatingStatic` を消さないこと。

- `base.js` — API 呼び出し、DOM ヘルパ、URL 判定、テーマと表示オプション
- `ai-style.js` — 文体（口調）と語り手の顔の編集。**⚙ の AI タブが唯一の口**
- `select-add.js` — 選択 →「＋ 辞書に登録」。ビューア・用語ページ・一覧で共用
- `progress.js` — 読書位置の記憶。ビューアからだけ使う
- `extract.js` — 「✨ 用語を抽出」→ 候補選択 → 下書き → まとめて保存
- `popup.js` / `editor.js` — 吹き出し・登録ダイアログ。どのページからも使える
- `speech.js` — 本文の読み上げ（Web Speech API）
- `heartbeat.js` — 専用ウィンドウで開いたときの生存確認。**全ページに要る**
  （`settings.js` と同じく HTML に script タグを 1 行足すだけ）
- `relations-draft.js` — 関係の AI 下書き → まとめて書き込み
- `merge.js` — 割れてしまった同じものを 1 つにまとめる。相手を選ぶ → 下見 → 実行
- `settings.js` / `update.js` — ⚙ の中身と更新のお知らせ。全ページの topbar に
  自分で差し込むので、各ページの JS からは import しない（HTML に script タグを
  1 行足すだけ。`settings.js` が `update.js` を import している）
- `overlay.js` — ビューアの上に他の画面を重ねる覆い。ビューアからだけ使う
- `viewer.js` / `glossary.js` / `entry.js` / `graph.js` / `doctor.js` — 各画面
- `dict-tabs.js` — 辞書の見方のタブ（一覧 ＋ 5 つの図）。**6 つの正はここ 1 か所**
  （地図は列の外。`OFF_TAB_MODES`）
- `graph-model.js` — 図の**形**だけ（段・孤立語・並びの種）。描き方は知らない
- `fabric.js` / `matrix.js` / `ego.js` / `timeline.js` — 図のもう 4 つの見せ方
  （交差しない図 / 行列 / 中心の図 / 時系列）

**操作の置き場所は 5 つしかない。新しいボタンを行に足す前にここを見ること。**
機能が増えるたびにツールバーへ 1 つ足していたら、1280px で 2 行に折り返し、
折り返した先で**「絞り込み」と「操作」の境目だった空白が消えていた**
（→ docs/ui-inventory.md にそのときの実測がある）。**幅の話は
「ボタンの文字は折り返さない」で解決済みで、ここで決めるのは折り返す前の中身**。

| 置き場所 | 当てるもの | 実装 |
| --- | --- | --- |
| 常時 | 毎回使う・**その画面にしか無い** | 行に直接 |
| タブ | **同じものを別の見方で出す**（置き換えではない） | `role="tablist"` |
| 畳む | ときどき使う・入口は残したい | `<details>`（**開閉は覚える**） |
| ⋯ メニュー | たまに使う・**消える操作** | `base.js` の `menuButton()` |
| ⚙ | 一度決めたら変えない | `settings.js` |

守ること 6 つ:

- **⋯ メニューは `base.js` の `menuButton()` だけ。** 画面ごとに書くと、Esc で
  閉じる・外を押すと閉じる・矢印キーで辿る・覆いを開き直したときに片付ける、の
  どれかが必ず抜ける（覆いは何度でも `mount()` を呼ぶ）
- **消える操作は ⋯ の中で、区切り線の下に置く**（用語ページの「まとめる」「削除」）。
  外に残すのは**戻せる操作だけ**。並びが意味を持たなくなると、削除が編集と同じ
  重さに見える
- **主要操作（`.primary`）は画面に 1 つまで。** 0 でもよい（相関図には無い）が、
  2 つ置くとどちらが主役か決まらない
- **タブにしたら、そのタブ専用の操作はタブの下の行へ移す。** 上の行に混ぜると
  **行の顔ぶれがタブごとに変わり**、「いま何ができるか」が固定でなくなる
  （相関図がそうなっていた —— 見せ方 6 通りでツールバーが 6〜11 個に伸び縮みした）
- **タブを disabled にしない。** 押せて、理由が出る形にする —— 時系列の軸が無い
  ときに段の図へ落として断りを出す、という既存の約束と揃える（押せないタブは
  **なぜ押せないのかを言う場所が無い**）
- **書き出しは ⋯ の中に置き、形式もその中で選ばせる**（冊子・図の画像・zip）。
  **ボタンの隣に形式の select を並べないこと** —— 折り返したときに別の行へ割れて、
  **何の形式なのか読めなくなる**（実際に `⬇ 画像` と `PNG▾` が割れていた）。
  形式の出し方は 2 通りでよい —— **ほかにも聞くことがあるならダイアログ**（冊子は
  巻末索引の有無も聞く）、**形式だけならメニューの項目を分ける**（図の画像は
  PNG / SVG の 2 つ。1 押しで済むものをダイアログにしない）

**選択肢が数十個になる select は作らない。** 実データで**34 語の辞書にタグが 69 個**
（うち 58 個は 1 語にしか付いていない）あり、開いた時点で読めなかった。
`<input list=...>` + `<datalist>` にすること —— 打てば絞れて、空のまま開けば
今までどおり全部出る。**候補から件数を落とさないこと**（どれが効くのか分からなくなる）。

**「読むもの」は横一列 1 行（上のバー）、ビューアの左は 🔍 とファイル一覧。**
フォルダのパスは**横に長い**（`C:\...\小説\銀河鉄道`）ので、幅 280px の縦のパネルに
置くと折り返して、いちばん使うファイル一覧を下へ押し下げる（最近開いたフォルダの
select も同じ理由で読めなかった）。読み方の設定（テーマ・「最初の 1 回だけリンク」）と
AI 設定は ⚙ に集めてある。守ること 9 つ:

- **topbar には足さない。** あちらはブランド・ナビ・⚙・語数で既に埋まっていて、
  狭い画面では語数を落として凌いでいる（もう 1 つ足すと横スクロールが出る）。
  バーは topbar の下に**別の行**として敷く
- **`.layout` の外に出したものは `body.overlaid` にも書く**（`pointer-events: none`）。
  覆いは topbar の下を全部覆うので見えてはいないが、`.layout` に書いた規則だけでは
  バーに届かない
- **バーは 1 行に収める。** 置くのは**毎回使うものだけ**で、たまに使うものは
  ⋯ の中（＋ その他の開き方）、結果の出る場所が別にあるものはそちらへ（横断検索は
  左パネル）。**行に足す前に「毎回使うか」を問うこと** —— 一度これを怠って
  3 段（165px）まで伸ばし、そのぶんパスの欄が 160px まで潰れた
- **パスと URL の入力欄が行の残りを全部取る**（`flex: 1 1 240px`）。横に長いから
  このバーを作ったので、ここが狭いままでは意味が無い（実測: 1 行に絞って 757px。
  3 段だった頃は 160px）
- **バーの下の行は、入力欄から読めないことだけ**（`noteRoot()`。既定のフォルダ・
  親フォルダの辞書・エラー）。**空なら行ごと出さない** —— パスは入力欄が持っている
  ので、同じ文字列をもう一度出すと行を 1 つ食うだけ
- **畳める**（`setSourceFolded()`。覚える）。読む面積は縦がすべてなので、要らない
  ときは畳めること。**ただし空の帯にしない** —— いま読んでいるものと戻す道（⌄）は
  必ず残す。どちらも消すと、この行が何のためにあるのかも、どう出すのかも分からなくなる
- **フォルダと URL はタブで排他**（`viewer.js` の `showSourceTab`）。
  `config.set_reading_url()` が立っていればどちらか一方しか効かない、という不変条件を
  そのまま画面にしている。**開いたものに合わせて自動で切り替える** —— 手で選んだタブと
  実際に読んでいるものが食い違うと、いま効いている辞書がどちらか分からなくなる。
  切り替えの関所は **`paintUrlDictionary()` の 1 か所**（呼ぶ側それぞれに書くと必ず
  どれかで抜ける）。**バーの下の行（開いているフォルダ）も一緒に隠す** ——
  URL を読んでいる最中にフォルダのパスが残ると、同じ取り違えが起きる
- **1 つだけのファイルと貼り付けはダイアログの中。** どちらも「たまたま手元にある
  ものを 1 回見る」入口。**開き方は減らしていない** —— ドロップは今までどおり
  ウィンドウのどこでも効く（ダイアログの中にもそう書いてある）
- **横断検索は入口も結果も左パネル**（1 行目の 🔍 と、`showSearchPanel()`）。
  入口だけをバーに置くと、**押す場所と出る場所が離れる**うえ、バーの 1 行を食う。
  結果はファイル一覧と入れ替える ——
  どちらも「開く候補の一覧」なので、並べると狭いパネルで一覧が下へ押し下がり、
  **結果を見ながら本文を開く**ができない。**入れ替えた以上、戻る道を必ず出すこと**
  （`#searchBack`。検索欄は上のバーにあり、空にして押し直す道だけでは離れすぎている）。
  検索そのものを**消さないこと** —— 索引を持たない代わりに毎回全文書を読む機能なので、
  入口ごと無くすと打ち切りの話も見えなくなる
- **表示オプションは `base.js` が持つ**（`firstOnly()` / `setFirstOnly()` /
  `watchFirstOnly()`）。⚙ はビューアの上に重なって開くので、**押した瞬間に本文へ
  効かせる**（テーマと同じ）。`storage` イベントは**別のタブ**でしか飛ばないので、
  同じページに居るビューアには届かない —— 自前の watcher でやること

**文体と顔の編集は `ai-style.js` の 1 か所**（⚙ の AI タブ）。**写しを作らないこと**
—— 優先順の案内（「全体に書いたのに効かない」）と上限の文言が片方だけ古くなる。
以前はビューアのサイドバーにも 📁 だけを出していたが、左をファイル一覧だけにした
ときに畳んだ（`scopes` / `showPaths` は残してあるが、いまの呼び出しは ⚙ の 1 つだけ）。
守ること 2 つ:

- **呼ぶ側が決めるのは置き場所 (`host`) と出すスコープだけ。** ⚙ は全体と 📁 の
  両方を並べる（**優先順が読めるのはそこだけ**なので、片方に絞った画面を増やすなら
  「押しのけた側の値も出す」を一緒に持っていくこと）
- **中の印は `data-sref`**（`data-ref` ではない）。呼ぶ側が `[data-ref]` を集めて
  refs を作るので、同じ名前（`save` `status` `scope`）が混ざると乗っ取る

**辞書・用語・図・点検は、ビューアの上に重ねて出す**（`overlay.js`）。ページと
して開き直すと、戻るたびに本文を取り直して描き直すことになる —— 実測で図から
ビューアへ **149 ms**（39,000 字）、長編では往復 0.5 秒級。重ねれば**閉じるのは
5 ms** で、読書位置・スクロール・読み上げの状態もそのまま残る（→ design-notes）。
守ること:

- **画面の中身の出どころは 1 つ。** 各モジュールが `mount(host, …)` を持ち、
  HTML 側は空の入れ物 (`#page-root`) だけを置く。**HTML に写しを作らないこと** ——
  作ると「ページでは出るのに重ねると出ない」が起きる
- **`location` を直接読まない。** URL は `mount()` の引数で渡す（重ねているとき、
  `location` は覆いが出しているものを指すとは限らない）。topbar の `#count` だけは
  覆いの外なので `document` から引く
- **`/glossary` などのページは残す。** 直接開かれた URL・ブックマーク・別窓のため。
  どちらの入り口でも同じ `mount()` が動くので二重管理にはならない
- **id を衝突させない。** 同じ document に複数の画面が居るようになった
  （`#root` がビューアと点検/用語ページで当たっていた）。新しく足すものは
  `data-ref` にするか、覆いの中だけで一意になる名前にする
- **仕掛けたものは片付ける。** 開くたびに `mount()` が走るので、document への
  listener やボタンを足しっぱなしにすると開いた回数だけ増える
  （`select-add.js` は `AbortController` で外し、`popup.js` は二重 install を弾く）
- **閉じるときに描き直すのは、辞書が変わったときだけ**（`dictionaryRevision()`）。
  毎回描き直すと重ねた意味が無くなり、まったく描き直さないと**登録したのに本文が
  古いまま**になる。登録・編集・削除・統合はすべて `invalidatePopupCache()` を
  通るので、そこを数えている

**辞書の見方は 6 つあり、一覧もその 1 つ。** 正は `dict-tabs.js`（`LIST_TAB` +
`MODES`）で、`glossary.js` と `graph.js` の両方がそこからタブ列を描く
（**どちらにも写しを置かないこと** —— 片方だけ増えると、同じタブ列が画面によって
違う顔ぶれになる）。topbar から「相関図」を外したのは、**同じ辞書を別の見方で
出しているだけ**なのに別の場所に見えていたから。総称も苦しくなっていた ——
地図は「どこ」、年表は「いつ」で、まとめて「相関図」と呼ぶ根拠が無い。
**タブの名前が中身を言うので、総称はもう要らない。** 守ること 5 つ:

- **同じ画面で切り替わるものはボタン、画面をまたぐものはリンク。** 図から図を
  リンクにすると、タブを押すたびに `/api/graph` を引き直すことになる
  （「見せ方を変えるだけならサーバへ行き直さない」という約束が消える）。
  逆に一覧と図の行き来をボタンにすると、覆い (`overlay.js`) が拾えず URL も履歴も
  合わなくなる —— あちらは `<a href>` を見ている
- **一覧 → 図は、行く前に見せ方を覚えてから素の `/graph` へ行く。** `?mode=` を
  付けないこと —— あれは「リンクが名指ししてきた」印で、**覚えているほうを
  書き換えず注意書きを出す**という別の意味を持つ（自分でタブを押したのに
  「開いたリンクの指定で…にしています」と言われることになる）
- **topbar には「図」を残す**（`/graph`）。一度外して**タブからしか行けない形**に
  したら、**入口を見失われた**（実際に「リンクが表示できない」と報告が来た）。
  同じ場所であることは、着いた先のタブ列が示せば足りる ——
  `overlay.js` の `nav` も `/graph` に合わせること（合わないと何も光らない）
- **用語ページの中ではこの列を出さない**（`graph.mount(…, { tabs: false })`）。
  あちらは「その語の見方」の列を自分で持っているので、二重になる。
  出していないときは「上のタブで戻せます」の断りも出さない（戻す先が無い）
- **絞り込みはタブをまたいで持っていく**（`paintDictTabs(…, { scope })`）。
  「文学の一覧」から図へ渡ったら「文学の図」が出る。**`graph.js` は
  `paintGraph()` からも呼び直すこと** —— `mount()` の 1 回だけだと、カテゴリの
  選択肢がまだ読めていないので一覧へのリンクが素の `/glossary` になり、
  「文学の図から一覧へ戻ったのに全部出る」という食い違いになる
- **地図は列に並べない**（`OFF_TAB_MODES`）。列に並ぶのは**範囲が同じもの**だけで、
  地図が出すのは**絵 1 枚の上**（辞書に絵が何枚あっても 1 枚しか出せない）——
  用語ページの 🗺 と同じ範囲のものが、辞書ぜんぶの見方の顔をしていた。
  **`MODES` に戻さないこと。** 代わりに守るのが 4 つ:
  - **入口は 3 つ残す** —— 図の ⋯ の「🗺 地図」・用語ページの 🗺・`?mode=map`。
    **⋯ を消さない**（絵が 1 枚も無い辞書では用語ページの 🗺 も出ないので、
    ここが唯一の「最初の 1 枚を入れる」道になる。鶏と卵の対策はそのまま）
  - **`?mode=` と `pickMode()` は `ALL_MODES` で見る**（`MODES` で見ると
    用語ページの 🗺 が黙って段の図に落ちる）
  - **開いているあいだは列の末尾に出す**（`paintDictTabs` が `current` を足す）。
    出さないと選ばれたタブが 1 つも無い列になり、**どこに居るのかも戻り道も
    画面に出ない**
  - **見せ方としては覚えない**（`rememberMode()` が弾く）。覚えると、次に図を
    開いたときにどのタブも選ばれていない状態から始まる

**用語ページの見方も同じ形にしてある**（`entry.js` の `entryTabs()`。
辞書 / 関係 N / この語の図 / 🗺 地図）。**辞書の見方の列とは範囲が違う**ので、
別の列にしてある（あちらは辞書、こちらは 1 語）。守ること 4 つ:

- **関係の一覧と「この語の図」は隣に置く。** 同じものの並びと絵なので、
  片方を辞書タブの下にぶら下げると「別のもの」に見える
- **語についての操作（編集・⋯・保存先）はタブの外。** 中に入れると、図を出して
  いる間だけ編集できない、になる
- **広い範囲への出口を関係の下に置かない。** 関係は**その語から見た書き方**
  しかしていない（向きの基準は「自分から `to` を見て」に固定）ので、その並びの
  下にカテゴリ全体・辞書全体への出口があると層が飛ぶ。**広い範囲はパンくずの側**
  —— カテゴリを押せばその絞り込みの一覧が出て、そこのタブが絞り込みごと図へ渡る
  （以前ここにあった「カテゴリ全体の図 →」を消せたのはそれがあるから）
- **地図タブは `map:` を書いた語だけ。** 辞書の見方の 6 つは 1 つも隠さないが、
  こちらは違う —— あちらはどの辞書にも 6 通りあるのに対し、こちらの地図は
  **その語が絵に結びつけられているか**で、書いていなければ出すものが無い。
  **消えたタブに居座らせない**（`entryTab` を辞書へ戻す）
- **ここに辞書の見方（段の図・行列・年表）を並べない。** 図のうち「その語のもの」と
  言えるのは**中心の図と地図だけ**で、残りは辞書全体の見方 —— 並べると「この語の図」と
  言いながら辞書全体が出る。代わりに**その語を名指ししたまま渡す**
  （`[data-ref=toDictFigures]` → `/graph?ref=…`）。**図より前に置くこと** ——
  うしろだと背の高い図をスクロールし切らないと見えない
- **渡った先では名指しされた語に目印を付ける**（`spotlight()`）。地図と年表の 2 つ。
  **年表では周りを暗くしない**（`{ dim: false }`）—— 地図は「広い絵のどこか」を
  探す話なので落として構わないが、**年表で他の行を落とすと前後が読めなくなる**
  （その語がいつなのかは前後が見えて初めて分かる）。目印は
  `.rel-chronicle .rel-node.lit` の枠と地で見せる。行に `data-ref` が要る

**図の見せ方は 6 つある。足すものであって、置き換えではない。**
`graph.js` の段の図（既定）、`fabric.js` の交差しない図（用語が横線、関係が縦線で、
**関係 1 本ごとに独立した列**を持つ）、`matrix.js` の行列（行が「から」、列が「へ」）、
`ego.js` の中心の図（1 語のまわり **何つ先まで**か。既定は 2）、`timeline.js` の
時系列（上から順に**関係が読めるようになる順**）、`map.js` の地図（**形を書いた語を
絵の上に置く**。点 `pin` / 線 `line` / 領域 `area` の 3 つで、**内部は「点の連続 +
種別」1 つに畳む**。**これだけタブ列に無い** —— 範囲が絵 1 枚ぶんなので）。
分岐は `draw()` の 1 か所だけで、どれも
`{ root, box }` を返す —— 見せ方ごとに別の道を作らないこと（片方だけ拡大できない、
片方だけ伏せた本数を出さない、が起きる）。守ること:

- **形の規則は `graph-model.js` に置く**（`levelsOf` / `splitLonely` / `seedOrder`）。
  片方に写しを作ると、上下の段や「関係の無い語」の扱いが 2 つの見せ方でずれて、
  **同じ辞書なのに違うことを言う図**になる
- **交差しない図でも上下は保つ。** 行の並びは段が主で、上にあるものが上位。
  形を変えても言っていることを変えない
- **線の見た目・当たり判定・強調は同じクラスを使い回す**（`.rel-edge` `.rel-edge-hit`
  `.rel-edge-label` `.rel-node` と `.hot` / `.lit` / `.focusing`）。`fabric.js` 側の
  CSS に足すのは**形だけ**
- **一言は列の上に縦書き**（列幅 22px に横書きは入らない）。**`writing-mode`
  を使わないこと** —— SVG でも効くが、`⇄` が `⇅` に回される（`text-orientation:
  upright` を付けても Chrome では回った）。この辞書では `⇄` が「相互」、上下は
  `▲▼` なので、回った矢印は**別の意味に読める**。`<tspan>` を 1 字ずつ置き、
  長音符・括弧・ダッシュ・リーダだけ `rotate="90"` で寝かせる（`LAID_DOWN`）
- **空白を落としてから切る**（`verticalChars()`）。縦に積むと空白は空の 1 行に
  なるので落とすが、落とす前に数えるとその字数ぶん早く切れて、切る必要のない
  一言に「…」が付く。全文は線に乗せたときの吹き出しで読ませる（`<text>` の中に
  `<title>` を入れない）
- **関係の無い語は 1 語 1 行にしない。** 行にすると、関係を持たない語の数だけ
  図が縦に伸びる（31 語のうち 15 語が孤立していた実例では、関係のある部分より
  孤立語の並びのほうが背が高くなった）。段の図と同じく折り返す
- **見せ方は覚える**（`localStorage`）。覆いは何度でも開き直されるので、毎回
  選び直させない。読めない値は既定に落ちる
- **説明は `data-detail` で渡す**（→ 図の下の枠）。見せ方ごとに枠へ書きに行かない
  —— 入れ物 1 つに仕掛けて属性を拾うだけなので、見せ方を足せば勝手に効く
- **`TEMPLATE` にバッククォートを書かないこと**（コメントの中も）。JS の
  テンプレート文字列なので、そこで文字列が切れて続きが式として読まれる
  （`` `<title>` `` と書いて `title is not defined` で画面が真っ白になった）
- **URL から見せ方と絵を名指しできる**（`?mode=` / `?map=`。用語ページの
  「🗺 地図で見る」がそれ）。**`?ref=` の「中心の図で開く」より強い** —— 見せ方まで
  書いてある指定を負けさせると、押した先が別の図になる。**覚えているほうは
  書き換えず、押しのけたことを注意書きに出す**（`?ref=` と同じ約束）。地図では
  名指しされた語を光らせて枠へ入れる（`spotlight()`）—— 座標が与えられている図は
  中心の図のように組み替えられないので、**広い絵の隅に置かれた語を見失う**

**「ここまで読んだぶん」は単位を揃えずに作る**（`reading.js`）。読書位置は
`progress.js` が**描画ブロックの添字**、サーバは**文字位置**で、換算する道は無い ——
**近いはずの数字で埋めると「ここまで読んだ」が静かに嘘になる**（ネタバレ抑止に使う
値なので許されない）。代わりに**本文の自動リンクそのもの**を見る（`Linker` の出力
なので判断が二重にならない）。守ること 4 つ:

- **線は画面の下端**（`readBlock()`）。`progress.js` の `topBlockOf()`（再開する位置、
  上端）とは**別の問い**なので分けてある —— 上端で切ると、**画面に見えている語が
  図に出ない**
- **決めきれない語は入れず、数えて出す**（`data-count` が 2 以上のリンク）。入れる
  ほうへ倒すと、**まだ会っていない人物の関係が出る**
- **ビューアは値ではなく関数を渡す**（`provideReading()`）。覆いは何度でも開き直される
  ので、押した時点の位置が要る。登録者が居なければ**チェックごと出さない**
- **覚えない**（開くたびに全体へ戻す）。伏せたまま開き直すと「関係が消えた」と読まれる

**図の書き出しは `graph-export.js` の 1 か所**（見せ方ごとに書かない —— どれも
`{ root, box }` を返すので、入り口は 1 つで済む）。守ること 3 つ:

- **見た目は焼き込む**（`getComputedStyle()` → `style` 属性）。クラス名だけ持って
  出ると、外では `style.css` も CSS 変数も引けず**素の黒い線と黒い文字**になる。
  CSS を丸ごと貼る形にしないのは、**変数の解決とセレクタの取捨を再実装する**ことに
  なるため
- **外部の画像は data URI に畳む**（地図の絵）。`/api/map?...` のままでは
  **こちらのサーバが動いていないと出ない** —— 渡した相手には絶対に出ない
- **保存するのは図の全体**（`contentBox`）で、いまの拡大率ではない。拡大は見るための
  操作であって図の範囲ではない

**乗せたもの／焦点が当たったものの説明は、図の下の枠に出す**（`installDetail()`）。
**高さは常に同じ**にすること —— 出たり消えたりで下の凡例まで動くと読みにくい
（空のときは案内文を出す。空の帯だけだと何の場所なのか分からない）。
ブラウザの吹き出し（`<title>`）だけに頼らないこと:

- **キーボードの焦点では出ない。** これが無いと、キーボードだけで図を歩く人には
  一言が最後まで読めない
- **図の中では一言を切ったり畳んだりしている**（縦書きの 12 字、置き場所の無い
  一言）。**全文が読める場所がここしかない**
- 文の正は `base.js`（`describeRelation` / `describeNode` / `relationWords`）。
  5 つの見せ方で同じ文にするため、**各モジュールに写しを作らない**

**中心の図が出しているのは「近所」であって全部ではない**（`ego.js`）。守ること:

- **出していないものは全部数えて返す**（`note`）。選んだ深さより遠い語・相手が出て
  いない関係・多すぎて置けなかった語。**`draw()` はこれをそのまま凡例へ
  通すこと** —— 一度ここで落として、黙って欠けた図になった
- **既定の半径は 2。ただし選べる**（`DEPTHS` = 1〜4。タブの下の行で選び、覚える）。
  1 は用語ページの関係一覧とほぼ同じ、3 以上は密な辞書だと辞書全体に近づいて
  取り柄（規模に依らない）が薄れる —— **どちらも既定にはしない理由であって、
  選ばせない理由ではない**（「直接の関係だけ」も「もう一歩広く」も実際に要る）。
  守ること 4 つ:
  - **上限は環ごとではなく合計**（`MAX_FAR`）。環ごとに持つと、深さを増やしたぶん
    天井も増えて「規模に依らない」が効かなくなる
  - **1 つ先は切らない。** 直接の関係を落とすと、この図がいちばん答えるはずの問い
    （「この語は誰とどう関わるか」）が欠ける
  - **凡例は選んだ深さで言う**（`drawn.depth`）。「外側が 2 つ先」と決め打つと、
    3 つ先を選んだ人には嘘になる
  - **同じ語は最初に届いた環にだけ置く**（幅優先）。中心からの遠さがその語の環に
    なるので、あとの環で置き直すと近さが嘘になる
- **上下は `levelsOf()` から出す**（上は 12 時、下は 6 時、対等は 3 時と 9 時）。
  **空いた向きは隣の扇が引き取る** —— 4 分の 1 ずつ固定にすると、対等ばかりの
  辞書で左右だけが混んで上下が空く
- **まわりの語は link ではなくボタン**（押すと中心が移る）。link にすると、隣を
  覗くたびに図から出ることになる。辞書ページへ行くのは**中心の語**を押したとき
- **一言は中心につながる線にだけ出す。** 線は全部中心に集まるので、外周まで出すと
  重なって両方読めない。残りは畳む（本数は凡例に出す）。位置も真ん中ではなく
  6 割ほど外側 —— 根元に置くと一言どうしが重なる
- **`?ref=` で名指しされたら中心の図で開く**が、**覚えている見せ方は書き換えない**
  （押しのけたことは注意書きに出す。「文書が無いので段の図にした」と同じ扱い）

**時系列の位置は計算する。保存しない**（`timeline.py`）。「両端が出そろうところ」＝
遅いほうの語の初出で、`Linker.first_positions()`（当たり方は `entries_in()` と同じ）と
`Document.locate_at()`（章名 / `p.42` / `L.42`）から出す。守ること:

- **2 語が近くに並ぶ窓を探す形に変えないこと。** 窓の幅という決めようのない定数が
  要るうえ、「どの組にも必ず定義できる」が消える（並ばない組の位置が決まらない）。
  「近くに並ぶ場面」を探すのは `ai.cooccurrence_context()` の仕事で、目的が違う
- **保存しない。** 保存した瞬間に版管理と補正が要る（外のエディタで書き換えれば
  黙ってずれる）。行番号は epub の章名・pdf のページと**1 つの物差しにならない**
- **並べ替えは計算値、表示は人が書いた `reveal`。** 人の文字列で並べると
  「第6章」と「六章」が別物になる。`reveal` を計算した位置で上書きしないこと
- **軸は 2 つある**（読む順 = ここの計算値 / 作中の時刻 = 関係の `when`）。
  **読む順は `?doc=` のときだけ**で、作中の時刻は辞書全体でも定義できる。
  **どちらで並べているかを必ず画面に出し**、選んだ軸が使えないときは
  もう片方に落として**そう書く**（覚えているほうは書き換えない）。
  どちらも使えなければ**黙って別の図に差し替えず**、段の図に落として断りを出す
  —— そのとき**どちらの軸が足りないのかまで書く**（「文書を開け」だけだと、
  時刻を書くという道が見えない）
- **位置を出せなかった関係は落とさず数える**（`undated`）。`hidden` / `outside` と
  同じ約束
- **横の線は外形が高さ 0**。`.rel-edge-band` を敷かないと、押せるのに「大きさの
  無い部品」として扱われる（段の図の `hitBand()` と同じ話）

**行列は「無い」を見せるためにある**（`matrix.js`）。段の図も交差しない図も
書かれた関係しか描かないので、**まだ書いていない組は絵に出ない**。行列は空きマスと
して残るので、関係の抜けを探せる。**埋まったマスだけ置く形にしないこと** ——
格子を消した瞬間、この見せ方を足した意味が無くなる。守ること:

- **相互は対角の両側を埋める。** 関係はファイルには片側にしか書かれないが、
  片側だけ埋めると一方的に見える（画面に「両側が埋まっていれば相互」と書いてある）。
  2 つのマスは**同じ 1 つの部品**にする（どちらを押しても同じ編集が開く）
- **並びはカテゴリが主、次に段。** 関係は同じカテゴリに固まるので、まとめると
  埋まったマスが対角のまわりに寄り、離れたマスが「カテゴリをまたぐ関係」になる
  （NodeTrix の「かたまりごとの行列」に当たる）。カテゴリの鍵は `<scope><>カテゴリ`
  —— **名前だけで束ねない**（同名が全体とフォルダの両方にありうる）
- **行と列そのものを掴めるようにする**（`.mx-band`）。名前の上だけが反応すると、
  マスと名前を何度も往復することになる。**マスより先に置くこと** —— 透明な帯が
  上に乗ると、マスが押せなくなる。飾り（格子・対角・切れ目）は `pointer-events: none`
- **十字に塗るのは乗せた語だけ。** 相手の行と列まで塗ると、盤面の半分が色で
  埋まって「どこを見ればいいのか」が消える（相手は名前を明るくするだけ）

**地図の形は画面から組み立てられる。守るのは「宣言は人がする」と「戻せる」**
（`map.js` の `installHandles` / `installPlacing` / `fitToKind`、`graph.js` の
`mapUndo`）。守ること 8 つ:

- **戻す道を先に置く。** 掴んで離した時点で書くので、**消す操作を足すなら `mapUndo` が
  先**（無いと消した頂点は取り返せない）。**同じ語の連続した書き換えは 1 つにまとめる**
  —— 矢印キーは 1 押しごとに保存するので、積むと戻すを何十回も押すことになる
- **足す口は線分の中点に置く。** 形は `<a>` の中にあるので、線そのものに「押すと頂点が
  入る」を重ねると**線から辞書ページへ行けなくなる**。中点の丸は掴む丸より小さく薄く
  （同じ見た目だと本物の頂点と見分けが付かない）
- **線と領域は絵の上を順に押して作れる**（`installPlacing`。種別は**押す前に**
  `placeKindPicker` で宣言する）。**押した点の数から種別を決めないこと** ——
  3 点押したから領域、にはしない（足りないまま確定しようとしたら断る）。
  守るのは 3 つ: **下書きは押せなくする**（`pointer-events: none`。自分の点が
  次の点を遮る）、**押した先のリンクを開かせない**（`preventDefault`。既に置かれた
  語の上を押すと辞書ページへ飛んで置きかけが消える）、**確定とやめるを画面に出す**
  （Enter / Esc だけにしない —— 発見できないうえ、手だけで完結できない）
- **消す口は乗せたときだけ出す**（✕）。常に出すと頂点の数だけ並んで絵が読めず、
  **掴もうとして消す**。右クリックにしない（発見できず、ブラウザのメニューと取り合う）
- **最小を割る削除は断る**（`LEAST`。線 2 / 領域 3）。サーバ側は割れた形を**丸ごと
  空にする**ので、通すと「1 つ消したら地図から消えた」になる。**種別を勝手に落とさない**
  —— 種別は人が宣言するもので、消した結果そうなるものではない（点に頂点を足して線に
  するのも同じ理由でやらない。種別を変えてから足す）
- **`put_map_shape()` の「残り 2 つを空にする」は `kinds` から導く。** 名前を並べ直すと
  必ずずれる —— 実際に線の項目が `path` → `line` に変わったあとも**ここだけ `path` を
  消していて**、線 → 点 に変えると線が残っていた（テストが pin → line しか見ていな
  かったので通っていた）
- **保存で描き直したら焦点を戻す**（`graph.js` の `focusedMapControl()` /
  `restoreMapFocus()`。控えるのは**保存の前**で、描き直したあとでは遅い）。
  保存が即時 ＝ **1 操作ごとに図が差し替わる**ので、戻さないと焦点が body へ落ち、
  **キーボードでは 2 回目以降が効かない**（実測: 矢印キーで 1 回しか動かせず、
  ＋ と Delete も 1 操作ごとに Tab で戻る必要があった）。マウスは 1 ドラッグ
  1 保存なので**気付けない** —— 画面はどちらでも正常に見える。手掛かりは
  頂点の丸の `data-ref` / `data-vertex`（**戻す先は同じ添字**で端に丸める。
  「消えたら先頭へ」にすると図の反対側へ飛ぶ）。同じ理由で ↩ と種別の選択も戻す
- **絵の外へ出させない**（`clampTo()`。掴んで動かす・矢印キー・置く の 3 か所とも）。
  外へ出た点は**描かれても画面に出ない** —— 点検は挙げてくれるが、**画面には何も
  出ない**ので開くまで気付けない。**上限の高さは絵が届いてから決まる**（縦横比は
  読み込むまで分からないので、`imgH` を書き換えて使う。決め打ちに戻さないこと）

**地図だけは配置を計算しない。だから重なったら畳むしかない**（`map.js`）。他の
5 つはラベルを空いている場所へ逃がせるが、**地図で動かせば別の場所を指してしまう**。
重ねると重なった 2 つとも読めないうえ**下の絵まで隠す**ので、後から来たほうを畳む
（`takeSpot()`。`.rel-map-name.tucked` / `.rel-edge-label.tucked`）。守ること 3 つ:

- **名前が先、一言は後、点はもっと先。** 名前はその語が何なのかを決めるもので、
  一言は関係の補足。**点そのものを先に取っておく** —— 名前の板が別の語の点を覆うと、
  その語が図から消えたのと同じことになる
- **並びを決め切る**（関係の多い語 → 上から左から / 一言は短い線から）。決めないと、
  開くたびに畳む語が入れ替わる
- **数は必ず出す**（名前は `note`、一言は `tucked` を返して凡例が出す）。**消しては
  いない**ので、乗せる・焦点が当たる・その語の関係を辿ると出る

**地図は時点で巻き戻せる**（`map.js` の `upTo`、`graph.js` の `timeSlider`）。
関係に書いた作中の時刻 (`when`) で絞ると、進軍路が順に伸びる。守ること 4 つ:

- **時刻の無い関係は常に出す。** 伏せる側に倒すと、**時刻を 1 つ書いただけで
  それ以外が全部消えた**ように見える（時刻の無い関係のほうが普通）。
  常に出していることも凡例に書く（書かないと「この時点にはまだ無いはず」と読まれる）
- **止まれるのは実際にある時点だけ**（この絵に出ている関係のもの）。図が変わらない
  目盛りを並べると、動かした人は壊れたと思う
- **覚えない。** 開くたびに全部へ戻す（「ここまで読んだぶん」と同じ約束）
- **スライダは一覧の行と別の入れ物に置く**（`#mapTimeBar`。顔ぶれが変わったときだけ
  作り直す）—— 描き直すたびに作り直すと、**掴んでいる最中に消えてドラッグが切れる**
  （保存のたびに焦点が飛んでいたのと同じ形）

**重なる形は色で分ける。色を決めるのは `map.js` の `shapeColors()` 1 か所**
（この地図の取り柄は**説ごとの経路が同じ絵に重なる**ことなのに、同色だと
どれがどの説かは一言のラベルでしか分からず、**その一言は重なれば畳まれる**）。
守ること 4 つ:

- **線と領域だけに振る。** 点は場所が 1 つなので取り違えようがなく、丸まで色が
  散ると図が騒がしくなるだけ（重なるのは線と領域）
- **色は `items` に載せて渡す**（一覧の色札は `graph.js` が作り直さない）。
  2 か所で作ると、**図と一覧で色が食い違って探せなくなる**
- **カテゴリごとの色にしない。** 諸説は同じカテゴリに並ぶので（`samples/戦国時代`
  の `説` がそれ）、**分けたいものがまとめて同じ色になる**
- **`--shape-color` で渡し、`fill` / `stroke` を直に書かない。** 直に書くとインラインが
  勝って**乗せたときの色が効かなくなる**。CSS 側は `var(--shape-color, var(--accent))`
  なので、色を振らない点は今までどおり。図の書き出しは `getComputedStyle` で
  解決済みの色を焼くので外でも出る

**相関図の配置に力学モデルを使わない**（`graph.js`）。カテゴリという階層があり、
`rank` で段が決まるので決定的に置ける。乱数も収束待ちも無いぶん、同じ辞書なら毎回
同じ絵になる。段が 1 枚しかないときだけ円に置く —— 横一列に並べると辺が全部
重なって読めない。`levelsOf()` の緩和はノード数で打ち切ってあるので、上下関係に
閉路があっても止まらなくはならない。段の**中**の並びだけは緩和を回して選ぶが
（下記）、回数は固定で乱数も時刻も使わないので、この性質は変わらない。

**関係が 1 本も書かれていない語を段に混ぜない**（`splitLonely()`）。混ぜると
**繋がっている語どうしを横へ押し広げるだけ**で、そのぶん線が図の端から端まで飛ぶ。
18 語 18 関係で図が破綻した実例は、**6 語が孤立していた**のが半分の原因だった
（最上段が 10 語になり、いちばん多く繋がっている語が右端へ追いやられていた）。
**消さずに区切り線の下の帯へ出す** —— `?doc=` の図は「この文書に出てくる語」を
出すと画面に書いてあるので、黙って落とすと嘘になる。段の外に置いたことは
区切り線・見出し・凡例の 3 か所に書く（書かないと「下にあるから下位」に見える）。
全部が孤立しているときは区切るものが無いので線も見出しも出さない。

**段の中の並びは緩和して「いちばん良かったもの」を採る**（`orderRows()`）。
1 回きりの下向き整列に戻さないこと —— **最上段には「前の段」が無い**ので、
そこだけ入力順のまま残る（実例では 6 本を持つ吾輩が右端に居座っていた）。
出発点を 2 つ（入力順と、多く繋がっているものからの幅優先）持たせているのも
同じ理由。読みにくさ (`badness`) は **交差 > 箱の貫通 > 総延長** の順に効かせる。
**行は中央で揃える** —— 左端揃えに戻すと、語数の少ない段が左に寄って線が全部斜めに走る。

**同じ段を結ぶ弧は上下へ振り分ける**（`arcSides()`）。片側へ寄せると、段をまたぐ線が
通っているまさにその帯へ重なる（「対等」の一群はまとめて 1 つの段に来るので、実例では
11 語の段に 15 本が折り重なり、反対側は空いていた）。いちばん外側の段だけは外へ逃がし、
挟まれた段は**長い弧から順に交互**に振る。**ラベルの候補も膨らんだ側から試すこと**
—— 上下同じ順で試すと、下へ逃がした弧のラベルだけが逃がしたかった側へ戻る。

**交差は消せない。追わないこと。** `rank` でノードが段に固定されている以上、
「交錯しない配置」（＝平面描画）は成立しない —— 層に固定した状態での交差最小化は
隣り合う 2 層だけでも NP 困難で、0 にできる保証が無い。密になる原因も並べ方ではなく
データの形（対等が多いと 1 段に集まる）。**力学モデルにも、広い段を 2 行や円に
崩す形にも逃げないこと**（→ docs/design-notes.md に理由と代償）。効くのは
「一度に全部を読ませない」側 ——

- **重なるくらいなら一言を畳む**（`placeLabels()` の `tucked`）。重ねて出すと
  重なった 2 つとも読めず、下の線まで隠す。**消しはしない** —— 本数を凡例に出し、
  線かその語に乗せれば出る（`.tucked.hot` / `.tucked.lit`）
- **1 つの語に乗せたら、その語の関係だけを濃く出す**（`installFocus()`）。
  listener は入れ物ではなく**ノードに付ける**（描き直しで一緒に捨てられる）

**ラベルの箱は実測より少し大きく見積もる**（`labelBox()`）。小さいと、置いたあとで
重なる（高さが 1.5px 足りないだけで、14px 差で並んだ 2 行が実際には触れていた）。

**ラベルは通し番号でずらさない**（`placeLabels()`）。線の上ならどこでもよいので、
ノードの箱と先に置いたラベルを避けられる場所を順に試す。**短い線から先に選ばせる**
（動かせる幅が狭いのはそちら）。どこも空いていなければ重なりの少ない場所に置く ——
**消さない**（黙って欠けたラベルは「一言が書かれていない関係」と見分けが付かない）。

**層は 3 枚。下から 線 → 一言 → ノード。** 辺ごとに線と一言をまとめて置く形に
戻さないこと —— **あとに描いた辺の線が前の辺の一言を横切り**、下になった文字は
縁取りごと消えて読めなくなる。ノードをいちばん上にするのは、逃がしきれなかった
一言が箱の名前を覆わないため。

**線と一言は一緒に光らせる**（`drawEdge()` の `light()`）。ラベルは空いている
ところへ逃がすので**線の真上とは限らず**、線だけ色が変わってもどの関係なのかが
分からない。層が分かれていて CSS の子孫セレクタが届かないので、`hot` を JS が
両方に付ける。**一言のほうに乗せても線が光る**（どちらから触っても同じ）。

**`<text>` の中に `<title>` を入れない。** 描かれないまま文字の内容として数えられ、
「一言」を読む側（スモークテストも含む）が別物を読む。

**当たり判定の帯は外形としても持たせる**（`hitBand()`）。**外形に線の太さは
入らない**ので、真横・真縦の辺は幅か高さが 0 になり、押せるのに「大きさの無い
部品」として扱われる（焦点の枠も、外形で見る道具も何も見つけられない）。
塗らない四角を 1 枚敷いて、押せる範囲そのものを外形にしてある
（**押しても拾わせないこと** —— 拾うと辺の外接矩形が丸ごと当たり判定になり、
隣の辺を覆う）。

**枠は中身から決める**（`boundsOf()`）。段の高さだけで作ると、弧・ラベル・帯の
見出しが枠の外で切れる（上に出る弧のぶんだけ足す形では、下へ逃がした弧と
左から伸びる見出しが漏れた）。

**拡大縮小と移動は `viewBox` を動かす。** CSS の `transform` に変えないこと ——
線もラベルも画像を引き伸ばしたようににじむ（SVG の意味が無い）。入れ物は
`height` を固定して `overflow: hidden`、svg は `width/height: 100%` で、
**中身の寸法を width / height に焼かない**（焼くと図が伸びたぶん入れ物ごと
横に広がり、元の横スクロールに戻る）。守ること:

- **掴みは動き出してから `setPointerCapture` する。** `pointerdown` で捕まえると、
  以後のポインタ事象（互換の `mouseup` を含む）が入れ物へ付け替えられ、
  **中の線やノードを押しても click がそこへ届かない** —— 線を押しても編集
  ダイアログが開かなくなる（実際に踏んだ。スモークテストが見張っている）
- **動かした直後のクリックは飲む**（`swallowClick`）。飲まないと、線の上で
  放しただけで編集ダイアログが開く。**必ず戻す**ので次のクリックは通る
- **「全体を出す」で拡大はしない**（`Math.min(…, 1)`）。1 語だけの図を入れ物
  いっぱいに引き伸ばすと、箱だけが画面を覆う
- **当たり判定は縮めない**（`.rel-edge-hit` の `vector-effect: non-scaling-stroke`）。
  縮小すると 14px が数 px になり、見えている線が押せなくなる
- **焦点が枠の外へ出たら動かす**（`bringIntoView`）。スクロールをやめたぶん、
  入れ物が勝手に送ってくれない —— Tab でノードや線をたどると見失う
- **`ResizeObserver` は開き直すたびに外す。** 覆いは何度でも開かれるので、
  外さないと開いた回数だけ増える（listener を片付けるのと同じ話）

**読書位置は「本文を描き替える前に」書き出す**（`progress.js` の `switchTo()`）。
描き替えたあとに呼ぶと、**新しい本文の位置を前の文書の鍵で保存する**。`setSource()` の
先頭で呼んでいるのはそのため。位置を段落の番号で持つのは、px だと窓の幅・文字サイズ・
表示オプションで動くから。

**鍵が作れない文書は覚えない**（`keyFor()` が `null` を返す）。ファイル選択・ドロップ・
貼り付けにはブラウザが絶対パスをくれないので、ファイル名しか手掛かりが無い。ここを
「ファイル名で覚える」に緩めると、**同名の別ファイルを同じ鍵で上書きする**。
初出ジャンプ (`?term=`) のときも戻さない —— 行き先が決まっているので、戻しても
すぐ上書きされ、「前回の続き」の案内だけが嘘になる。

**「どこまで」だけでなく「どれを」も覚える**（`viewer.js` の `rememberOpened()`）。
位置だけ覚えていても、相関図や辞書へ寄り道して戻ると**本文が案内文に戻る**ので、
読んでいたものを手で開き直すことになる。守るのは 3 つ:

- **覚えるのはフォルダの中のファイルだけ。** 貼り付け・ドロップ・URL は読み直す道が
  無いので、切り替えた時点で**捨てる**（残すと、次に開いたときに読んでいたものと
  違うものが出る）。鍵に root を含めるのは `progress.js` の `keyFor()` と同じ理由
- **開き直すのは一覧に出ているファイルだけ。** フォルダを切り替えたあとや外で
  消されたあとにそのまま `openContent` を呼ぶと、**ページを開いた瞬間にエラーが出る**
- **URL の `?open=` が最優先。** 覚えているものより、指定されたもの

**横断検索は索引を持たない**（`/api/content-search`）。索引にすると、外のエディタで
書き換えられたファイルとずれる。**打ち切りは必ず返すこと**（`files_truncated` /
`hits_truncated` / `skipped`）—— 黙って切ると「この語は無かった」と区別が付かない。
ヒットへ寄せるのは `[data-gloss]` ではなく**見えているテキスト**で探す（検索語は
辞書に無いことのほうが多い）。

**用語で探すとき（`?ref=`）は `Linker` の規則を使う。** 素の部分一致に戻すと
`API` が `rapid` に当たり、**リンクにならない語を「出てくる」と言う**ことになる。
`Linker.finditer()` が `annotate()` と同じ正規表現を素のテキストに当てる口。

**冊子 (`/api/booklet`) は zip とは別物。** zip は**データの持ち運び**、冊子は
**人に渡して読ませる 1 枚**。守ること 4 つ:

- **中身を作るのは `core.booklet` 1 か所。HTML はその Markdown を描いたもの**
  （2 通りに書くと片方だけ古くなる）
- **並びは五十音**（画面の束ね方と同じ規則）。カテゴリ順にしないのは、冊子は
  引くものだから。**リンクは張らない** —— 書き出し先（GitHub / エディタ / 印刷）で
  見出しのアンカーの作られ方が違うので、**どこかで必ず切れる**
- **索引は呼ぶ側が渡す**（`core` は本文の置き場所を知らない）。数える口は
  `_occurrence_index()` **1 つ** —— 2 か所で数えると画面と冊子で食い違う。
  **本文を読むので既定では入れない**（頼まれたときだけ）
- **ファイル名は ASCII だけ。** `Content-Disposition` は latin-1 しか通らないので、
  日本語を載せるとその場で落ちる（zip が `export_name()` でカテゴリ名を入れないのと
  同じ理由。**実際にここで踏んだ**）。中身の見出しは日本語のままでよい

**公開 (`publish.py`) は冊子とも zip とも別物。** zip は**データの持ち運び**、冊子は
**渡して読ませる 1 枚**、公開は**貼ったときに出る絵が付いた 1 枚**（`core` には置けない
—— 出力先を知る必要がある）。守ること 6 つ:

- **既定の出力先を持たない**（`config.publish_dir()` が `None` を返す）。勝手にどこかへ
  書くと、置いた覚えのないフォルダにファイルが増える
- **`og:image` は絶対 URL でなければ書かない。** 相対は無視されるので書いても出ない
  —— **ページは正しく出るのにカードだけ黙って出ない**ぶん、半端なタグのほうが原因を
  隠す。基準 URL が無いまま書いたことは `warnings` で必ず返す
- **URL には `?v=<中身の印>` を付ける。** X はカードを **URL ごとに**覚えていて、確実に
  更新させる手段が無い（旧 Card Validator は廃止）。中身から作るので、変わっていなければ
  URL も変わらない
- **入れる値は全部「名乗り」。** `card_stamp` はリクエスト本文から来るので形を照合し
  (`safe_stamp`)、`base_url` の関門は**読むところ**に置く（`config.clean_base_url()`）
  —— 書き込みの口だけで見ると、**環境変数と設定ファイルの 2 経路が素通りする**。属性へ
  入れる直前に `_attr()` を通し、**エスケープは 1 回だけ**（先にかけたものを渡すと二重）
- **PNG を作れるのはブラウザだけ**（サーバに画像ライブラリが無い）。`card.js` が
  `{ root, box }` を返し、`graph-export.figureBytes()` が既存の道でバイト列にする。
  **サーバ側にラスタライザを足さないこと** —— `glosspop.spec` とビルド確認が付いてくる
- **commit も push もしない。書くだけ**（更新が「隣に展開して起動してください」と言う
  だけで通してきた線と同じ）

**本文のページは「サーバが無くても読める」ことが値打ち。** 守ること 5 つ:

- **`popup.js` の写しを作らない。** 通信は `base.js` の `api()` 1 か所なので、そこだけ
  埋め込み辞書を返す実装に差し替える（吹き出しの辿る・戻る・複数件・位置は本物のまま）
- **辞書は属性に置く**（`data-dict`）。インラインの `<script>` に JSON を書くと、辞書の
  本文に `</script>` が現れただけで**そこでスクリプトが終わる** —— 人が書くものなので
  実際に起こる。属性なら HTML のエスケープ 1 つで済み、`_attr()` **1 か所**に任せられる
- **辞書ページへの `href` は外す**（`unlink()`）。**押せるのに飛び先が無い、が最悪** ——
  素のクリックは `popup.js` が止めるが、**Ctrl / 中クリックは別タブで開く**ので 404 に
  なる。`tabindex` に置き換えて、キーボードの焦点は残す
- **外へ出す前に落とす**（`STRIP_KEYS`）。保存先のフルパス・顔・画像の URL は、渡した
  相手には出ないどころか**こちらの環境が漏れる**。判明位置つきの関係も伏せる
- **見た目と仕掛けは `assets/` に 1 回だけ。** `style.css` は 82KB あるので、文書ごとに
  埋めると 20 文書で 2MB 近く重複する。**辞書だけ**がページごとに違う

**見出しは機械で作る (`core.headline`)。** カードの掴みを担っているのは**見出しが主張で
あること**で、実測では**絵を一切変えず**に見出しだけ名前 (`戦国時代`) → 主張
(`同じ日に、2 つの事件`) にしたら、目次に見えていた 1 枚が読ませるものになった。
守ること 4 つ:

- **言えなければ `None`。** 呼ぶ側は辞書の名前に落ちる（外れた主張は無いより悪い。
  読みや作中の時刻で「分からないものは空で返させる」と決めたのと同じ判断）
- **書かれている桁しか言わない。** 年しか書かれていないものを「同じ日」と言わない。
  「だいたい」の印が付いていれば「ころ」に落とす（`16世紀` を 1501 年ちょうどのように
  言わないため）
- **カテゴリ名は引用するだけで意味を読まない。** 揃っているときだけ借り、揃っていな
  ければ「語」。鍵は `<scope><>カテゴリ`（名前だけで束ねない）
- **数える口は `relations.link_counts()` 1 つ。** 見出しとカードの並びが同じ数を別々に
  出すと「12 本と書いてある語が先頭に来ない」が起きる。**判明位置つきは既定で数えない**
  （`build_graph(spoilers=False)` と同じ既定 —— カードのほうが人目に付くので、ここだけ
  緩めると伏せている意味が無い）

**カードに何語入るかは `core` で決めない。** 入る数は実際の字幅で決まるので、測れる側
（`card.js`）が切って**落とした数を返す**（`hidden` / `outside` / `tucked` と同じ約束で、
画面には「他 N 語」と出る）。ここに「23 語」のような定数を置くと、用語名の長い辞書で
**黙って溢れ**、短い辞書では無駄に余る。字幅は `estTextWidth` の見積もりではなく
`getComputedTextLength()` で**実測する**（そのために `host` は画面にある要素であること）。
**色は CSS 変数から引かない** —— 図はその場で見るのでテーマに従ってよいが、カードは
**外へ出て、見る人の設定とは無関係に表示される**ので、書き出した人のテーマで配られる絵が
変わってはいけない。

**索引 (`/api/occurrences`) も索引を持たない。** 横断検索とまったく同じ約束で、
開くたびに本文を読み直す（`read_cached()` が変わっていないものを読み飛ばす）。
守ること 4 つ:

- **1 文書 1 走査で全語ぶん取る**（`Linker.occurrences()`）。語ごとに読み直すと
  3000 語で本文を 3000 回読むことになる。**照合は `_hits()` 1 か所から**
  （`entries_in` / `first_positions` と同じ。別に書くと**リンクにならない語を
  索引に載せる**）
- **打ち切りは必ず返す**（`files_truncated` / `skipped`）。この索引の主眼は
  **1 度も出てこない語**なので、「読んでいないだけ」と混ざると**見る意味が消える**
  —— 打ち切ったときは画面にもそう書く
- **点検に混ぜない。** `doctor` は `core` にあって文書を読まないし、全体の辞書では
  「本文に出てこない」は正常（開いているフォルダと関係の無い語がある）。
  **警告ではなく情報**として索引に出す
- **1 語あたりの文書は切ってよいが、切った数を返す**（`more_files`）。
  長編 1 冊に何十回出る語で一覧が縦に伸びるのを防ぐ

**使用例に貼る文は文字数ではなく文の切れ目で採る**（`_sentence`）。抜粋
（`_snippet`）を貼ると「…」つきの半端な文が辞書に溜まる。

**目次は位置を作り直さない**（`viewer.js` の `locateSections`）。章の見出しは
`documents.py` がすでに本文へ入れている（epub は元の見出し / `第 N 章`、pdf は
`【p.42】`）ので、描き終わった本文を頭からなぞって段落に対応づける。アンカーを
埋める形に変えると、`htmlclean` が `id` を落とすところに手を入れることになる。
**探すときは前の節より後ろだけを見る** —— 章の名前は本文にも出てくるので、全体から
最初の一致を採ると手前へ飛ぶ。対応づけられなかった数は画面に出す。

**タグにマスターは無い。** `/api/tags` が使われているものを数え上げる。`categories.yaml`
に相当するものを作ると、用語 0 件のタグが残って掃除の口が要る。用語ページの `#タグ` は
`?tag=`（完全一致）へ飛ばすこと —— `?q=` に戻すと、**そのタグ名が本文に出るだけの語**
まで混ざる。

**読み上げは非同期の癖が 3 つある**（`speech.js`）。どれも実際に踏んだ:

- **`speechSynthesis.cancel()` は直前の発話の `onend` / `onerror` を後から飛ばす。**
  真偽値のフラグを cancel の直後に戻すと、遅れて来た古いイベントを正常終了と誤読して
  段落を二重に進める。発話ごとに世代（`seq`）を持たせて照合すること
- **`pause()` / `resume()` は同期的に `speechSynthesis.paused` を変えない。**
  呼んだ直後に読むと前の値が返り、ボタンの表示が 1 手遅れる。状態は自前で持つ
- **音声の一覧は遅れて届く。時間で諦めないこと。** `voiceschanged` を 1.5 秒だけ
  待って `getVoices()` を 1 回で確定させていたため、間に合わなかった環境で
  **そのページでは以後ずっと 0 件**になり、「この環境には読み上げに使える音声が
  ありません」しか出なくなっていた（実測: アプリの窓の Edge はボタンが出た時点で
  27 件、そのあと 326 件まで増える。**ローカルの日本語 4 件も最初の 1.5 秒には
  入っていない**）。`voiceschanged` を張りっぱなしにして、届くたびに読み直すこと

**オンラインの音声は既定で選ばない**（`localService === false`）。合成は提供元の
サーバでされる＝**読ませた本文がそこへ出ていく**ので、Gemini と同じく「利用者が
明示的に選んだときだけ」に揃える。守ること 3 つ:

- **一覧から落とさない。** 落とすと、ローカル音声の無い環境で読み上げが丸ごと
  使えなくなる。並びをローカル優先にして、既定の選択をローカルに限る形で担保する
- **選ばれていない状態を一覧の先頭で表さない**（「音声を選ぶ…」を挟む）。
  `voices[0]` に落とすと、選んだ覚えのないまま押した瞬間に本文が出ていく。
  `utter.voice` を空のままにするのも同じ（ブラウザの既定が使われる）
- **🌐 と注意書きを出す。** 外へ出るものは画面に書く（相関図の範囲と同じ約束）

なお「1 発話が 15 秒を超えると無音のまま止まる」という Chromium の既知の癖は
**この環境では再現しなかった**（106 文字 = 20.2 秒でも鳴り切った）。報告は多いので
`MAX_CHUNK` で文単位に切って渡してあるが、**そこを疑う前に「単に読み上げに時間が
かかっているだけ」を先に疑うこと**（20 秒かかる段落を 12 秒で「停止」と誤診した）。

**一覧のカードを `<a>` に戻さないこと**（`glossary.js` の `card()`）。**リンクの
中ではドラッグが「リンクを掴む」になり、文字を選べない** —— 一覧にだけ
「選択 → 辞書に登録」が無かったのはこれが理由で、`draggable="false"` を付けても
Chrome は選択させてくれない（実際に試した）。いまは見出しだけが本物のリンクで、
残りはクリックのハンドラで飛ばしている。**飛べることはスモークテストで見張る** ——
リンクを外した以上、壊れても HTML はそれらしく見えるので画面を開くまで気付けない。

**SVG の細い線はそのままでは押せない**（`graph.js`）。辺には透明な太い線
（`.rel-edge-hit`）を下に重ねて当たり判定にしてある。消すと「押しても何も起きない」
になり、HTML も JS も正しいので原因が見えない。

**関係の上下（`RANK_OPTIONS` / `RANK_MARK`）の正は `base.js`。** 用語ページ・関係の
下書き・相関図の 3 か所が同じ表示をする。値は `models.RANKS` と同じ文字列でないと
サーバが弾くので、各ファイルに写しを作らないこと。

**カテゴリ名を含む文字列を空白で割らない。** カテゴリ名には**空白を使える**ので、
`` `${scope} ${category}` `` を `split(" ")` で戻すと「銀河 鉄道」が切れる。相関図の
カテゴリ選択は `/`（カテゴリ名では禁止）で 1 回だけ割っている。同じ理由で、ref を
つなぐ鍵の区切りには `<>`（カテゴリ名でも slug でも弾かれる）を使う。
スモークテストが空白入りのカテゴリ名で見張っている。

**辞書の取り込みは 2 通り。どちらも消す前に必ず控えを取る**（`archive.py`）。
ここは**辞書を丸ごと消しうる唯一の経路**なので、控えを「先にバックアップして
ください」という注意書きに落とさないこと（`copy_data_root` も `move` も
`installer` も「元を消さない」で通してきたぶん、ここだけ手順に頼ると必ず抜ける）。
控えは書き出しと**同じ zip** なので、そのまま取り込み直せば戻る —— 別形式にしないこと。

（**1 件ずつ消す経路は `delete` と `merge` の 2 つ。** どちらも人が対象を名指しした
ときだけ動くので控えは取らないが、**何が消えるかを先に見せる**のが代わりの担保
—— 統合の下見が畳めない項目と参照元を全部並べるのはそのため。）

**併合 (`mode="merge"`) の衝突は「取り込む側が勝つ」の 1 行だけ。** ここを
`updated_at` の新しいほうに変えないこと —— **時計がずれた PC が 1 台あると静かに
古いほうが勝つ**（しかも勝ったほうが正だと思い込んだまま気付かない）。1 件ずつ
選ばせる形にも戻さないこと —— 統合 (`merge.py`) は 1 組 2 件だから成立するので
あって、100 語の衝突は人が捌けない。規則を 1 つに絞る代わりに、**控えを取り、
上書きした語を全部名前で返す**のが引き換え。

**実行の前に件数を出す**（`archive.plan()` / `POST /api/import-glossary/plan`）。
下見は数えるだけで控えも取らない。「入れ替わります」の一言だけで押させないこと。
名前の一覧は `MAX_REPORTED` で切るので、**切ったことは `truncated` で返す**。

入れ替えは**ディレクトリごと**（`glossary` → `glossary.replaced-<時刻>`、
`glossary.incoming` → `glossary` の 2 回の rename）。**併合でも同じ** ——
いまの辞書を作業用ディレクトリに複製してから上書きする。1 ファイルずつ書く形に
変えると、途中で失敗したときに**半分だけ新しい辞書**が残る。

**併合ではカテゴリマスターの手元の並びを保つ。** 丸ごと差し替えると、手元にしか
無いカテゴリが順序と説明だけ失う（ディレクトリは残るので次の `load()` で名前順に
復活し、**決めた並びが黙って崩れる**）。サブカテゴリだけは和集合 —— 値ではなく
「先出しの候補」なので、消して得るものが無い。

**取り込む zip の形は必ず確かめる**（`inspect()`）。`glossary/<カテゴリ>/<slug>.md`
の 2 段が無ければ通さない。緩めるとアプリ本体の zip を取り込ませたときに
**辞書が空で置き換わる**。展開先の外に出る要素は `installer.safe_members()` で弾く
（外から来た書庫をライブラリ任せにしない、は更新の zip と同じ規則）。

**含めるのは全体の辞書とカテゴリマスターと地図の絵だけ。** ローカル辞書と URL 辞書を
足すと、置き換えで消える範囲が広がる。増やすなら、消える範囲が広がることを UI に出すこと。

**地図の絵だけは「書き出しには入れるが、取り込みでは消さない」**（`_export_maps` /
`_write_maps`）。座標は用語のファイルにあるのに絵はディレクトリの外にあるので、
入れないと**渡した先で形だけ揃って地図が真っ白**になる。**それでも置き換えの対象には
しない** —— 「zip の中身そのものになる」は用語についての約束で、絵はエントリと違って
zip の正体を決めない（`inspect()` が数えるだけなのはそのため）。**「入っていない ＝
消してよい」に変えると、古い版が書き出した zip を取り込んだだけで絵が全部消える。**
守ること 4 つ:

- **通す拡張子の正は `models.MAP_SUFFIXES` 1 か所**（`store` と `archivefmt` が読む）。
  写しを作ると、拡張子を足したときに**手元では見えるのに zip から静かに落ちる**
- **書く直前に `store.map_path()` へ通す**（`map_members()` の 1 段 + 拡張子の判断に
  加えて、もう一度）。展開先は**配る口が中身を検査せずに返す**置き場所なので、
  zip の中の名前をそのまま繋がない
- **同じ名前で拡張子が違う絵は片付ける**（`clear_other_maps`）。残すと `map_file()` の
  探索順で決まる絵が出て「差し替えたのに変わらない」になる
- **全部書き出すときは絵も全部**（まだどの語も指していないものも）。控えは書き出しと
  同じ zip なので、落とすと「消える前の唯一の写し」が嘘になる。一部だけのときは
  選んだカテゴリの語が指している絵だけ（カテゴリマスターと同じ扱い）

**控えは中を見られて、1 件だけ戻せる**（`list_backups` / `backup_contents` /
`restore_entry`）。守ること:

- **控えの名前は外へ出さない。** 画面から来る文字列なので、組み立てた結果が
  控えの置き場所の中にあることを最後に確かめる（`_backup_path`）
- **戻すときは保存し直さない。** 控えは「消える前の写し」なので、ファイルの中身を
  そのまま書く。`store.save()` を通すと本文の整形や `updated_at` が変わる
- **上書きになるかを押す前に出す**（`here`）。1 件ずつ戻す経路では控えを取らない
  ので、代わりに先に見せる（削除・統合と同じ扱い）
- **古いものを自動で消す口は作らないこと。** 溜まるのは事実だが、控えは消える前の
  唯一の写しなので、勝手に捨てると約束のほうが壊れる。合計の大きさだけ出す
- **並べ替えは名前から作った鍵で**（`_backup_order`）。名前をそのまま並べると
  同じ秒の `-2` が先に来るし、ファイルの時刻は Windows では粒度が粗くて並ばない

**渡す範囲は書き出す側だけで決める**（`export_bytes(only=…)`）。**取り込む側に
「一部を取り込むモード」を足さないこと** —— 併合はもともと「入っているものを足して
上書きする」だけなので、中身が一部でもそのまま通る。足すと置き換え・併合との
組み合わせが増えて、どれを選ぶのか人に分からなくなる。守ること:

- **全部書き出すときはカテゴリマスターをファイルのまま入れる。** 控え
  (`write_backup`) もここを通るので、書式ごと元に戻せる形を崩さない。一部のときは
  選んだぶんを組み立て直す（書式の正は `categories.dumps()` 1 か所）
- **一部であることは manifest にも残す**（`partial` / `categories`）
- **行き先が外に出る関係を先に数える**（`export_plan()`）。関係は名前で書くので、
  相手が入っていない zip を渡すと**保存はできるがリンクにも図の辺にもならない**。
  数えるのは解決できている関係だけ —— 壊れている参照は点検の担当で、二重に出さない
- **カテゴリ名をファイル名に入れない。** 空白も日本語も使えるので、
  `Content-Disposition` に載せると経路ごとに化ける（`-part-` とだけ書く）

**新しい版で自分自身を置き換えない**（`installer.py`）。動いている exe は差し替え
られず、署名なしバイナリの自己書き換えはウイルス対策ソフトに嫌われ、`Program Files`
では昇格も要る。隣に展開して「そちらを起動してください」と言うだけにしてある
（旧フォルダがそのまま戻り先になる）。**ここを「自動で入れ替える」に変えないこと。**

**外から来た zip をライブラリ任せに展開しない。** `safe_members()` が絶対パス・`..`・
シンボリックリンク・展開先の外に出る要素を自分で弾く。展開先の名前もタグをそのまま
使わず、組み立てた結果がアプリと同じ階層に収まっていることを確かめる。途中で失敗
したら書きかけを消す —— **半端なフォルダを残すと、それを起動されかねない。**

**更新でいちばん怖いのは「辞書が消えた」に見えること。** 新しい版を隣に展開して
既定のまま起動すると、データは旧フォルダに残ったままになる。`find_data_candidates()`
が隣を見て候補を返し、⚙ の先頭に出す。**いま中身があるときは黙る**（普通に使って
いる人に余計なものを出さない）。

**引き継ぎも保存先の移動も元を消さない。** 移した先で問題が出たときに戻れるように
するため。旧フォルダをどうするかは案内するだけで、こちらでは片付けない。

**テーマのダークの値は 2 か所にある**（`style.css`）。CSS にブロックを再利用する
手段が無いので、`@media (prefers-color-scheme: dark)` 側と `:root[data-theme="dark"]`
側に同じ内容を書いている。**片方だけ直すと「OS がダークのときだけ色が違う」**という
気づきにくい壊れ方をする。スモークテストが両方を実際に描かせて突き合わせている。

**テーマと文字の大きさは描画の前に当てる。** 各 HTML の `<head>` にインライン
script があり、localStorage を読んで `data-theme` と `data-fontsize` を立てる。
モジュールの読み込みを待って当てると、暗いテーマのときページを開くたびに一瞬白く
光り、大きさを変えていると一度小さい字で描いてから飛ぶ。キー名も値も `base.js` の
`THEME_KEY` / `FONT_KEY` と同じものを書いてあるので、変えるときは両方直すこと。
**写しは 6 つの HTML 全部にある** —— 1 つ直し忘れると、そのページだけ効かない
（スモークテストが開き直して見ている）。

**文字の大きさの正は `--fs-base` 1 つ**（`style.css` の `:root`）。ほかの字は
`calc(var(--fs-base) * N / 15)` で作ってあり、名前の数字は**既定 (15px) での
ピクセル数**。**生の px を書かないこと** —— その 1 か所だけ設定に付いてこず、
「本文は大きくなったのに注記だけ小さいまま」になる（画面を開くまで分からない）。
新しい大きさが要るときは `:root` に 1 行足してから使う。

**ただし相関図の SVG は付いてこない**（`.rel-node text` などの `font:` 指定は
生の px のまま）。箱の大きさもラベルの置き場所も JS が計算していて、その見積もりが
**この数字を知らない**（`base.js` の `estTextWidth` の既定 11、`svgVerticalText` の
`lineHeight`、`graph.js` の `labelBox`）。片方だけ大きくすると、避けたはずの
ラベルが重なり、縦書きの列から文字がはみ出す —— しかも**図は描けてしまう**。
大きく見せたいなら図の拡大縮小（`viewBox`）で、そちらは線もラベルも一緒に動く。
var 化するなら**先に JS 側の見積もりへ同じ値を渡すこと**。

**ダイアログの listener は最初の `await` より前に付ける。** ダイアログは開いた瞬間
から操作できるのに、読み込みを待ってから付けると**その間の操作が黙って無視される**
（設定のテーマを選んでも何も起きなかった）。`extract.js` が close イベントを
取り逃がした件と同じ形で、これで 2 回目。

**そのうえで、読み込みが届いたら描き直す。** 先に開くだけにして描き直さないと、
**読み込みの前に開かれたときに空のまま固まる**（カテゴリ管理が
「カテゴリがまだありません」で止まっていた）。開いた時点で持っているものを描き、
`await ready` のあともう一度描くのが型（`glossary.js` の `#manageCats`）。
**手元では一瞬で返るので絶対に踏まず、負荷の高い CI でだけ「要素が 15 秒
出てこない」になる** —— スモークテストが応答を止めて再現している。

**外へ通信するのは `updates.py` と、Gemini を選んだときの `llm.py` だけ。**
`llm` のほうは**利用者が明示的に選んだときしか動かない**（既定は Claude Code CLI で、
外に出るのは CLI 側）。増やすときは、切れること（`GLOSSPOP_UPDATE_CHECK=0` と ⚙）と、
**lifespan で叩かないこと**を守る。lifespan に置くと起動のたびに勝手に出ていくうえ、
`TestClient` が lifespan を走らせるので**テストが外へ通信する**。`/api/update` を
叩かれたときだけ動く形にしてある。確認の間隔（1 日）は設定ファイルの時刻で持つので、
再起動を繰り返しても増えない。失敗は**覚えない** —— 覚えると次に開いても再試行しなくなる。

**テストは実ネットワークを塞いである**（conftest の `forbid_real_network`）。
塞ぐのは実 transport だけで、`TestClient` の `ASGITransport` には影響しない。
`httpx.Client.send` ごと潰すと TestClient も死ぬので、そこを塞ぎ直さないこと。

**バージョン比較は読めないものを「新しくない」に倒す。** 読めない文字列で
「更新があります」と出すほうが害が大きい（ユーザーは確かめようがない）。

**`.rel-sub` を見出しに使い回さない。** `text-transform: uppercase` が付いているので、
ファイル名に当てると `銀河.md` が `銀河.MD` になる（実際に踏んだ）。日本語しか入らない
小見出し専用。

**ボタンの文字は決して折り返さない。** `button, .btn`（と見た目がボタンの
`.topnav a` `.chip`）に `white-space: nowrap` と `flex-shrink: 0` を当ててある。
縦に割れたラベル（「保 / 存」）は読めず、押せる範囲も分からなくなる。**ボタンごとに
nowrap を書き足さないこと** —— 決めているのは 1 か所で、以前は `dialog.sheet footer`
などに個別に書いてあった（書き忘れた新しい UI で毎回踏む）。

代わりに**幅は周りで吸収する**。踏みやすいのは 4 つ:

- **狭いときは行のほうを折り返す**（`flex-wrap: wrap`）。ダイアログの footer と
  `.cat-row` `.setting-row` がそう。**ただし `.check-row` は例外** —— `.wide` が
  幅 100% なので、wrap にすると横の小さなボタン（お気に入りの ☆）が必ず次の行へ落ちる。
  ここは入力欄と `.wide` が縮んで吸収する（`.wide` だけ `flex-shrink` を戻してある）
- **隣に基準幅を持たせる**（`.cat-row-main` の `flex: 1 1 140px`）。`flex: 1`（基準 0）
  だと、ボタンが縮まないぶん**隣が 0 まで潰れて 1 文字ずつ縦に割れる**
- **長い名前が入るボタンは `…` で切る**（`.filelist button` `.merge-cand`）。ellipsis は
  ブロックにしか効かないので `display: block` にすること（既定は `inline-flex`）。
  入れ物が grid なら列を `minmax(0, 1fr)` にする —— 既定の `auto` は min-content 以上に
  伸びるので、切られずに**横スクロールが出る**
- **`.filelist` の中に小さなボタンを並べるときは `width: auto` に戻す。**
  `.filelist button` は `width: 100%` なので、そのままだと隣を 0 幅まで潰す
  （ボタンは `flex-shrink: 0` で縮まないので、**潰れるのは必ず隣のほう**）。
  実際に、控えの一覧が日付ではなく「2」1 文字だけの幅になった
- **topbar は幅が足りないと溢れる。** 折り返さない＝縮まないので、`@media` で間隔を
  詰め、無くても困らないもの（⚙ の文字・語数）を落としてある

折り返してよいのは**本文の抜粋**だけ（`.search-results .filelist button`）。ラベルではなく
中身なので 1 行に切ると何にヒットしたのか分からなくなる。**ラベルを持つボタンに
広げないこと。**

**`node.hidden = true` はそれだけでは効かない。** `button` などに `display` を
指定しているので UA スタイルの `[hidden]` が負ける。`style.css` の先頭近くで
`[hidden] { display: none !important; }` を当てて打ち消している。**要素ごとに
`.foo[hidden] { display: none }` を足す書き方を増やさないこと**（この規則より前は
そうなっていて、新しい UI を足すたびに「hidden にしたのに出たまま」を踏む）。

**設定はタブで分けてある**（`settings.js` の `showTab`）。**フッタの「保存」は
データの保存先のもの**なので、AI タブでは出さない —— 出しっぱなしにすると、AI を
変えたあとにこちらを押して「保存したのに効かない」になる（AI 側は自前の保存ボタンを
持ち、押した時点で効く）。タブの色は**既存の変数だけで作る** —— 新しい色を足すと、
ダークの定義が 2 か所あるぶん片方だけ直す事故が起きる。

**`prompt()` / `alert()` を入力 UI に使わない。** 一度使って作り直した。値の入力は
インラインの `<input>`/`<select>` にする（`confirm()` は削除確認にだけ使っている）。

## テスト

`tests/conftest.py` の autouse fixture が `config.GLOSSARY_DIR` などを tmp_path に
差し替える。**`store._ready` のリセットも fixture がやっている** ので、
`store.ensure_ready()` の初期化をテストで当てにするなら fixture を確認すること。

**`claude` の有無をテストの前提にしない。** 手元は PATH に `claude` があるので
`ai.available()` が真になり、AI 経路のエンドポイントは 503 を返さず先まで進む。
CI には無いので 503 で止まり、**手元で通ったテストだけが向こうで落ちる**
（実際にリリースのワークフローで踏んだ）。AI 経路を叩くテストでは
`monkeypatch.setattr(ai, "available", lambda: True)` を明示すること。
手元で CI と同じ状況を作るなら、`GLOSSPOP_CLAUDE_BIN` を空にして PATH からも外す。

**タグを打つ前に `.\check.cmd ci` を通す**（`packaging/check-ci.ps1`）。手元では
通って CI で落ちる失敗を 2 回続けて踏み、そのたびに壊れたタグを消して付け直した。
見ているのはバージョンの一致、**`claude` を PATH から外した**テスト、時間に依存する
テストの繰り返し。**`uv` は絶対パスで呼ぶこと** —— `claude` と同じフォルダに入って
いることがあり、PATH から外すと `uv` まで消える（実際に踏んだ）。

繰り返しの掃き出しは**網であって証明ではない**。20 回に 1 回落ちるものは 40 回でも
1 割ほど取り逃がす。確実にしたいなら回数を増やすのではなく、テストを決定的に書く。

**時間で競走するテストを書かない。** 「別スレッドで遅れて listen させて、繋がるのを
待つ」形のテストが 20 回に 1 回落ち、**リリースのワークフローを止めた**。再試行の
回数だけを見れば十分なら、`create_connection` を差し替えて回数を数えること。
本物のソケットを使うのは、状態が最初から確定しているとき（既に listen 済み /
誰も listen していない）だけにする。

**フロントは `tests/test_smoke_ui.py` が通しで見張っている。** 本物のサーバを
別スレッドで立てて Chrome で操作し、「登録 → 本文でリンクになる → 吹き出し →
関係 → 図 → 点検」まで走る。HTML は正しいのに JS が落ちている、という壊れ方は
ここでしか捕まらない（実際に 3 件見つけた）。

- **手元の Chrome を使う**（`channel="chrome"`）。ブラウザを別途落とさせない
- Chrome も playwright も無ければ丸ごと skip する。**通常の `uv run pytest` を
  止めないこと**が条件
- ページの JS 例外は `pageerror` で拾って失敗させている。画面に出ない例外を通さない
- **本体が落ちたときの手掛かりは teardown で `print` する**（`_watch_page`）。
  JS の例外・console の error・4xx / 5xx と繋がらなかった要求を集めてある。
  **fixture はテスト本体の例外を受け取れない** —— pytest は例外を generator へ
  投げ込まず teardown を普通に進めるので、`except` で拾って `add_note` する手は
  効かない（`else` のほうが走る。実際に書いて確かめた）。失敗したテストの出力は
  pytest が「Captured stdout teardown」として出すので、そこへ流している。
  これが無いと「要素が 15 秒出てこない」だけが残り、**原因が消える**
- 要素は `data-ref` で掴む。ボタンの文言で掴むと、文言を変えるたびに落ちる
- **待つ対象を広く取らない。** `svg.rel-graph` の `textContent` にはノードの
  `<title>` に入れた要約まで混ざるので、「図に『級友』が出るまで待つ」が**最初から
  成立**して保存を待たずに検証していた（実際に踏んだ。テストは通るのに何も見ていない）。
  `.rel-edge-label` のように、変わる要素だけを見ること
- SVG の `<text>` は HTMLElement ではないので `inner_text()` が使えない。`text_content()`
- **一覧を待つのは `open_glossary()` 経由にする。** `.card` を直接待つと、読み込みが
  失敗して `.status.error` が出た場合も**素の 15 秒タイムアウト**になり、理由が残らない
  （v0.6.0 のリリースを止めた落ち方）。`#list` は読み込み中だけ `aria-busy` を持ち、
  終わると必ず何かを描くので、「busy でない かつ 中身がある」が「1 回描き終わった」の
  合図になる。**画面側にテスト用の目印は足していない**（本番の挙動をそのまま見る）
- **画面が間に合っていない状況は `sleep` ではなく「応答を止めて手で通す」で作る。**
  `sleep` で作った再現は、それ自体が次のフレークになる。止めるのはサーバ側
  （`store.category_tree` を `threading.Event` で待たせる）—— playwright の sync API
  では route ハンドラの中で寝ると `goto` ごと止まり、**そもそも再現しない**

スモークテストが届かないところ（AI を呼ぶ経路など）は、これまでどおり手で
Playwright を動かして確かめる。

## 変更時に一緒に直すもの

| 変えたもの | 一緒に直す |
| --- | --- |
| 辞書のスキーマ / CLI 引数 | `.claude/skills/gloss-add/SKILL.md` |
| 関係 (`relations`) の項目 | SKILL.md の「関係を書く」、MANUAL の「関係を図で見る」、`base.js` の `RANK_OPTIONS` / `RANK_MARK` と `describeRelation`、`graph.js` の辺のダイアログ（`openEdgeEditor` / `onEdgeSave`）、`entry.js` の `relationRow()` / `relationForm()`、**組み立て直している 2 か所**（`merge.py` の `_default_relations` / `merge.js` の `apply`）。**忘れるとまとめた瞬間だけ静かに消える** |
| 作中の時刻 (`when`) の書き方 | `core.whenfmt`（**読むのはここ 1 か所**）、`relations.relation_when()`（**継ぎ方の正**）、`ai` の `_WHEN_RULES` と `filter_when()`（**AI の申告を削る口**）、`doctor` の `unreadable_when`（語と関係の**両方**）、`timeline.js` の `AXES`、MANUAL の「作中の時刻を書く」と点検の表、SKILL.md の表 |
| エントリの項目（フォームに出さないもの） | `editor.js` の `KEPT_FIELDS`。**`PUT /api/entries/{ref}` は丸ごと差し替える**ので、載せ忘れた項目は編集ダイアログを開いて更新しただけで静かに消える |
| 抽出の種別 (`EXTRACT_KINDS`) | MANUAL の「まとめて登録する」の表 |
| 統合で選ばせる項目 (`merge.CONFLICT_FIELDS`) | `merge.js` の `FIELD_LABELS`（載っていない項目は英語の名前のまま画面に出る） |
| 設定に足した項目 | MANUAL の「設定」、`settings.js` の `PATH_LABELS`（場所を足したとき） |
| 描画前に当てる設定（テーマ・文字の大きさ） | `base.js` のキーと値、**6 つの HTML の head のインライン script**（写しなので全部） |
| 文字の大きさ（`--fs-base` と段階） | `style.css` の `:root`、`settings.js` の `<option>`、MANUAL の「文字の大きさ」の表。**周りの px は触らない** |
| 一覧の束ね方を足した | `glossary.js` の `<option>`・`paint()` の分岐・**覚えている値の検証**（読めない値は既定に落とす）、MANUAL の「束ね方は 3 つ」の表、CLAUDE.md のこの節、`tests/test_smoke_ui.py` |
| ビューアの「読むもの」バー / 左パネルの構成 | MANUAL の「読むものを選ぶ場所（上のバー）と、左パネル」（開き方の一覧・横断検索・フォルダを開く の各節も参照している）、`tests/test_smoke_ui.py` の `open_other_source()` / `open_content_search()`、`style.css` の `body.overlaid`（`.layout` の外に出したものは自分で書く） |
| 画面に操作を足した | **まず「操作の置き場所は 5 つしかない」の表**（行に直接足す前に）。⋯ に入れるなら `base.js` の `menuButton()` を使う（画面ごとに書かない）、MANUAL の該当画面の節、`tests/test_smoke_ui.py`（`data-ref` で掴んでいる） |
| ペルソナの顔 (`config.PERSONA_*` / `core.imagefmt`) | MANUAL の「語り手の顔」、`samples/README.md`、`docs/voices.md`、`ai-style.js` の `accept`。**画像を配る口は `GET /api/persona`、受け取る口は `POST /api/persona` の 1 つずつだけ** |
| 文体 (`ai.STYLE_PRESETS` / 効く範囲 / 置き場所) | `ai-style.js`（画面は ⚙ の AI タブ 1 か所）、MANUAL の「文体（口調）」の 2 つの表、`docs/voices.md`（効く範囲の表を載せている）。**例は `ai.py` が正**（`settings.js` に写しを置かない） |
| AI の提供元・モデル・思考の深さ | `llm.py`（`PROVIDERS` / `EFFORTS` / `CLAUDE_MODELS`）、MANUAL の「使う AI」と環境変数の表、`settings.js` の「AI」節、README の「AI について」 |
| 更新まわりの挙動 | MANUAL の「更新のしかた」、`packaging/release-intro.md` の「更新するとき」 |
| 自動リンクの規則 | MANUAL.md の「自動リンクの規則」、`content/ようこそ.md`。**当てない表記 (`excludes`) を触るなら** `core.linker` の 2 周目・`finditer()`・`models.exclusions`・`entryfile.FM_KEYS`・`editor.js` の `KEPT_FIELDS`・`merge` の `UNION_FIELDS` と `merge.js` の並べ書き |
| カテゴリ名の制約 | `models.normalize_category()`、MANUAL、SKILL.md、`ai.build_prompt()` |
| 画面 (`glossary` / `entry` / `graph` / `doctor` / `occurrences`) の中身 | そのモジュールの `TEMPLATE`（**HTML 側には写しを置かない**）。新しい画面を足したら `overlay.js` の `ROUTES` と、殻の HTML（head のインライン script の写しも） |
| 読書位置の扱い | `progress.js`（再開する位置）と `reading.js`（読んだところ）は**別の問い**。混ぜないこと |
| 図の形の規則（段・孤立語・並び・帯の折り返し） | `graph-model.js`（**1 つの見せ方だけに写しを作らない** —— `graph.js` / `fabric.js` / `matrix.js` / `ego.js` / `timeline.js` が同じものを読む） |
| 地図の形 (`pin` / `line` / `area`) | `models.EntryBase` と `map_shape`、`entryfile.FM_KEYS`、`relations._node`、`app.put_map_shape` の `kinds`、`map.js` の `LEAST` / `KIND_WORDS` / `fitToKind`、`entry.js` の `mapLink()`、`doctor` の地図の 4 つ、MANUAL の「地図に置く」と点検の表、SKILL.md の表。**`at` と `path` は名前に使えない**（時系列の文字位置・`_entry_payload` の保存先とぶつかる） |
| 地図の絵の置き場所 / 配る口 | `core.imagefmt`（**画像の拡張子と見分け方と大きさの正**。`store` の `maps_dir` / `map_file` / `map_path` と `archivefmt.map_members` が読む）、`app.IMAGE_TYPES` と `_SVG_SIZED`、MANUAL の「地図に置く」。**配る口には `CSP: sandbox` と `nosniff` を付けたまま**にすること（SVG を通せる根拠がそこにしかない） |
| 受け入れる画像形式（`IMAGE_SUFFIXES` / `MAP_SUFFIXES`） | `core.imagefmt` の `sniff()` **と `size()` の両方**（片方だけ足すと、その形式の絵で点検が静かに緩む）、`tests/test_imagefmt.py` の「拡張子と読める形式を揃える」、`ai-style.js` の `accept`、`graph.js` の絵ダイアログの `accept` |
| zip に入れるもの（辞書・マスター・地図の絵・用語ごとの画像） | `core.archivefmt`（**GlossPopApp と共有する形**）、`archive` の `_export_maps` / `_export_images` / `_write_maps` / `_write_images`、`settings.js` の下見の文言、MANUAL の「辞書の書き出し / 取り込み」。**取り込みで消える範囲を広げないこと** |
| 用語ごとの画像 | `store` の `images_dir` / `image_file` / `image_path` / `list_images` / `move_image` / `delete_image`、`app` の `/api/entry-image` と `_image_url` / `_image_index` / `_graph_images`、`popup.js` の `faceOf()`、`glossary.js` の `card()`、`entry.js` の `imagePanel()`、`map.js` の `FACE_R`（地図の点）、MANUAL の「用語ごとの画像」 |
| 時系列の軸 / 並べるものを足した | `timeline.js` の `AXES` と `rows` の分岐、`graph.js` の `TIME_AXES` / `TIME_ROWS` と**覚えている値の検証**・`hasWhen()`・凡例と説明の 2 通り、MANUAL の「時系列」、`tests/test_smoke_ui.py` |
| 図の見せ方を足した | **`dict-tabs.js` の `MODES` / `MODE_WORDS` / `MODE_HINTS`**（タブの正。`glossary.js` と `graph.js` の両方のタブ列に出る。**タブ列に並べるかは範囲で決める** ——辞書ぜんぶでないものは `OFF_TAB_MODES` へ）、`graph.js` の `draw()` の分岐と凡例、MANUAL の「見方は 6 つある」の表、CLAUDE.md のこの節。**書き出し (`graph-export.js`) は触らなくてよい**（`{ root, box }` を返す限り） |
| 中心の図の深さ (`ego.js` の `DEPTHS`) | `graph.js` の `#egoDepth` と覚えている値の検証、`style.css` の `ego-ringN`（環が増えるなら）、MANUAL の「中心の図」の表、`tests/test_smoke_ui.py` |
| 用語ページの見方を足した | `entry.js` の `entryTabs()` / `tabSearch()` / `paintTabBody()` と `render()` の `bodies`、MANUAL の用語ページのタブの表、`tests/test_smoke_ui.py`（`data-tab` で掴んでいる）。**辞書の見方のタブ (`dict-tabs.js`) とは別の列**（範囲が違う —— あちらは辞書、こちらは 1 語） |
| 公開ページ / メタ画像 | `publish.py`（書き出しと安全規則）、`core.headline`（見出しの規則）、`core.card`（載せる中身）、`static/card.js`（絵。**入る数は測って決める**）、`static/publish.js`（押す口）、`settings.js` の「公開」タブ、MANUAL の「公開する」と環境変数の表、`tests/test_publish.py` と `tests/test_smoke_ui.py` |
| 依存の追加 / 動的 import・データファイルの追加 | `packaging/glosspop.spec`（ビルドして exe 起動まで確認） |
| exe のアイコン | `packaging/icons/*.svg` を直して `uv run python packaging/make-icons.py`。**`.ico` も一緒にコミットする**（ビルド時には作らない）。アプリ側は `static/favicon.svg` と揃える |
| 画面の見た目（ビューア・一覧・用語ページ・図・抽出ダイアログ） | `docs/images/` の該当スクショ（README と MANUAL が貼っている。撮り直しは content/ を開いて 1280×820 のライトで撮る） |
| 辞書のスキーマ / 文体 / 点検の判定 | `samples/` は**動く実物**として公開しているので、`tests/test_samples.py` が落ちていないか見る（落ちたらサンプルを直す。README の「置いて開くだけ」が嘘になる） |
| 機能を足した / 設計の判断をした | `docs/open-questions.md`（**片付いた宿題は消す** —— 残ると「まだ無いもの」に見える）、`docs/design-notes.md`（決めたことと、その代償を書く） |
| 使い方の説明を増やした | `MANUAL.md`（**README には移さない** —— README は入口だけに保つ。目次も一緒に直す） |
| ビルド・リリースの手順 | `DEVELOP.md` |
| バージョン | `pyproject.toml` と `glosspop/__init__.py` と `uv.lock` の**3 つとも**（前 2 つがタグと不一致だと release ワークフローが落ちる。lock は落ちないぶん気付けない）。MANUAL の版番号の例も見る（あそこに出るのは**次の版**）。**`CHANGELOG.md` にその版の節を書く** —— 無いとワークフローが落ちる |
| 画面の構造（`data-ref` / クラス名 / 一覧の作り） | `docs/video/*/plan.json` のセレクタ（**解説動画の台本**。落ちても静かに撮れてしまう —— `gmp record <plan.json>` を通すと分かる）、`.claude/skills/ghostplay/SKILL.md` のセレクタ表 |
