# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

使い方・データ形式・自動リンクの規則は [README.md](README.md) にある。ここには
**README を読んでも分からないこと**（設計の前提、壊しやすい不変条件、開発時の落とし穴）
だけを書く。

## コマンド

```powershell
uv sync --all-groups            # 依存を入れる (dev グループ含む)
uv run glosspop serve           # http://127.0.0.1:8765/
uv run pytest                   # 全テスト
uv run pytest tests/test_linker.py::test_longest_match_wins   # 単体
uv run pytest -k "カテゴリ or category"
```

`glosspop` CLI は辞書操作もできる（`add` / `list` / `show` / `move` / `rm` / `categories`）。
`.claude/skills/gloss-add/SKILL.md` がその使い方をスキルとして持っている。**辞書の
スキーマや CLI の引数を変えたら SKILL.md も直すこと。**

### サーバの止め方（Windows で必ず踏む）

`uv run glosspop serve` の親プロセス（`uv`）を kill しても、**子の python が生き残って
ポートを掴んだまま**になる。新しいサーバは bind に失敗して黙って死に、古いコードが
動き続けるので「直したのに反映されない」という誤診をする。ポートの所有者を落とすこと。

```powershell
Get-NetTCPConnection -LocalPort 8765 -State Listen |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

`uv sync` が `glosspop.exe` を消せずに失敗するときも、たいていサーバが動いている。

## アーキテクチャ

データの流れは一本道:

```
ソース (ファイル / URL / 貼り付け)
  → render.render_source()      Markdown・テキスト・HTML → HTML
  → Linker.annotate()           テキストにだけ <a class="gloss-link"> を差し込む
  → ブラウザ                     popup.js が data-gloss から辞書を引いて吹き出しを出す
```

`store` が辞書、`categories` がカテゴリマスター、`ai` が Claude CLI 呼び出し。
`app.py` はこれらを繋ぐだけで、ロジックを持たない。

### 壊しやすい不変条件

**ディレクトリ名とファイル名が正。** エントリは `<辞書ルート>/<カテゴリ>/<slug>.md`。
`category` `slug` `scope` は frontmatter に書かない（書いても `_entry_from_file()` が
パスの値で上書きする）。一意キーは `Entry.ref` で、API も URL もこの文字列を使う。
**同じ用語名でもカテゴリが違えば別エントリ**なので、`find_by_surface()` はリストを返す。
単数を期待するコードを書かないこと。

**「関連語」は `relations` に一本化した。** 旧 `related`（向きも一言も無い名前の
並び）は `EntryBase` の before バリデータが `relations` に畳む。ファイル・API・CLI の
どこから来ても通るよう、**入り口 1 か所**でやっている。`_FM_KEYS` から外してあるので、
古いファイルは次に保存した時点で移行される（`migrate_layout()` と同じ考え方）。
2 か所に書けるまま残すと、どちらに書くか毎回迷ううえ、`related` 側は相関図に出ない。

**関係 (`relations`) に ID は無い。参照は名前で書く。** 揺れを吸収するのは
`relations.resolve()` **1 か所だけ**なので、ここを迂回して自前で照合を書かないこと。
吸収しているのは 3 つ:

- **正規化** — `normalize_link()` が NFC・空白・`/` の周りを潰す。「空白を足しただけで
  切れる」を防ぐのがここ
- **転送** — `move()` と `save()` がカテゴリ移動のとき旧 ref を `former_refs` に積む。
  **参照側は書き換えない**（wiki のリダイレクト）。全エントリを書き換えて回る実装に
  変えないこと —— 手で `mv` された経路が必ず漏れる
- **曖昧さ** — 同じ用語名がカテゴリ違いで併存できるので、書き手のカテゴリ →
  同じ辞書 → 全体、の順に絞る。**絞りきれないときは黙ってどれかに寄せない**
  （寄せた瞬間に相関図の辺と本文のリンク先が食い違う）

`former_refs` は**ファイルの値が正**。`save()` が下書きの値ではなく既存ファイルから
読み直しているのは、項目を省いた更新で転送情報が消えないため。

**向きの基準は「自分から `to` を見て」に固定してある。** `label` `back` `rank` の
全部が同じ向き。「相互 / 一方的」のフラグを別に持たないのは、`back` の有無が
それを決めるから。フラグを足すと二重に持つことになり、必ずずれる。

**重複の判定は ref で、プロンプトへの表示は用語名で。** `ai.existing_pairs()` が
用語名、`ai.existing_ref_pairs()` が ref を返すのはこのため。片方で兼ねようとして
用語名で照合したら、**「すでにある関係」を素通しして同じ組に 2 本目の辺が生えた**。
同名がカテゴリ違いで併存できる以上、名前は鍵にならない。

**関係の書き込みはエントリ単位でまとめる**（`/api/relations`）。1 本ずつ PUT すると、
同じエントリに複数の関係が付いたとき後の書き込みが前のものを消す。クライアントから
順に投げる形に戻さないこと。相関図から 1 本だけ直すときも同じで、そのエントリの
`relations` を作り直して丸ごと書く。

**辺から書き手の関係へたどる鍵は `index` と `rel_to` の 2 つ**（`build_graph`）。
`to` は解決後の ref なので、ファイルに書かれた文字列とは違いうる（用語名だけで
書ける仕様の裏返し）。`index` は **`enumerate(entry.relations)` から採る** ——
出した辺だけを数えると、判明位置つきを伏せたぶんずれて**別の関係を書き換える**。
書く直前に読み直して `rel_to` が一致するか確かめること（合わなければ書かない）。

**点検 (`doctor.py`) は正常なものを問題として出さない。** カテゴリ違いの同名は
この辞書の狙いどおりの機能だし、関係が 1 本も無いエントリも普通にある。ここが緩むと
警告が常時数十件出て、誰も見なくなる（＝本物の壊れが埋もれる）。挙げるのは
「直さないと読み手に見えないもの」「黙って壊れているもの」だけ。

**相関図では `reveal` つきの関係を既定で出さない**（`build_graph(spoilers=False)`）。
相関図は本文より先を一望させるので、ネタバレ抑止は本文生成 (`ai.py`) だけでは
足りない。いつ判明するかを機械で比べる手段が無いので、**判明位置が書かれているか**で
切る —— これが嘘をつかない唯一の線引き。伏せた本数は `hidden` で必ず返すこと
（黙って欠けた図を出さない）。

**辞書ルートは 2 つある。** グローバル (`data/glossary/`) と、開いているフォルダの
ローカル (`<フォルダ>/.glosspop/glossary/`)。`store.glossary_dir(scope)` を必ず通すこと。
ref はグローバルが `<カテゴリ>/<slug>`、ローカルが `.local/<カテゴリ>/<slug>`
（カテゴリ名は「.」で始められないので衝突しない）。**グローバル側の形を変えていない**ので、
既存の URL・CLI・スキルはそのまま通る。

ローカル辞書まわりで踏みやすいのは 3 つ:

- `_signature()` に**ローカルのルートパス自体**を入れてある。入れないと、フォルダを
  切り替えてもキャッシュが効いて前のフォルダの辞書が出てくる
- `find_in_category()` はスコープも見る。見ないと「全体の辞書に同名があるので
  このフォルダに登録できない」という誤った衝突になる
- **カテゴリマスターはグローバルのもの。** ローカル保存では `categories.ensure()` を
  呼ばない（小説の「登場人物」がマスターに残ると邪魔になる）。`category_tree()` は
  ローカルぶんをディレクトリから作って `scope` 付きで足している

**カテゴリの改名・削除は必ずスコープを取る**（`rename_category` / `delete_category`）。
グローバル決め打ちだったため、**用語の入ったローカルのカテゴリが「空」と判定され、
同名のグローバル側が代わりに消えた**（空かどうかを `not e.is_local` で数えていた）。
数えるのは `e.scope == scope` の中だけ。ローカルはディレクトリが正なので、改名は
実体を動かすだけでよく、マスターには触らない。**API・CLI・UI の 3 つとも
`scope` を渡すこと** —— 渡さない経路が 1 つでも残ると同じ事故が起きる。

**画面でも 2 つの辞書を見分けられるようにする。** カテゴリ管理と絞り込みは
`📁` を付け、絞り込みの値は `<scope>/<カテゴリ>` にしてある（相関図と同じ形。
`/` はカテゴリ名で禁止なので 1 回だけ割れる）。名前だけを値にすると、同名が
両方にあるとき選択肢が見分けられない。

**CLI の `--folder` は `config.set_content_dir()` を呼ぶだけ。** 一度きりの CLI には
「開いているフォルダ」の状態が無いので、そこだけ先に決めてから既存の経路
（`store.glossary_dir(LOCAL_SCOPE)`）に流す。**CLI 用のローカル辞書探索を別に
書かないこと** —— 祖先方向の探索・段数の上限・URL 辞書との排他が 2 か所に分かれる。
辞書を読む前に反映する必要があるので `main()` の `ensure_ready()` より前で呼んでいる
（あとから差し替えると `store` のキャッシュが前のフォルダの辞書を掴んだままになる）。
書いた場所は `_announce_local()` が stderr に出す —— 祖先の辞書が使われることがあり、
`--folder` を省けば既定の content フォルダになるので、**黙って書かない**。

**Linker はレンダリング済み HTML を文字列として走査する。** DOM は作らない。
`_tokenize()` でタグとテキストに切り、`_runs()` で「読者に見えるテキスト」の
まとまりを作ってから照合する。ここが 2 つの規則を担っている:

- `SKIP_TAGS`（`pre` `a` `script` `style` `textarea`）の内側はリンクしない
- `INLINE_TAGS` の中だけテキストを連結する。ブロック要素と `<br>` は連結しない
  → `**冪**等` は繋がるが `<p>冪</p><p>等</p>` は繋がらない

**大文字小文字を無視するのは既定であって全部ではない。** `_case_sensitive()` が真に
なる表記（3 文字以下の全大文字 ASCII）は `(?-i:…)` で囲んで区別する。これが無いと
別名 `MD` が `README.md` の拡張子に当たった —— 境界チェックは英数字以外を通すので
`.` が境界になる。**ここを「全部無視」に戻さないこと**（`ML` `DB` を登録するたびに
同じ壊れ方をする）。上限 `_ACRONYM_MAX_LEN` を外すと `NASA` のような全大文字の
固有名詞まで区別する側に倒れるので、広げるなら実例が出てからにする。

**`htmlclean.VOID_TAGS` に void 要素を漏れなく入れること。** `DROP_TREES`（中身ごと
捨てるタグ）に void 要素が入っていると、閉じタグを待ち続けて**それ以降の本文が全部
消える**。`<input>` がそうで、**検索ボックスのあるページ（＝たいていの Web ページ）が
空になっていた**。URL 読み込みを触るときは、実際のサイトを 1 つ開いて本文が出ることを
確かめること（`example.com` のような単純なページでは気付けない）。

**`MAIN_CANDIDATES`（`main` / `article`）で本文を採るのは最初のひとつだけ。** 見つける
たびに `parts` を差し替える作りにすると、**2 つめの `<article>` が 1 つめを丸ごと
消す**。記事を `<article>` で並べるページ（ブログの一覧、章が並ぶページ）が該当する。
`_main_done` がその番人。これも「黙って本文が消える」系統なので、上の `<input>` と
同じく実際のページで確かめること。

`render.md_to_html()` は `html: False` で生 HTML を通さない。これは安全のためだけで
なく、**Linker のタグ走査が想定外の構造を踏まないため**でもある。外部 HTML を
入れる経路（URL 読み込み）は `htmlclean` の許可制サニタイザを必ず通す。

**辞書本文は保存時に 1 文 1 行へ整形される**（`render.soften_paragraphs()`）。
表示側は `breaks: True` の markdown インスタンスを使う。ソース文書用の `_md` とは
別インスタンスなので、片方だけ設定を変えないこと。

**開いているフォルダは `config.content_dir()` で引く。** ビューアから
`/api/content-root` で切り替えられるようになったので、`config.CONTENT_DIR` は
「既定値」でしかない。直接参照すると、切り替えたあとに一覧・読み出し・パス検査が
別のフォルダを見て、`_safe_content_path()` のディレクトリ脱出チェックが意味を失う。
上書きはプロセス内に残るので、**テストは conftest の fixture でリセットしている**。

任意のフォルダを開ける = 巨大なツリーを掴みうるということでもある。`_iter_content_files()`
が隠しディレクトリと `SKIP_DIRS` に降りず、`MAX_CONTENT_FILES` で打ち切るのはそのため。
打ち切ったことは `truncated` で UI に出す（黙って切らない）。

**凍結した exe の標準出力は UTF-8 ではない。** 日本語 Windows のコンソールでは
CP932 になる（開発時の `uv run` では UTF-8 なので、**この差は exe でしか表面化しない**）。
子プロセスとやりとりする値は必ずバイト列で受け渡すこと — `picker.py` は
`sys.stdout.buffer` へ UTF-8 で書き、親は `text=True` を使わずに自分で復号している。
これを怠って「日本語を含むフォルダを開けない」不具合を出した。CLI もパイプ越しのときだけ
UTF-8 に固定している（コンソールへ直接出すときに UTF-8 で書くと今度は表示が化ける）。

**固定するのは stdin も。** `_use_utf8_when_piped()` が stdout / stderr だけを直していた
ため、`echo '{"term":"冪等"}' | glosspop add --json -` の入力が CP932 で復号され、
**見出し語がサロゲートに化けたまま保存されていた**（`/gloss-add` スキルが使う経路そのもの）。
`uv run` でも凍結後でも起きる。ファイル入力 (`@path`) は最初から UTF-8 決め打ちなので、
`-` だけが穴だった。

**「いま読んでいるもの」はフォルダか URL のどちらか一方。** `config.set_reading_url()`
が立っていれば `sites/` の下を最長一致で探し、そうでなければフォルダを祖先方向に探す。
併用しないのは、小説フォルダを開いたまま Web を読んで登場人物名が無関係なページで
リンクになるのを防ぐため。**URL 側は辞書を勝手に作らない**ので
`local_glossary_dir()` は `None` を返しうる —— `store.glossary_dir()` と
`local_available()` はそれを前提にしている。

**URL のパスをそのままディレクトリ名にしない**（`sites.py`）。`..`・`:`・クエリ・
Windows の禁止文字を潰したうえで、組み立てた結果が `SITES_DIR` の外に出ていないことを
最後に必ず検査する。ホストらしくない入力（空白入り、ドットもポートも無い）は弾く。

**ローカル辞書は祖先方向に探す**（`config.local_root()`）。開いているフォルダから
いちばん近い `.glosspop` を使い、無ければ開いているフォルダに作る。1 巻 2 巻を別
フォルダにしていても作品フォルダに 1 つ置けば共有できる。遠い祖先の辞書が黙って
繋がると驚くので、**親を使っているときは UI にその場所を出す**
（`/api/content` の `local_dir` / `local_is_ancestor`）。

**アプリモードで開いた `msedge.exe` の寿命は当てにできない**（`appwindow.py`）。
`--app=<URL>` で起動したプロセスはブラウザ本体を別プロセスで生んですぐ終了するので、
`Popen.wait()` は「窓が閉じた」ことにならない。これを使って「窓を閉じたらサーバも
止める」を書いたら、**窓は開いたままサーバだけ落ちた**。窓の寿命に紐づけたいなら
プロセスではなくページ側からの生存確認（heartbeat）でやること。専用プロファイルを
渡しているのは、普段のブラウザのセッションと混ぜないため（localStorage も別勘定になる）。

**フォルダ選択ダイアログは必ず別プロセスで開く**（`picker.py`）。tkinter はメイン
スレッドでしか安全に動かず、FastAPI のワーカースレッドから呼ぶと固まる。凍結した
exe には python インタプリタが無いので、自分自身を隠しコマンド `__pick-folder` で
再実行している（`cli.main()` の先頭で引数を横取りしている）。**`glosspop.spec` の
`excludes` に tkinter を戻さないこと** — 戻すと exe 版だけダイアログが開かなくなる。

**ファイルを読むのは `documents.py` の仕事。** `path.read_text()` を直接呼ばないこと。
吸収しているのは 3 つ:

- **文字コード** — UTF-8 → cp932 → euc_jp。青空文庫は Shift_JIS で、UTF-8 決め打ち
  （`errors="replace"`）だと化けたまま「読めてしまう」ので気付きにくい
- **改行** — バイト列から decode すると CRLF が素通りする。`\n\s*\n` で段落を
  数えている箇所（ネタバレ抑止）が静かに壊れるので `decode()` で LF に揃えている
- **位置** — epub は章名、pdf はページ番号、それ以外は行番号。`Document.locate()`

**epub の manifest の href は percent-encoded。** OPF の href は IRI なので、日本語や
スペースを含むファイル名は `%E7%AC%AC...` で入っている。`zipfile` は生の名前しか
受けないので、復号せずに読むと `KeyError` になり**その章だけ黙って消える**
（`_read_member()` が両方を試す）。章名が英数字の epub では気付けない。

**青空文庫の正規表現は行単位で書く。** 冒頭の説明ブロックを `(?:.*\n){0,6}?...` と
DOTALL で書いたら 4 万字の作品でハングした（`.` が行をまたいで際限なく戻る）。
`[^\n]*` を使い、走査範囲も先頭 3000 字に限ること。

**ルビは `rt` / `rp` を中身ごと捨てる**（`htmlclean.DROP_TREES`）。タグだけ落とすと
読みが地の文に残り、`銀河ぎんが` になって照合が壊れる。親字側の `ruby` / `rb` は
`KEEP_TAGS` と `linker.INLINE_TAGS` の両方に入れて、ルビをまたぐ語を 1 語として繋ぐ。

**ビューアで開ける拡張子は 2 か所にある。** `app.CONTENT_SUFFIXES`（一覧と読み出し）と
`viewer.js` の `OPENABLE`（リンクを辿るかの判定）。片方だけ足すと、一覧には出るのに
リンクからは開けない（またはその逆）という食い違いになる。

**ローカル `.html` には base_url が無い。** `htmlclean._safe_url()` は base_url が
空のときだけ相対 href を残す（`allow_relative`）。ここを緩めて `src` も残すと、
配信する経路が無いので壊れた画像が並ぶ。URL 経由では base_url があるので従来どおり
絶対化される。

### AI 下書き（`ai.py` と `llm.py`）

**「何を頼むか」と「誰にどう頼むか」を分けてある。** `ai.py` がプロンプトの組み立てと
後処理、`llm.py` が提供元（Claude Code CLI / Gemini API）・モデル・思考の深さ。
`ai.py` は提供元を知らず、外に触るのは `ai._generate()` の 1 か所だけ
（テストもそこを差し替える）。**提供元ごとに差し替え先が変わる作りにしないこと。**

**モデルを既定の引数に焼き込まない。** `GLOSSPOP_CLAUDE_ARGS` の既定に
`--model sonnet` が入っていたため、⚙ でモデルを選んでも**追加引数が後勝ちして
効かなかった**（設定を足したその日に踏んだ）。既定は `llm.DEFAULT_CLAUDE_MODEL`
に置き、追加引数に `--model` を書いた人の指定だけを尊重する。

**Gemini の思考の深さは世代で項目が違う。** 3 系は `thinkingLevel`、2.5 系は
`thinkingBudget` しか受けず、取り違えると 400 で落ちる（実測）。モデル名で見分けると
新しい世代が出るたびに直すことになるので、**API が「その項目は無い」と言ってきたときだけ
1 度言い直す**。**モデルの一覧も焼き込まない** —— API から引く（記憶で書いたら、
手元のキーで引いた一覧には知らない世代が並んでいた）。

**API キーは返す口を作らない。** 設定ファイルに置けるようにしてあるが、
`/api/ai/settings` が返すのは「設定済みかどうか」だけ。環境変数
（`GEMINI_API_KEY`）が優先で、その場合は UI から触らせない。

`claude -p` は API キー不要だが、**プロジェクト内で実行すると
CLAUDE.md と `gloss-add` スキルを拾い、「重複を確認するため CLI を実行させて」と
要求して承認待ちで固まる。** そのため:

- cwd は `tempfile.gettempdir()/glosspop-ai`（プロジェクト外）
- `--disallowed-tools` で全ツールを落とし、`--disable-slash-commands` を付ける
- `--bare` は使えない（ANTHROPIC_API_KEY 必須になり、OAuth を読まなくなる）

プロンプトを触るときは、カテゴリの提案が既存へ寄りすぎないか実際に確かめること
（音楽用語を「プログラミング」に入れる、他カテゴリのサブカテゴリを流用する、
といった失敗を実際に踏んだ）。

**ネタバレ対策（`spoiler`）はプロンプトだけでなく、渡すテキスト自体で担保する。**
`position` は AI を呼ばない（`/api/ai/draft` が claude 抜きで返す唯一の経路）。
`first` は `context_up_to_first()` が初出の前後だけを切り出す。**後ろは文字数ではなく
段落の切れ目で打ち切る** — 初出が章末にあると N 文字取るだけで次の章まで入り、
「読んでいないはずの展開」が要約に出る（実際にテストで踏んだ）。プロンプトの禁止文だけに
頼らないこと。

**抽出は「何を抜き出すか」を先に決めさせる（`EXTRACT_KINDS`）。** ただ
「辞書化する価値のある語」と頼むと、AI は**語義を説明できる語**ばかり挙げ、
**登場人物がまるごと落ちる**。人名は「意味の分からない語」ではないので、その基準に
引っかからない。種別ごとに独立した枠を与え、プロンプトで**枠の振り替えを明示的に
禁じる**ことでしか埋まらない（「集まらなくても他へ回さない」の一文を消さないこと）。

**上限も種別ごとにかける**（`allocate_quota()`）。合計に対して先頭から切ると、AI が
人物を先に並べただけで専門用語が全部消える。`filter_candidates()` の `limit` は
**合計の厳密な上限**で、種別数より小さいと 0 件の枠ができる。実際の経路
(`extract_terms()`) だけが下限を持ち上げている ——「選んだ種別が黙って出てこない」が
いちばん困るので。

**関係の下書き (`draft_relations`) は用語を作らない。** 一覧に無い語を AI が足して
きたら `filter_relations()` が落とす。ここを通すと、本文中の別の呼び方や AI の
言い換えが行き先になり、**保存はできるのにリンクも図の辺も生えない**関係ができる
（候補語で `filter_candidates()` がやっているのと同じ話）。

**まとめて登録は「抽出 1 回 + 下書き N 回」。** `extract_terms()` は候補語だけを 1 回で
挙げさせ、本文の生成は選ばれた語について `/api/ai/draft` を語数ぶん呼ぶ（クライアント側で
逐次）。ここを 1 回のプロンプトで N 語ぶんの本文まで作らせる形にすると、出力が長くなって
品質が落ちる。サーバに進捗の状態は持たせていない。

**保存先（`scope`）も AI に選ばせている。** `/api/ai/draft` に `scope: "auto"`
（既定）を渡すと、プロンプトに「保存先」の節とフォルダ名が入り、`scope` を答えさせる。
明示された値が来たときは聞かない（答えより指定が優先）。**カテゴリマスターを触るかは
解決後の `scope` で判断する** — リクエストの値で判断すると、AI がローカルを選んだのに
マスターへ登録してしまう。

**AI が挙げた候補はそのまま使わない。** `filter_candidates()` が、登録済みの語と
**文書中にその表記で現れない語**を落とす。後者を通すと「登録したのに本文でリンクにならない」
エントリができる（AI は平気で原形や言い換えを返す）。落とした語は理由つきで UI に出す。

**長い本文は頭から切らずに全体から間引く**（`sample_text()`）。`text[:12000]` のように
先頭だけ渡すと、345,000 字の小説では**冒頭 3.5% しか AI に届かない** —— 『吾輩は猫である』で
迷亭も寒月も一度も渡っておらず、「候補に挙がらない」「関係が引けない」の正体がこれだった。
窓の頭は `[0, 末尾-take]` に等間隔で置く（`len` で割ると**結びが一度も届かない**）。
切れ目には `〔中略〕` を入れ、**間引いたことをプロンプトにも書く** —— 黙って繋ぐと、
飛んだ前後を連続した話として読まれる。`combine_documents()` の 1 ファイルあたりの枠は
**下限**で、文書が少ないときは全体の枠を等分する（長編 1 冊のフォルダで冒頭 3000 字しか
読めなかった）。

**持ち時間は「出す件数」と「頼む相手」から見積もる**（`llm.estimate_timeout()`）。
既定の 180 秒では関係の下書きが**必ずタイムアウトする**（実際に踏んだ）。**所要時間は
出力トークン数（思考を含む）にほぼ比例する** —— 毎秒 90 トークン前後、関係 20 本ぶんで
12,000 トークン＝約 140 秒。起動は 5 秒しかかからないので、**そこを削っても意味は無い**。
**提供元で桁が違う**（同じ 19 語・20 本要求で Claude 258.6 秒 / Gemini 72.4 秒）ので、
`SECONDS_PER_ITEM` を提供元ごとに持つ —— 同じ見積もりを当てると、速いほうで
「駄目だった」と分かるのが遅すぎる。UI 側では本数を選ばせて経過秒を出す
（応答は最後にまとめて返るので、出さないと固まったように見える）。

**一時的な失敗だけ引き直す**（`llm.TransientError`）。高負荷 (503) やレート制限 (429) は
待てば直るのに、数分かけた下書きがその一言で丸ごと消えるのは割に合わない（Gemini で
実際に踏んだ）。**恒久的な失敗（鍵違い・モデル名の誤り）は引き直さない** —— 待たせた
うえで同じ結果になる。`timeout` は**全体の持ち時間**として扱い、残りを次の試行に配る
（1 回ごとに与えると、引き直すたびに待ち時間が倍々になる）。CLI 側は文言で見分けるので
`_TRANSIENT_MARKS` を増やしすぎないこと。

**セッションを継いでも速くならない。試す前にここを読むこと。** `claude -p` は毎回
使い捨てなので「親セッションを立てて語ごとに `--fork-session` で枝分かれさせれば、
本文を渡し直さずに済んで速いはず」と考えたくなる。**実測は逆で、遅くなった**
（2 語ぶんの下書きで 25.8 秒 → 41.3 秒）。理由は 2 つ:

- **入力の再処理はほぼ無料。** 本文 12,000 字を渡して答えさせるだけなら 4.8 秒。
  起動も 5 秒。**節約できる余地がそもそも無い**
- **文脈に本文を抱えたまま質問すると、モデルが余計に考えて出力が増える**
  （同じ下書きで 476 → 714 トークン）。所要時間は**出力トークン数（思考を含む）に
  ほぼ比例する**（毎秒 90 トークン前後）ので、そのぶん遅くなる

「2 回目は速い」という測定結果が出たら、**同じ仕事量を比べているか**を先に疑うこと
（20 本と 3 本を比べて「3.6 倍速い」と誤読した）。速くしたいなら**書かせる量を減らす**
（本数を絞る）のが唯一効く手で、渡す本文を削っても効かないうえ、関係が引けなくなった
原因（本文が足りない）に逆戻りする。

なお仮に速くなったとしても、`--resume` は**前の回の本文を文脈に残す**ので
**ネタバレ設定をまたいで継いではいけない** —— この辞書のネタバレ抑止は「渡すテキスト
そのもので担保する」（プロンプトの禁止文に頼らない）という約束の上に立っているので、
継いだ瞬間に「初出の場面だけ」の保証が嘘になる。

**初出モードの本文は、全員ぶんが入るように配分する**（`first_scene_context()`）。
初出窓（1 語あたり最大 2,400 字）をそのまま繋いで頭から切っていたため、19 語では
**52% が黙って落ち、後ろに並んだ主人・吾輩・迷亭・黒 が丸ごと消えていた**。関係は
2 語が揃って初めて書けるので、片方が消えた時点でその関係は**絶対に出てこない**
（「初出モードでは関係が出ない」の正体がこれ）。切り詰めるときは
`_densest_window()` が**相手の名前も写っているところ**を残す —— 頭から取ると、その語が
出てくる前の場面ばかりが残って関係が読めない。**初出窓の外は見ない**ので、ネタバレの
約束は変わらない（テストが実際に窓の内側であることを確かめている）。

**関係の下書きに渡すのは「登録済みの語が一緒に出てくる場面」**（`cooccurrence_context()`）。
関係が書いてあるのは 2 つの名前が近くに並ぶところであって、文書の冒頭ではない。
ここを `combine_documents()` の頭出しに戻すと、長編では関係の場面が一度も届かず
**「見つかりませんでした」しか返さない**（それが実際の壊れ方だった）。窓は**別名も同じ
エントリとして**数え、重なる窓は捨てる（同じ場面を何度渡しても新しい関係は出ない）。

**同じものの別の呼び方は新しいエントリにしない**（`split_aliases()` → `/api/aliases`）。
「主人」と「苦沙弥先生」が別エントリに割れると、本文のリンク先も相関図のノードも二重になる。
抽出のプロンプトに `alias_of` を答えさせ、**行き先が 1 つに決まるときだけ**別名の枠に回す
（決まらないときは黙って寄せず、普通の候補として扱う）。**本文に無い表記は別名にもしない** ——
候補語と同じで、登録してもリンクにならない。

## 確認用スクリプト

`check.cmd`（メニュー / `test` `app` `build` `rebuild` `exe` `kill` `all`）と
`packaging/check-exe.ps1`。**大半の変更はビルドせずに確認できる** — 凍結でしか
変わらないのは `config.DATA_ROOT`・`cmd_serve` の文字列 import・`picker` の子プロセス・
`entry.py` の既定引数の 4 か所だけで、どれも `sys.frozen` を差し替えるテストで見られる。
ビルドが要るのは **`hiddenimports` / `datas` の漏れ**だけ（静的解析で追えないものが対象なので、
原理的に凍結しないと出ない）。手元で繰り返すときは `build.ps1 -Fast`（`--clean` を付けない。
48 秒 → 21 秒。**spec や依存を変えたときは付けないこと**）。

**`.cmd` / `.bat` は ASCII のみ、改行は CRLF。** `cmd.exe` はコンソールのコードページ
（日本語環境なら CP932）で読むので UTF-8 の日本語は化け、**2 バイト目が `0x5C` になる文字
（`表` `十` `ソ` など）は構文解析を壊す**。LF だとラベルや複数行が壊れる（`.gitattributes`
で `eol=crlf` に固定してある）。日本語を書きたい処理は `.ps1` 側に置くこと。

**`.ps1` は UTF-8 BOM 付きで保存する。** Windows PowerShell 5.1 は BOM が無いと ANSI
（CP932）として読むので、日本語の文字列リテラルが壊れて構文エラーになる。`check.cmd` は
`pwsh` があればそちらを使うが、5.1 に落ちても動くように BOM を付けてある。**BOM を外さないこと。**

**exe の確認は「起動の前後の差」で見る。** 専用ウィンドウが開いたかを「いま開いている窓の数」で
判定すると、手元で試した窓が残っているだけで落ちる（実際に踏んだ）。子プロセス（`--type=` 付きの
renderer / gpu）も数に入るので、ブラウザ本体だけを見ること。

## Windows ビルド（`packaging/`）

`.\packaging\build.ps1` で PyInstaller の onedir ビルド（`dist\GlossPop\`）を作る。
手順は README にある。ここには**壊しやすい点**だけ:

**凍結すると `__file__` は一時展開先（`_internal`）を指す。** そのため
`config.APP_DIR` は `sys.frozen` のとき `sys.executable` の親を基準にする。
ここを `PACKAGE_DIR` 基準に戻すと、**保存した辞書が起動ごとの一時ディレクトリに
書かれて消える**（サーバは正常に動いて見えるので気付きにくい）。逆に `STATIC_DIR` は
`_internal` 側で正しいので `PACKAGE_DIR` 基準のまま。

**`APP_DIR` と `DATA_ROOT` は別物。** `APP_DIR` は更新のたびに入れ替わる側、
`DATA_ROOT` は残る側で、既定では同じ場所を指す。ここを混ぜると、保存先を外に
移す設定（⚙）が意味を失う。

**設定ファイルは `APP_DIR` の下に置かない**（`config.SETTINGS_FILE`）。中身は
「データをどこに置くか」なので、アプリのフォルダを丸ごと入れ替えたときに一緒に
消えたら意味が無い。OS のユーザー領域に置いてある。テストは
`GLOSSPOP_SETTINGS_FILE` で逃がすこと —— **本物の `%APPDATA%` に書くテストを
足さない**。

**優先順は 環境変数 > 設定ファイル > 既定。** 環境変数を最優先にしているのは、
テストと一時的な切り替えが設定ファイルに引きずられないようにするため。
壊れた設定ファイルでは既定に落ちる（起動できなくならないこと）。

**保存先の変更は次の起動から効く。** `config` は import 時に解決済みで、途中で
差し替えると `store` のキャッシュや開いているフォルダの状態と食い違う。
`/api/settings` は設定ファイルを書くだけにして、UI に再起動を促している。
**ここを「その場で反映」に変えないこと。**

**データの複製 (`copy_data_root`) は元を消さない。** 移した先で問題が出たときに
戻れるようにするため。ブラウザプロファイルのキャッシュは運ばない（名前に `Cache` /
`Crashpad` を含むディレクトリ）が、**`Local Storage` は運ぶ** —— お気に入り・
最近開いたフォルダ・ネタバレ設定がそこにあり、落とすと「辞書は無事なのに設定だけ
消えた」になる。

**文字列 import は凍結後に解決できない。** `cmd_serve()` は `--reload` のときだけ
`"glosspop.app:app"` を渡し、通常は app オブジェクトを直接渡す。ここを文字列に
戻すと exe が `Could not import module "glosspop.app"` で即死する。

**動的に読むものを足したら spec に足す。** 静的解析で追えないものは
`packaging/glosspop.spec` の `hiddenimports` / `datas` に書く必要がある
（`glosspop.*` は `collect_submodules` でまとめて入れてある）。新しい依存を
`pyproject.toml` に足したときは、**ビルドして exe を起動するところまで確認する**
——`uv run` では動いても凍結すると落ちる、という差が出るのはここだけ。

## フロントエンド

依存ゼロの ES モジュール。ビルド工程は無い。`static/` を編集したら
サーバ再起動は不要（ファイルはディスクから読まれる）。

`/static` と HTML には `Cache-Control: no-cache` を付けている。これが無いと
ブラウザが古い JS を出し、原因不明の壊れ方をする（実際に半日溶かした）。
`RevalidatingStatic` を消さないこと。

- `base.js` — API 呼び出し、DOM ヘルパ、URL 判定
- `select-add.js` — 選択 →「＋ 辞書に登録」。ビューアと辞書ページで共用
- `progress.js` — 読書位置の記憶。ビューアからだけ使う
- `extract.js` — 「✨ 用語を抽出」→ 候補選択 → 下書き → まとめて保存
- `popup.js` / `editor.js` — 吹き出し・登録ダイアログ。どのページからも使える
- `speech.js` — 本文の読み上げ（Web Speech API）
- `relations-draft.js` — 関係の AI 下書き → まとめて書き込み
- `settings.js` / `update.js` — ⚙ の中身と更新のお知らせ。全ページの topbar に
  自分で差し込むので、各ページの JS からは import しない（HTML に script タグを
  1 行足すだけ。`settings.js` が `update.js` を import している）
- `viewer.js` / `glossary.js` / `entry.js` / `graph.js` / `doctor.js` — 各ページ

**相関図の配置に力学モデルを使わない**（`graph.js`）。カテゴリという階層があり、
`rank` で段が決まるので決定的に置ける。乱数も収束待ちも無いぶん、同じ辞書なら毎回
同じ絵になる。段が 1 枚しかないときだけ円に置く —— 横一列に並べると辺が全部
重なって読めない。`levelsOf()` の緩和はノード数で打ち切ってあるので、上下関係に
閉路があっても止まらなくはならない。

**読書位置は「本文を描き替える前に」書き出す**（`progress.js` の `switchTo()`）。
描き替えたあとに呼ぶと、**新しい本文の位置を前の文書の鍵で保存する**。`setSource()` の
先頭で呼んでいるのはそのため。位置を段落の番号で持つのは、px だと窓の幅・文字サイズ・
表示オプションで動くから。

**鍵が作れない文書は覚えない**（`keyFor()` が `null` を返す）。ファイル選択・ドロップ・
貼り付けにはブラウザが絶対パスをくれないので、ファイル名しか手掛かりが無い。ここを
「ファイル名で覚える」に緩めると、**同名の別ファイルを同じ鍵で上書きする**。
初出ジャンプ (`?term=`) のときも戻さない —— 行き先が決まっているので、戻しても
すぐ上書きされ、「前回の続き」の案内だけが嘘になる。

**横断検索は索引を持たない**（`/api/content-search`）。索引にすると、外のエディタで
書き換えられたファイルとずれる。**打ち切りは必ず返すこと**（`files_truncated` /
`hits_truncated` / `skipped`）—— 黙って切ると「この語は無かった」と区別が付かない。
ヒットへ寄せるのは `[data-gloss]` ではなく**見えているテキスト**で探す（検索語は
辞書に無いことのほうが多い）。

**用語で探すとき（`?ref=`）は `Linker` の規則を使う。** 素の部分一致に戻すと
`API` が `rapid` に当たり、**リンクにならない語を「出てくる」と言う**ことになる。
`Linker.finditer()` が `annotate()` と同じ正規表現を素のテキストに当てる口。

**使用例に貼る文は文字数ではなく文の切れ目で採る**（`_sentence`）。抜粋
（`_snippet`）を貼ると「…」つきの半端な文が辞書に溜まる。

**目次は位置を作り直さない**（`viewer.js` の `locateSections`）。章の見出しは
`documents.py` がすでに本文へ入れている（epub は元の見出し / `第 N 章`、pdf は
`【p.42】`）ので、描き終わった本文を頭からなぞって段落に対応づける。アンカーを
埋める形に変えると、`htmlclean` が `id` を落とすところに手を入れることになる。
**探すときは前の節より後ろだけを見る** —— 章の名前は本文にも出てくるので、全体から
最初の一致を採ると手前へ飛ぶ。対応づけられなかった数は画面に出す。

**タグにマスターは無い。** `/api/tags` が使われているものを数え上げる。`categories.yaml`
に相当するものを作ると、用語 0 件のタグが残って掃除の口が要る。用語ページの `#タグ` は
`?tag=`（完全一致）へ飛ばすこと —— `?q=` に戻すと、**そのタグ名が本文に出るだけの語**
まで混ざる。

**読み上げは非同期の癖が 2 つある**（`speech.js`）。どちらも実際に踏んだ:

- **`speechSynthesis.cancel()` は直前の発話の `onend` / `onerror` を後から飛ばす。**
  真偽値のフラグを cancel の直後に戻すと、遅れて来た古いイベントを正常終了と誤読して
  段落を二重に進める。発話ごとに世代（`seq`）を持たせて照合すること
- **`pause()` / `resume()` は同期的に `speechSynthesis.paused` を変えない。**
  呼んだ直後に読むと前の値が返り、ボタンの表示が 1 手遅れる。状態は自前で持つ

なお「1 発話が 15 秒を超えると無音のまま止まる」という Chromium の既知の癖は
**この環境では再現しなかった**（106 文字 = 20.2 秒でも鳴り切った）。報告は多いので
`MAX_CHUNK` で文単位に切って渡してあるが、**そこを疑う前に「単に読み上げに時間が
かかっているだけ」を先に疑うこと**（20 秒かかる段落を 12 秒で「停止」と誤診した）。

**SVG の細い線はそのままでは押せない**（`graph.js`）。辺には透明な太い線
（`.rel-edge-hit`）を下に重ねて当たり判定にしてある。消すと「押しても何も起きない」
になり、HTML も JS も正しいので原因が見えない。

**関係の上下（`RANK_OPTIONS` / `RANK_MARK`）の正は `base.js`。** 用語ページ・関係の
下書き・相関図の 3 か所が同じ表示をする。値は `models.RANKS` と同じ文字列でないと
サーバが弾くので、各ファイルに写しを作らないこと。

**カテゴリ名を含む文字列を空白で割らない。** カテゴリ名には**空白を使える**ので、
`` `${scope} ${category}` `` を `split(" ")` で戻すと「銀河 鉄道」が切れる。相関図の
カテゴリ選択は `/`（カテゴリ名では禁止）で 1 回だけ割っている。同じ理由で、ref を
つなぐ鍵の区切りには `<>`（カテゴリ名でも slug でも弾かれる）を使う。
スモークテストが空白入りのカテゴリ名で見張っている。

**辞書の取り込みは置き換え。消す前に必ず控えを取る**（`archive.py`）。ここは
**このリポジトリで唯一データを消す経路**なので、控えを「先にバックアップしてください」
という注意書きに落とさないこと（`copy_data_root` も `move` も `installer` も
「元を消さない」で通してきたぶん、ここだけ手順に頼ると必ず抜ける）。控えは書き出しと
**同じ zip** なので、そのまま取り込み直せば戻る —— 別形式にしないこと。

入れ替えは**ディレクトリごと**（`glossary` → `glossary.replaced-<時刻>`、
`glossary.incoming` → `glossary` の 2 回の rename）。1 ファイルずつ消して書く形に
変えると、途中で失敗したときに**半分だけ新しい辞書**が残る。

**取り込む zip の形は必ず確かめる**（`inspect()`）。`glossary/<カテゴリ>/<slug>.md`
の 2 段が無ければ通さない。緩めるとアプリ本体の zip を取り込ませたときに
**辞書が空で置き換わる**。展開先の外に出る要素は `installer.safe_members()` で弾く
（外から来た書庫をライブラリ任せにしない、は更新の zip と同じ規則）。

**含めるのは全体の辞書とカテゴリマスターだけ。** ローカル辞書と URL 辞書を足すと、
置き換えで消える範囲が広がる。増やすなら、消える範囲が広がることを UI に出すこと。

**新しい版で自分自身を置き換えない**（`installer.py`）。動いている exe は差し替え
られず、署名なしバイナリの自己書き換えはウイルス対策ソフトに嫌われ、`Program Files`
では昇格も要る。隣に展開して「そちらを起動してください」と言うだけにしてある
（旧フォルダがそのまま戻り先になる）。**ここを「自動で入れ替える」に変えないこと。**

**外から来た zip をライブラリ任せに展開しない。** `safe_members()` が絶対パス・`..`・
シンボリックリンク・展開先の外に出る要素を自分で弾く。展開先の名前もタグをそのまま
使わず、組み立てた結果がアプリと同じ階層に収まっていることを確かめる。途中で失敗
したら書きかけを消す —— **半端なフォルダを残すと、それを起動されかねない。**

**更新でいちばん怖いのは「辞書が消えた」に見えること。** 新しい版を隣に展開して
既定のまま起動すると、データは旧フォルダに残ったままになる。`find_data_candidates()`
が隣を見て候補を返し、⚙ の先頭に出す。**いま中身があるときは黙る**（普通に使って
いる人に余計なものを出さない）。

**引き継ぎも保存先の移動も元を消さない。** 移した先で問題が出たときに戻れるように
するため。旧フォルダをどうするかは案内するだけで、こちらでは片付けない。

**テーマのダークの値は 2 か所にある**（`style.css`）。CSS にブロックを再利用する
手段が無いので、`@media (prefers-color-scheme: dark)` 側と `:root[data-theme="dark"]`
側に同じ内容を書いている。**片方だけ直すと「OS がダークのときだけ色が違う」**という
気づきにくい壊れ方をする。スモークテストが両方を実際に描かせて突き合わせている。

**テーマは描画の前に当てる。** 各 HTML の `<head>` に 3 行のインライン script が
あり、localStorage を読んで `data-theme` を立てる。モジュールの読み込みを待って
当てると、暗いテーマのときページを開くたびに一瞬白く光る。キー名は `base.js` の
`THEME_KEY` と同じものを書いてあるので、変えるときは両方直すこと。

**ダイアログの listener は最初の `await` より前に付ける。** ダイアログは開いた瞬間
から操作できるのに、読み込みを待ってから付けると**その間の操作が黙って無視される**
（設定のテーマを選んでも何も起きなかった）。`extract.js` が close イベントを
取り逃がした件と同じ形で、これで 2 回目。

**外へ通信するのは `updates.py` と、Gemini を選んだときの `llm.py` だけ。**
`llm` のほうは**利用者が明示的に選んだときしか動かない**（既定は Claude Code CLI で、
外に出るのは CLI 側）。増やすときは、切れること（`GLOSSPOP_UPDATE_CHECK=0` と ⚙）と、
**lifespan で叩かないこと**を守る。lifespan に置くと起動のたびに勝手に出ていくうえ、
`TestClient` が lifespan を走らせるので**テストが外へ通信する**。`/api/update` を
叩かれたときだけ動く形にしてある。確認の間隔（1 日）は設定ファイルの時刻で持つので、
再起動を繰り返しても増えない。失敗は**覚えない** —— 覚えると次に開いても再試行しなくなる。

**テストは実ネットワークを塞いである**（conftest の `forbid_real_network`）。
塞ぐのは実 transport だけで、`TestClient` の `ASGITransport` には影響しない。
`httpx.Client.send` ごと潰すと TestClient も死ぬので、そこを塞ぎ直さないこと。

**バージョン比較は読めないものを「新しくない」に倒す。** 読めない文字列で
「更新があります」と出すほうが害が大きい（ユーザーは確かめようがない）。

**`.rel-sub` を見出しに使い回さない。** `text-transform: uppercase` が付いているので、
ファイル名に当てると `銀河.md` が `銀河.MD` になる（実際に踏んだ）。日本語しか入らない
小見出し専用。

**`node.hidden = true` はそれだけでは効かない。** `button` などに `display` を
指定しているので UA スタイルの `[hidden]` が負ける。`style.css` の先頭近くで
`[hidden] { display: none !important; }` を当てて打ち消している。**要素ごとに
`.foo[hidden] { display: none }` を足す書き方を増やさないこと**（この規則より前は
そうなっていて、新しい UI を足すたびに「hidden にしたのに出たまま」を踏む）。

**設定はタブで分けてある**（`settings.js` の `showTab`）。**フッタの「保存」は
データの保存先のもの**なので、AI タブでは出さない —— 出しっぱなしにすると、AI を
変えたあとにこちらを押して「保存したのに効かない」になる（AI 側は自前の保存ボタンを
持ち、押した時点で効く）。タブの色は**既存の変数だけで作る** —— 新しい色を足すと、
ダークの定義が 2 か所あるぶん片方だけ直す事故が起きる。

**`prompt()` / `alert()` を入力 UI に使わない。** 一度使って作り直した。値の入力は
インラインの `<input>`/`<select>` にする（`confirm()` は削除確認にだけ使っている）。

## テスト

`tests/conftest.py` の autouse fixture が `config.GLOSSARY_DIR` などを tmp_path に
差し替える。**`store._ready` のリセットも fixture がやっている** ので、
`store.ensure_ready()` の初期化をテストで当てにするなら fixture を確認すること。

**`claude` の有無をテストの前提にしない。** 手元は PATH に `claude` があるので
`ai.available()` が真になり、AI 経路のエンドポイントは 503 を返さず先まで進む。
CI には無いので 503 で止まり、**手元で通ったテストだけが向こうで落ちる**
（実際にリリースのワークフローで踏んだ）。AI 経路を叩くテストでは
`monkeypatch.setattr(ai, "available", lambda: True)` を明示すること。
手元で CI と同じ状況を作るなら、`GLOSSPOP_CLAUDE_BIN` を空にして PATH からも外す。

**タグを打つ前に `.\check.cmd ci` を通す**（`packaging/check-ci.ps1`）。手元では
通って CI で落ちる失敗を 2 回続けて踏み、そのたびに壊れたタグを消して付け直した。
見ているのはバージョンの一致、**`claude` を PATH から外した**テスト、時間に依存する
テストの繰り返し。**`uv` は絶対パスで呼ぶこと** —— `claude` と同じフォルダに入って
いることがあり、PATH から外すと `uv` まで消える（実際に踏んだ）。

繰り返しの掃き出しは**網であって証明ではない**。20 回に 1 回落ちるものは 40 回でも
1 割ほど取り逃がす。確実にしたいなら回数を増やすのではなく、テストを決定的に書く。

**時間で競走するテストを書かない。** 「別スレッドで遅れて listen させて、繋がるのを
待つ」形のテストが 20 回に 1 回落ち、**リリースのワークフローを止めた**。再試行の
回数だけを見れば十分なら、`create_connection` を差し替えて回数を数えること。
本物のソケットを使うのは、状態が最初から確定しているとき（既に listen 済み /
誰も listen していない）だけにする。

**フロントは `tests/test_smoke_ui.py` が通しで見張っている。** 本物のサーバを
別スレッドで立てて Chrome で操作し、「登録 → 本文でリンクになる → 吹き出し →
関係 → 相関図 → 点検」まで走る。HTML は正しいのに JS が落ちている、という壊れ方は
ここでしか捕まらない（実際に 3 件見つけた）。

- **手元の Chrome を使う**（`channel="chrome"`）。ブラウザを別途落とさせない
- Chrome も playwright も無ければ丸ごと skip する。**通常の `uv run pytest` を
  止めないこと**が条件
- ページの JS 例外は `pageerror` で拾って失敗させている。画面に出ない例外を通さない
- **本体が落ちたときの手掛かりは teardown で `print` する**（`_watch_page`）。
  JS の例外・console の error・4xx / 5xx と繋がらなかった要求を集めてある。
  **fixture はテスト本体の例外を受け取れない** —— pytest は例外を generator へ
  投げ込まず teardown を普通に進めるので、`except` で拾って `add_note` する手は
  効かない（`else` のほうが走る。実際に書いて確かめた）。失敗したテストの出力は
  pytest が「Captured stdout teardown」として出すので、そこへ流している。
  これが無いと「要素が 15 秒出てこない」だけが残り、**原因が消える**
- 要素は `data-ref` で掴む。ボタンの文言で掴むと、文言を変えるたびに落ちる
- **待つ対象を広く取らない。** `svg.rel-graph` の `textContent` にはノードの
  `<title>` に入れた要約まで混ざるので、「図に『級友』が出るまで待つ」が**最初から
  成立**して保存を待たずに検証していた（実際に踏んだ。テストは通るのに何も見ていない）。
  `.rel-edge-label` のように、変わる要素だけを見ること
- SVG の `<text>` は HTMLElement ではないので `inner_text()` が使えない。`text_content()`

スモークテストが届かないところ（AI を呼ぶ経路など）は、これまでどおり手で
Playwright を動かして確かめる。

## 変更時に一緒に直すもの

| 変えたもの | 一緒に直す |
| --- | --- |
| 辞書のスキーマ / CLI 引数 | `.claude/skills/gloss-add/SKILL.md` |
| 関係 (`relations`) の項目 | SKILL.md の「関係を書く」、README の「関係と相関図」、`base.js` の `RANK_OPTIONS` / `RANK_MARK` |
| 抽出の種別 (`EXTRACT_KINDS`) | README の「まとめて登録する」の表 |
| 設定に足した項目 | README の「設定」、`packaging/release-notes.md` の「できること」、`settings.js` の `PATH_LABELS`（場所を足したとき） |
| AI の提供元・モデル・思考の深さ | `llm.py`（`PROVIDERS` / `EFFORTS` / `CLAUDE_MODELS`）、README の「使う AI」と環境変数の表、`settings.js` の「AI」節 |
| 更新まわりの挙動 | README の「更新のしかた」、`packaging/release-notes.md` の「更新するとき」 |
| 自動リンクの規則 | README.md の「自動リンクの規則」、`content/ようこそ.md` |
| カテゴリ名の制約 | `models.normalize_category()`、README、SKILL.md、`ai.build_prompt()` |
| 依存の追加 / 動的 import・データファイルの追加 | `packaging/glosspop.spec`（ビルドして exe 起動まで確認） |
| 機能を足した / 設計の判断をした | `docs/open-questions.md`（**片付いた宿題は消す** —— 残ると「まだ無いもの」に見える）、`docs/design-notes.md`（決めたことと、その代償を書く） |
| バージョン | `pyproject.toml` と `glosspop/__init__.py` の**両方**（タグと不一致だと release ワークフローが落ちる）。`release-notes.md` の版番号の例も見る |
